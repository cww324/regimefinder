dataset_defaults:
  primary_symbols:
  - BTC-USD
  secondary_symbols:
  - ETH-USD
  timeframe: 5m
  horizon_default_bars: 6
  lookback_default_days: 365
gates:
  baseline_cost_modes:
  - gross
  - bps8
  - bps10
  baseline_cost_config:
    gross:
      fee_bps: 0
      slip_bps: 0
    bps8:
      fee_bps: 0
      slip_bps: 8
    bps10:
      fee_bps: 0
      slip_bps: 10
  walkforward:
    train_days: 60
    test_days: 15
    step_days: 15
  bootstrap:
    iterations: 3000
    ci: 0.95
    report_metrics:
    - mean_ci_low
    - mean_ci_high
    - p_mean_gt_0
hypotheses:
- id: H15
  family: mean_reversion
  class: tradeable_hypothesis
  description: Frozen BTC mean-reversion entry (MR2) with ETH 1h trend confirmation.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - 'BTC entry condition: vwap_z <= -2.0.'
  - 'ETH confirmation condition: ETH 1h close > EMA20 and EMA20 slope(3) > 0.'
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC VWAP z-score
  - ETH-USD 5m closes
  - ETH 1h EMA20 and EMA20 slope(3)
  parameters:
    horizon_bars: 6
    fixed:
      btc_vwap_z_entry_max: -2.0
      eth_confirm_timeframe: 1h
      eth_ema_span: 20
      eth_ema_slope_lag_hours: 3
  allowed_variants: []
  status: frozen
- id: H16
  family: shock_structure
  class: tradeable_hypothesis
  description: Frozen ETH shock lead-lag continuation test toward BTC horizon return.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - Trigger when ETH absolute 1-bar return is in top decile of rolling 20-day window.
  - Trade BTC in ETH shock-bar direction (continuation test).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - ETH-USD 5m closes
  - BTC-USD 5m closes
  - ETH 1-bar absolute return rolling decile rank
  parameters:
    horizon_bars: 6
    fixed:
      shock_window_bars: 5760
      shock_percentile_threshold: 0.9
  allowed_variants: []
  status: frozen
- id: H17
  family: shock_structure
  class: structural_diagnostic
  description: Frozen non-directional ETH shock magnitude map for BTC move-size conditioning.
  rationale: Stats-only module used as conditioning reference for H18.
  entry_rules:
  - Condition on ETH absolute 1-bar return top decile over rolling 20-day window.
  - Measure BTC forward absolute move magnitude at fixed horizon.
  exit_rules:
  - No trading signal output; diagnostics only.
  required_inputs:
  - ETH-USD 5m closes
  - BTC-USD 5m closes
  - ETH 1-bar absolute return rolling decile rank
  parameters:
    horizon_bars: 6
    fixed:
      shock_window_bars: 5760
      shock_percentile_threshold: 0.9
      output_mode: magnitude_only
  allowed_variants: []
  status: frozen
- id: H18
  family: mean_reversion
  class: tradeable_hypothesis
  description: Frozen H15 directional entry gated by frozen H17 volatility-conditioning
    module.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - Apply H15 BTC/ETH directional entry exactly as frozen.
  - 'Require frozen H17 conditioning gate: ETH absolute shock in top decile.'
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - H15 frozen signal state
  - H17 frozen conditioning gate
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  parameters:
    horizon_bars: 6
    fixed:
      base_signal: H15
      conditioning_module: H17
  allowed_variants: []
  status: frozen
- id: H19
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Frozen ETH-BTC relative momentum spread tail regime classifier.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - Compute ETH-BTC 1h relative momentum spread using 6h lookback.
  - Compute rolling spread percentile rank.
  - Classify short_tail when spread_pct < 0.10; long_tail when spread_pct >= 0.90;
    neutral otherwise.
  exit_rules:
  - Use regime output for gating downstream hypotheses.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - BTC/ETH 1h returns
  parameters:
    horizon_bars: 6
    fixed:
      relative_momentum_lookback_hours: 6
      spread_percentile_window_bars: 2000
      short_tail_threshold: 0.1
      long_tail_threshold: 0.9
  allowed_variants: []
  status: frozen
- id: H20
  family: mean_reversion
  class: tradeable_hypothesis
  description: Frozen H18 entry gated by H19 long-tail regime.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - 'Base signal: frozen H18 long entry.'
  - 'Gate: H19 long-tail regime (spread_pct >= 0.90).'
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - H18 frozen signal state
  - H19 frozen regime state
  parameters:
    horizon_bars: 6
    fixed:
      regime_gate: long_tail
      regime_threshold: 0.9
  allowed_variants: []
  status: frozen
- id: H21
  family: mean_reversion
  class: tradeable_hypothesis
  description: Frozen H18 entry gated by H19 short-tail regime.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - 'Base signal: frozen H18 long entry.'
  - 'Gate: H19 short-tail regime (spread_pct < 0.10).'
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - H18 frozen signal state
  - H19 frozen regime state
  parameters:
    horizon_bars: 6
    fixed:
      regime_gate: short_tail
      regime_threshold: 0.1
  allowed_variants: []
  status: frozen
- id: H22
  family: mean_reversion
  class: tradeable_hypothesis
  description: Frozen H18 entry gated by H19 neutral regime.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - 'Base signal: frozen H18 long entry.'
  - 'Gate: H19 neutral regime (0.10 <= spread_pct < 0.90).'
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - H18 frozen signal state
  - H19 frozen regime state
  parameters:
    horizon_bars: 6
    fixed:
      regime_gate: neutral
      regime_low_inclusive: 0.1
      regime_high_exclusive: 0.9
  allowed_variants: []
  status: frozen
- id: H23
  family: mean_reversion
  class: tradeable_hypothesis
  description: Frozen H15 entry gated by H19 neutral regime.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - 'Base signal: frozen H15 long entry.'
  - 'Gate: H19 neutral regime (0.10 <= spread_pct < 0.90).'
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - H15 frozen signal state
  - H19 frozen regime state
  parameters:
    horizon_bars: 6
    fixed:
      regime_gate: neutral
      regime_low_inclusive: 0.1
      regime_high_exclusive: 0.9
  allowed_variants: []
  status: frozen
- id: H24
  family: mean_reversion
  class: tradeable_hypothesis
  description: Frozen H15 entry gated by H19 long-tail regime.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - 'Base signal: frozen H15 long entry.'
  - 'Gate: H19 long-tail regime (spread_pct >= 0.90).'
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - H15 frozen signal state
  - H19 frozen regime state
  parameters:
    horizon_bars: 6
    fixed:
      regime_gate: long_tail
      regime_threshold: 0.9
  allowed_variants: []
  status: frozen
- id: H25
  family: mean_reversion
  class: tradeable_hypothesis
  description: Frozen H15 entry gated by H19 short-tail regime.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - 'Base signal: frozen H15 long entry.'
  - 'Gate: H19 short-tail regime (spread_pct < 0.10).'
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - H15 frozen signal state
  - H19 frozen regime state
  parameters:
    horizon_bars: 6
    fixed:
      regime_gate: short_tail
      regime_threshold: 0.1
  allowed_variants: []
  status: frozen
- id: H26
  family: mean_reversion
  class: tradeable_hypothesis
  description: Frozen H15 entry gated by H19 mid regime.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - 'Base signal: frozen H15 long entry.'
  - 'Gate: H19 mid regime (0.25 <= spread_pct < 0.75).'
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - H15 frozen signal state
  - H19 frozen regime state
  parameters:
    horizon_bars: 6
    fixed:
      regime_gate: mid
      regime_low_inclusive: 0.25
      regime_high_exclusive: 0.75
  allowed_variants: []
  status: frozen
- id: H27
  family: shock_structure
  class: tradeable_hypothesis
  description: Frozen liquidity shock continuation hypothesis.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - Trigger when range > 1.8x median(range,20) and volume > 1.8x median(volume,20).
  - Trade in shock-bar direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  parameters:
    horizon_bars: 6
    fixed:
      range_multiple: 1.8
      volume_multiple: 1.8
      median_window_bars: 20
  allowed_variants: []
  status: frozen
- id: H28
  family: range_structure
  class: tradeable_hypothesis
  description: Frozen liquidity sweep reversal hypothesis.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - Detect break of prior 12-bar high/low.
  - Require close back inside breakout range within 2 bars.
  - Fade failed break direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLC
  parameters:
    horizon_bars: 6
    fixed:
      breakout_lookback_bars: 12
      reentry_within_bars: 2
  allowed_variants: []
  status: frozen
- id: H29
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Frozen H19 neutral-entry state transition signal.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - Trigger when H19 enters neutral regime from non-neutral state.
  - 'Neutral regime definition: 0.10 <= spread_pct < 0.90.'
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - H19 frozen regime state
  - BTC-USD 5m closes
  parameters:
    horizon_bars: 6
    fixed:
      regime_low_inclusive: 0.1
      regime_high_exclusive: 0.9
      transition_required: true
  allowed_variants: []
  status: frozen
- id: H30
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Frozen ETH/BTC 1h sign-divergence continuation hypothesis.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - Trigger when ETH 1h slope sign != BTC 1h slope sign.
  - Trade BTC in ETH slope sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - ETH 1h EMA20 slope sign
  - BTC 1h EMA20 slope sign
  - BTC-USD 5m closes
  parameters:
    horizon_bars: 6
    fixed: {}
  allowed_variants: []
  status: frozen
- id: H31
  family: cross_asset_regime
  class: structural_diagnostic
  description: Frozen conditional distribution map by ETH 1h slope sign x H19 regime.
  rationale: Stats-only mapping logged as non-tradeable.
  entry_rules:
  - Condition BTC h=6 returns on (ETH 1h EMA20 slope sign, H19 regime bucket).
  exit_rules:
  - No trading signal output; diagnostics only.
  required_inputs:
  - ETH 1h EMA20 slope sign
  - H19 regime bucket
  - BTC-USD 5m closes
  parameters:
    horizon_bars: 6
    fixed:
      output_mode: conditional_distribution
  allowed_variants: []
  status: frozen
- id: H32
  family: cross_asset_regime
  description: Frozen H32 baseline (see FINDINGS_SIMPLIFIED.md and FINDINGS_TECHNICAL.md).
  rationale: Reference frozen validated research lineage without altering logic.
  entry_rules:
  - See existing frozen H32 rule definition in findings files.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - H19 short_tail regime state
  parameters:
    horizon_bars: 6
    fixed: {}
  allowed_variants: []
  status: frozen
- id: H33
  family: cross_asset_regime
  description: Frozen H33 symmetry mirror of H32 (see findings files).
  rationale: Reference frozen symmetry validation without altering logic.
  entry_rules:
  - See existing frozen H33 mirror-rule definition in findings files.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - H19 short_tail regime state
  parameters:
    horizon_bars: 6
    fixed: {}
  allowed_variants: []
  status: frozen
- id: H34
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Frozen short-tail directional rule gated to top 30% absolute ETH 1h
    slope.
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - Require H19 short-tail regime (spread_pct < 0.10).
  - Require |ETH 1h EMA20 slope| in top 30% tail.
  - Trade BTC in ETH slope sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - H19 frozen regime state
  - ETH 1h EMA20 slope sign and absolute magnitude rank
  - BTC-USD 5m closes
  parameters:
    horizon_bars: 6
    fixed:
      regime_gate: short_tail
      regime_threshold: 0.1
      abs_slope_percentile_min: 0.7
  allowed_variants: []
  status: frozen
- id: H35
  family: cross_asset_regime
  class: structural_diagnostic
  description: Frozen short-tail decile maps for absolute and signed ETH 1h slope.
  rationale: Stats-only conditional map with no trade signal.
  entry_rules:
  - In H19 short-tail regime, bin ETH slope by absolute and signed deciles.
  - Report BTC h=6 conditional means by bin.
  exit_rules:
  - No trading signal output; diagnostics only.
  required_inputs:
  - H19 frozen regime state
  - ETH 1h EMA20 slope series
  - BTC-USD 5m closes
  parameters:
    horizon_bars: 6
    fixed:
      output_mode: decile_map
      regime_gate: short_tail
  allowed_variants: []
  status: frozen
- id: H36
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Frozen short-tail bottom signed-slope decile rule (BTC short).
  rationale: Legacy frozen definition transcribed from findings to remove pointer-only
    stubs.
  entry_rules:
  - Require H19 short-tail regime (spread_pct < 0.10).
  - Require ETH signed 1h slope in bottom decile.
  - Trade BTC short.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - H19 frozen regime state
  - ETH signed 1h slope decile rank
  - BTC-USD 5m closes
  parameters:
    horizon_bars: 6
    fixed:
      regime_gate: short_tail
      regime_threshold: 0.1
      signed_slope_decile: 1
      trade_direction: short_only
  allowed_variants: []
  status: frozen
- id: H37
  family: cross_asset_regime
  description: ETH slope alignment in H19 neutral regime.
  rationale: Test whether ETH 1h trend-direction signal is stronger in neutral spread
    regime than short-tail.
  entry_rules:
  - 'Trigger when H19 neutral regime is active: 0.10 <= spread_pct < 0.90.'
  - Compute ETH 1h EMA20 slope sign and trade BTC in that same sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign
  - H19 spread_pct regime state
  parameters:
    horizon_bars: 6
    fixed:
      regime_low_inclusive: 0.1
      regime_high_exclusive: 0.9
  allowed_variants: []
  status: planned
- id: H38
  family: cross_asset_regime
  description: ETH slope-only direction in H19 long-tail regime.
  rationale: Test ETH trend-direction transfer when spread is in long-tail extremes.
  entry_rules:
  - 'Trigger when H19 long-tail regime is active: spread_pct >= 0.90.'
  - Compute ETH 1h EMA20 slope sign and trade BTC in that same sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign
  - H19 spread_pct regime state
  parameters:
    horizon_bars: 6
    fixed:
      regime_threshold_long_tail: 0.9
  allowed_variants: []
  status: planned
- id: H39
  family: cross_asset_regime
  description: 'Frozen H39 baseline: ETH 1h EMA20 slope sign flip drives BTC direction
    (no regime gating).'
  rationale: Frozen lock after batch validation; preserve exact no-gating inflection
    logic.
  entry_rules:
  - Trigger when ETH 1h EMA20 slope sign flips versus prior bar.
  - Trade BTC in the new ETH slope-sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    horizon_bars: 6
    fixed:
      regime_gating: none
      tuning: forbidden
  allowed_variants: []
  status: frozen
- id: H40
  family: cross_asset_regime
  description: BTC slope confirmed by ETH slope.
  rationale: Test continuation when both assets share the same 1h slope sign.
  entry_rules:
  - Trigger when BTC 1h EMA20 slope sign equals ETH 1h EMA20 slope sign and is non-zero.
  - Trade BTC in that shared sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - BTC 1h EMA20 slope sign
  - ETH 1h EMA20 slope sign
  parameters:
    horizon_bars: 6
    fixed: {}
  allowed_variants: []
  status: planned
- id: H41
  family: cross_asset_regime
  description: BTC/ETH 1h slope divergence reversal.
  rationale: Test mean-reversion in BTC when BTC and ETH slope signs diverge.
  entry_rules:
  - Trigger when BTC 1h EMA20 slope sign does not equal ETH 1h EMA20 slope sign.
  - Trade BTC opposite its own BTC slope sign.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - BTC 1h EMA20 slope sign
  - ETH 1h EMA20 slope sign
  parameters:
    horizon_bars: 6
    fixed: {}
  allowed_variants: []
  status: planned
- id: H42
  family: shock_structure
  description: BTC top-decile shock continuation.
  rationale: Test whether extreme BTC one-bar moves continue over short horizon.
  entry_rules:
  - Compute rolling 20-day percentile of BTC 1-bar returns.
  - Trigger when BTC 1-bar return is >= 90th percentile.
  - Trade in the BTC shock-bar direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1-bar returns
  - Rolling 20-day return percentile state
  parameters:
    horizon_bars: 6
    fixed:
      lookback_days_percentile: 20
      top_decile_threshold: 0.9
  allowed_variants: []
  status: planned
- id: H43
  family: shock_structure
  description: BTC top-decile shock reversal.
  rationale: Test mean-reversion after extreme BTC one-bar moves.
  entry_rules:
  - Compute rolling 20-day percentile of BTC 1-bar returns.
  - Trigger when BTC 1-bar return is >= 90th percentile.
  - Trade opposite the BTC shock-bar direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1-bar returns
  - Rolling 20-day return percentile state
  parameters:
    horizon_bars: 6
    fixed:
      lookback_days_percentile: 20
      top_decile_threshold: 0.9
  allowed_variants: []
  status: planned
- id: H44
  family: shock_structure
  description: ETH shock leads BTC direction.
  rationale: Test cross-asset lead-lag where ETH extreme shocks transfer direction
    to BTC.
  entry_rules:
  - Compute rolling 20-day percentile of ETH 1-bar returns.
  - Trigger when ETH 1-bar return is >= 90th percentile.
  - Trade BTC in ETH shock-bar direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - ETH 1-bar returns
  - Rolling 20-day ETH return percentile state
  parameters:
    horizon_bars: 6
    fixed:
      lookback_days_percentile: 20
      top_decile_threshold: 0.9
  allowed_variants: []
  status: planned
- id: H45
  family: shock_structure
  description: ETH shock divergence versus BTC.
  rationale: Test ETH-led impulse when ETH is in top-decile shock and BTC is not.
  entry_rules:
  - Compute rolling 20-day percentiles of ETH and BTC 1-bar returns.
  - Trigger when ETH 1-bar return is >= 90th percentile and BTC 1-bar return is <
    90th percentile.
  - Trade BTC in ETH shock-bar direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - BTC and ETH 1-bar returns
  - Rolling 20-day return percentile states
  parameters:
    horizon_bars: 6
    fixed:
      lookback_days_percentile: 20
      top_decile_threshold: 0.9
  allowed_variants: []
  status: planned
- id: H46
  family: shock_structure
  description: Dual-shock alignment continuation.
  rationale: Test continuation when BTC and ETH both register same-direction top-decile
    shocks.
  entry_rules:
  - Compute rolling 20-day percentiles of BTC and ETH 1-bar returns.
  - Trigger when both assets are >= 90th percentile in the same sign direction.
  - Trade BTC in that shared shock direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - BTC and ETH 1-bar returns
  - Rolling 20-day return percentile states
  parameters:
    horizon_bars: 6
    fixed:
      lookback_days_percentile: 20
      top_decile_threshold: 0.9
  allowed_variants: []
  status: planned
- id: H47
  family: volatility_conditioning
  description: High-volatility continuation.
  rationale: Test short-horizon continuation during top-decile BTC true-range conditions.
  entry_rules:
  - Compute rolling 20-day percentile of BTC true range.
  - Trigger when BTC true range is >= 90th percentile.
  - Trade in current BTC bar direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLC
  - BTC true range series
  - Rolling 20-day true-range percentile state
  parameters:
    horizon_bars: 6
    fixed:
      lookback_days_percentile: 20
      high_vol_threshold: 0.9
  allowed_variants: []
  status: planned
- id: H48
  family: volatility_conditioning
  description: Low-volatility breakout follow-through.
  rationale: Test whether direction chosen by next bar after low-volatility setup
    carries forward.
  entry_rules:
  - Compute rolling 20-day percentile of BTC true range.
  - Trigger when BTC true range is <= 10th percentile.
  - Trade in the direction of the next bar.
  exit_rules:
  - Hold for fixed horizon bars from entry.
  required_inputs:
  - BTC-USD 5m OHLC
  - BTC true range series
  - Rolling 20-day true-range percentile state
  parameters:
    horizon_bars: 6
    fixed:
      lookback_days_percentile: 20
      low_vol_threshold: 0.1
  allowed_variants: []
  status: planned
- id: H49
  family: volatility_conditioning
  description: Volatility expansion from compression.
  rationale: Test expansion-bar direction persistence after compressed range cluster.
  entry_rules:
  - Compute rolling 20-day percentile of BTC true range.
  - Trigger when prior 5 bars are all <= 25th percentile, then first expansion bar
    occurs.
  - Trade in direction of the first expansion bar.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLC
  - BTC true range series
  - Rolling 20-day true-range percentile state
  parameters:
    horizon_bars: 6
    fixed:
      lookback_days_percentile: 20
      compression_threshold: 0.25
      compression_bars: 5
  allowed_variants: []
  status: planned
- id: H50
  family: volatility_conditioning
  description: ETH high-volatility alignment on BTC.
  rationale: Test whether ETH volatility regime informs BTC directional continuation.
  entry_rules:
  - Compute rolling 20-day percentile of ETH true range.
  - Trigger when ETH true range is >= 90th percentile.
  - Trade BTC in ETH bar direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLC
  - ETH-USD 5m OHLC
  - ETH true range series
  - Rolling 20-day ETH true-range percentile state
  parameters:
    horizon_bars: 6
    fixed:
      lookback_days_percentile: 20
      high_vol_threshold: 0.9
  allowed_variants: []
  status: planned
- id: H51
  family: mean_reversion
  description: VWAP z <= -2 in H19 neutral regime.
  rationale: Test fixed-threshold long mean-reversion only in neutral spread regime.
  entry_rules:
  - Trigger when BTC VWAP z-score <= -2.0 and H19 neutral regime is active (0.10 <=
    spread_pct < 0.90).
  - Enter long BTC.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - BTC VWAP z-score series
  - H19 spread_pct regime state
  parameters:
    horizon_bars: 6
    fixed:
      vwap_z_threshold: -2.0
      regime_low_inclusive: 0.1
      regime_high_exclusive: 0.9
  allowed_variants: []
  status: planned
- id: H52
  family: mean_reversion
  description: VWAP z >= +2 in H19 neutral regime.
  rationale: Test fixed-threshold short mean-reversion only in neutral spread regime.
  entry_rules:
  - Trigger when BTC VWAP z-score >= +2.0 and H19 neutral regime is active (0.10 <=
    spread_pct < 0.90).
  - Enter short BTC.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - BTC VWAP z-score series
  - H19 spread_pct regime state
  parameters:
    horizon_bars: 6
    fixed:
      vwap_z_threshold: 2.0
      regime_low_inclusive: 0.1
      regime_high_exclusive: 0.9
  allowed_variants: []
  status: planned
- id: H53
  family: mean_reversion
  description: Extreme VWAP z +/-3 across all regimes.
  rationale: Test stronger fixed-threshold mean-reversion without regime gating.
  entry_rules:
  - Trigger when absolute BTC VWAP z-score >= 3.0.
  - Trade opposite direction of z-score sign.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - BTC VWAP z-score series
  parameters:
    horizon_bars: 6
    fixed:
      vwap_abs_z_threshold: 3.0
  allowed_variants: []
  status: planned
- id: H54
  family: range_structure
  description: 12-bar breakout continuation.
  rationale: Test short-horizon continuation after local range break.
  entry_rules:
  - Trigger long when close breaks above prior 12-bar high.
  - Trigger short when close breaks below prior 12-bar low.
  - Trade in breakout direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLC
  - Rolling 12-bar high/low levels
  parameters:
    horizon_bars: 6
    fixed:
      breakout_lookback_bars: 12
  allowed_variants: []
  status: planned
- id: H55
  family: range_structure
  description: Failed breakout reversal.
  rationale: Test fade signal when breakout fails quickly and closes back inside prior
    range.
  entry_rules:
  - Trigger when price breaks prior 12-bar high/low then closes back inside that range
    within 1 bar.
  - Trade opposite the failed break direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLC
  - Rolling 12-bar high/low levels
  parameters:
    horizon_bars: 6
    fixed:
      breakout_lookback_bars: 12
      reentry_window_bars: 1
  allowed_variants: []
  status: planned
- id: H56
  family: range_structure
  description: Consecutive momentum burst continuation.
  rationale: Test continuation after short streak of directional bars.
  entry_rules:
  - Trigger when 3 consecutive BTC bars have the same direction.
  - Trade in direction of the streak.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLC
  - Bar direction sign series
  parameters:
    horizon_bars: 6
    fixed:
      consecutive_bars: 3
  allowed_variants: []
  status: planned
- id: H59
  family: cross_asset_regime
  description: Frozen H59 BTC-only slope flip control.
  rationale: Frozen replication control removing cross-asset transfer by trading BTC
    on BTC slope-sign flips.
  entry_rules:
  - Compute BTC 1h EMA20 slope sign.
  - Trigger when BTC slope sign flips versus prior 1h bar.
  - Trade BTC in the new BTC slope-sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1h EMA20 slope sign series
  parameters:
    horizon_bars: 6
    fixed:
      tuning: forbidden
      classification_lock: PASS gross / PASS bps8 / PASS bps10
  allowed_variants: []
  status: frozen
  signal_group: CA-4
- id: H60
  family: cross_asset_regime
  description: Frozen H60 ETH-self slope flip trade.
  rationale: Frozen replication control trading ETH on ETH slope-sign flips to isolate
    self-asset behavior.
  entry_rules:
  - Compute ETH 1h EMA20 slope sign.
  - Trigger when ETH slope sign flips versus prior 1h bar.
  - Trade ETH in the new ETH slope-sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    horizon_bars: 6
    fixed:
      tuning: forbidden
      classification_lock: PASS gross / PASS bps8 / PASS bps10
  allowed_variants: []
  status: frozen
  signal_group: CA-3
- id: H61
  family: cross_asset_regime
  description: Frozen H39 next-bar execution realism variant.
  rationale: Frozen replication pass explicitly enforcing next-bar close entry on
    frozen H39 signal logic.
  entry_rules:
  - 'Signal is H39: ETH 1h EMA20 slope sign flip versus prior 1h bar.'
  - Trade BTC in the new ETH slope-sign direction.
  - 'Execution: detect signal at t, enter at close[t+1], hold 6 bars from entry.'
  - Drop trade if t+1 does not exist.
  exit_rules:
  - Hold for fixed horizon bars from entry.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    horizon_bars: 6
    fixed:
      entry_mode: next_bar_close
      tuning: forbidden
      classification_lock: PASS gross / PASS bps8 / BORDERLINE bps10
  allowed_variants: []
  status: frozen
- id: H62
  family: cross_asset_regime
  description: 'H59 horizon sensitivity swap: horizon=4.'
  rationale: No-tuning horizon-only replication of H59 at shorter hold.
  entry_rules:
  - 'Same as H59: BTC 1h EMA20 slope sign flip versus prior 1h bar.'
  - Trade BTC in the new BTC slope-sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1h EMA20 slope sign series
  parameters:
    horizon_bars: 4
    fixed:
      base_hypothesis: H59
      variant_type: horizon_swap_only
      tuning: forbidden
  allowed_variants: []
  status: planned
- id: H63
  family: cross_asset_regime
  description: 'H59 horizon sensitivity swap: horizon=8.'
  rationale: No-tuning horizon-only replication of H59 at longer hold.
  entry_rules:
  - 'Same as H59: BTC 1h EMA20 slope sign flip versus prior 1h bar.'
  - Trade BTC in the new BTC slope-sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1h EMA20 slope sign series
  parameters:
    horizon_bars: 8
    fixed:
      base_hypothesis: H59
      variant_type: horizon_swap_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-2
- id: H64
  family: cross_asset_regime
  description: 'H60 horizon sensitivity swap: horizon=4.'
  rationale: No-tuning horizon-only replication of H60 at shorter hold.
  entry_rules:
  - 'Same as H60: ETH 1h EMA20 slope sign flip versus prior 1h bar.'
  - Trade ETH in the new ETH slope-sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    horizon_bars: 4
    fixed:
      base_hypothesis: H60
      variant_type: horizon_swap_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-3
- id: H65
  family: cross_asset_regime
  description: 'H60 horizon sensitivity swap: horizon=8.'
  rationale: No-tuning horizon-only replication of H60 at longer hold.
  entry_rules:
  - 'Same as H60: ETH 1h EMA20 slope sign flip versus prior 1h bar.'
  - Trade ETH in the new ETH slope-sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    horizon_bars: 8
    fixed:
      base_hypothesis: H60
      variant_type: horizon_swap_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-1
- id: H66
  family: cross_asset_regime
  description: H63 longer-lookback replication at 120 days.
  rationale: No-rule-change replication of H63 with shorter lookback window.
  entry_rules:
  - Same as H63 (same signal logic, same execution).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H63
      variant_type: lookback_replication_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-2
- id: H67
  family: cross_asset_regime
  description: H65 longer-lookback replication at 120 days.
  rationale: No-rule-change replication of H65 with shorter lookback window.
  entry_rules:
  - Same as H65 (same signal logic, same execution).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H65
      variant_type: lookback_replication_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-1
- id: H68
  family: cross_asset_regime
  description: H63 odd-day subsample replication.
  rationale: No-rule-change stability check on odd calendar day trades only.
  entry_rules:
  - Same as H63, keep trades where entry day-of-month is odd.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H63
      day_parity: odd
      variant_type: subsample_replication_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-2
- id: H69
  family: cross_asset_regime
  description: H63 even-day subsample replication.
  rationale: No-rule-change stability check on even calendar day trades only.
  entry_rules:
  - Same as H63, keep trades where entry day-of-month is even.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H63
      day_parity: even
      variant_type: subsample_replication_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-2
- id: H70
  family: cross_asset_regime
  description: H65 odd-day subsample replication.
  rationale: No-rule-change stability check on odd calendar day trades only.
  entry_rules:
  - Same as H65, keep trades where entry day-of-month is odd.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H65
      day_parity: odd
      variant_type: subsample_replication_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-1
- id: H71
  family: cross_asset_regime
  description: H65 even-day subsample replication.
  rationale: No-rule-change stability check on even calendar day trades only.
  entry_rules:
  - Same as H65, keep trades where entry day-of-month is even.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H65
      day_parity: even
      variant_type: subsample_replication_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-1
- id: H72
  family: cross_asset_regime
  description: H63 180-day replication control.
  rationale: Explicit rerun of H63 at current lookback for side-by-side robustness
    comparison.
  entry_rules:
  - Same as H63.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H63
      variant_type: lookback_replication_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-2
- id: H73
  family: cross_asset_regime
  description: H65 180-day replication control.
  rationale: Explicit rerun of H65 at current lookback for side-by-side robustness
    comparison.
  entry_rules:
  - Same as H65.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H65
      variant_type: lookback_replication_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-1
- id: H74
  family: cross_asset_regime
  description: H59 reference rerun at 120 days.
  rationale: Reference comparison anchor for slope-flip family at shorter lookback.
  entry_rules:
  - Same as H59.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 6
    fixed:
      base_hypothesis: H59
      variant_type: reference_replication_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-4
- id: H75
  family: cross_asset_regime
  description: H60 reference rerun at 120 days.
  rationale: Reference comparison anchor for slope-flip family at shorter lookback.
  entry_rules:
  - Same as H60.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 6
    fixed:
      base_hypothesis: H60
      variant_type: reference_replication_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-3
- id: H76
  family: cross_asset_regime
  description: H63 next-bar execution realism variant.
  rationale: 'Execution realism validation: same H63 signal, entry at next bar close.'
  entry_rules:
  - Same signal as H63.
  - Enter at close[t+1], hold 8 bars from entry.
  - Drop non-executable end-of-data trades.
  exit_rules:
  - Hold for fixed horizon bars from entry.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H63
      entry_mode: next_bar_close
      variant_type: execution_realism_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-2
- id: H77
  family: cross_asset_regime
  description: H65 next-bar execution realism variant.
  rationale: 'Execution realism validation: same H65 signal, entry at next bar close.'
  entry_rules:
  - Same signal as H65.
  - Enter at close[t+1], hold 8 bars from entry.
  - Drop non-executable end-of-data trades.
  exit_rules:
  - Hold for fixed horizon bars from entry.
  required_inputs:
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H65
      entry_mode: next_bar_close
      variant_type: execution_realism_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-1
- id: H78
  family: cross_asset_regime
  description: H63 extra-cost stress replication.
  rationale: Cost fragility validation with additional bps12 and bps15 stress outputs.
  entry_rules:
  - Same as H63.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H63
      stress_cost_modes:
      - bps12
      - bps15
      variant_type: cost_stress_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-2
- id: H79
  family: cross_asset_regime
  description: H65 extra-cost stress replication.
  rationale: Cost fragility validation with additional bps12 and bps15 stress outputs.
  entry_rules:
  - Same as H65.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H65
      stress_cost_modes:
      - bps12
      - bps15
      variant_type: cost_stress_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-1
- id: H80
  family: cross_asset_regime
  description: H63 UTC window replication 00:00-08:00.
  rationale: Time-of-day sanity check for H63.
  entry_rules:
  - Same as H63, keep trades in UTC 00:00-08:00.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1h EMA20 slope sign series
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      base_hypothesis: H63
      utc_window: 00:00-08:00
      variant_type: tod_window_only
      tuning: forbidden
  allowed_variants: []
  status: planned
- id: H81
  family: cross_asset_regime
  description: H63 UTC window replication 08:00-16:00.
  rationale: Time-of-day sanity check for H63.
  entry_rules:
  - Same as H63, keep trades in UTC 08:00-16:00.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H63
      utc_window: 08:00-16:00
      variant_type: tod_window_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-2 | ST
- id: H82
  family: cross_asset_regime
  description: H63 UTC window replication 16:00-24:00.
  rationale: Time-of-day sanity check for H63.
  entry_rules:
  - Same as H63, keep trades in UTC 16:00-24:00.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H63
      utc_window: 16:00-24:00
      variant_type: tod_window_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-2 | ST
- id: H83
  family: cross_asset_regime
  description: H65 UTC window replication 00:00-08:00.
  rationale: Time-of-day sanity check for H65.
  entry_rules:
  - Same as H65, keep trades in UTC 00:00-08:00.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H65
      utc_window: 00:00-08:00
      variant_type: tod_window_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-1 | ST
- id: H84
  family: cross_asset_regime
  description: H65 UTC window replication 08:00-16:00.
  rationale: Time-of-day sanity check for H65.
  entry_rules:
  - Same as H65, keep trades in UTC 08:00-16:00.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H65
      utc_window: 08:00-16:00
      variant_type: tod_window_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-1 | ST
- id: H85
  family: cross_asset_regime
  description: H65 UTC window replication 16:00-24:00.
  rationale: Time-of-day sanity check for H65.
  entry_rules:
  - Same as H65, keep trades in UTC 16:00-24:00.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_hypothesis: H65
      utc_window: 16:00-24:00
      variant_type: tod_window_only
      tuning: forbidden
  allowed_variants: []
  status: planned
  signal_group: CA-1 | ST
- id: H86
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Session-overlap momentum transfer with ETH trend confirmation.
  rationale: Probe whether BTC edge concentrates during high-liquidity UTC overlap
    windows rather than full-day sampling.
  WHY: Test session/time-window robustness by restricting execution to fixed overlap
    hours and requiring ETH trend state confirmation.
  RULES:
  - Only allow entries during UTC 12:00-20:00.
  - Require ETH 1h EMA20 slope sign to be non-zero and stable for the last 2 completed
    1h bars.
  - Trade BTC in ETH slope-sign direction.
  - No entries outside window; hold fixed horizon for active trades.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    utc_window: 12:00-20:00
    eth_slope_stability_bars_1h: 2
    trade_direction: signed
  FAILURE_CONDITIONS:
  - Edge disappears once execution is not restricted to overlap window.
  - Signal degrades when ETH slope sign churn increases intraday.
  - Trade count becomes too low for stable fold support.
  EXPECTED_FAILURE_MODE: 'Over-concentration risk: a few session clusters drive outcomes
    while out-of-sample windows contribute noise.'
  entry_rules:
  - Restrict entries to UTC 12:00-20:00 only.
  - Require ETH 1h EMA20 slope sign to be unchanged for the last 2 completed 1h bars
    and non-zero.
  - Trade BTC in ETH slope-sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  - UTC timestamp window filter
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      utc_window: 12:00-20:00
      eth_slope_stability_bars_1h: 2
      trade_direction: signed
  allowed_variants: []
  status: planned
- id: H87
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Regime-transition event entry using spread_pct state changes.
  rationale: Isolate whether transition moments carry more directional information
    than steady-state regime occupancy.
  WHY: Test regime transition behavior directly by trading only on H19 spread_pct
    state changes.
  RULES:
  - Detect transition events when spread_pct crosses regime boundaries (0.10 or 0.90).
  - Allow entry only on the first eligible 5m bar after transition.
  - Require ETH 1h EMA20 slope sign agreement with transition direction filter.
  - Trade BTC in ETH slope-sign direction.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 6
    regime_threshold_low: 0.1
    regime_threshold_high: 0.9
    transition_cooldown_bars: 12
  FAILURE_CONDITIONS:
  - Transition events are too sparse for required fold/sample support.
  - Boundary-crossing noise causes false transitions with no follow-through.
  - Edge vanishes when transition timing lags by one bar.
  EXPECTED_FAILURE_MODE: 'Event-timing fragility: performance depends on precise crossing
    bars and decays under minor timestamp drift.'
  entry_rules:
  - Compute H19 spread_pct regime state using thresholds 0.10 and 0.90.
  - Trigger only when state changes versus prior bar (short-tail/neutral/long-tail
    transition).
  - Apply 12-bar cooldown after each accepted transition entry.
  - Require ETH 1h EMA20 slope sign non-zero and trade BTC in that sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - H19 spread_pct regime state
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 6
    fixed:
      regime_threshold_low: 0.1
      regime_threshold_high: 0.9
      transition_only: true
      transition_cooldown_bars: 12
  allowed_variants: []
  status: planned
- id: H88
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Mid-volatility execution window conditioned on realized-vol percentile.
  rationale: Evaluate whether moderate volatility regimes provide better execution
    quality than very low or shock-high volatility states.
  WHY: Test volatility-conditioned execution windows using existing derived vol percentile
    features.
  RULES:
  - Compute BTC realized-vol percentile on rolling 20-day window.
  - Allow entries only when vol percentile is in [0.40, 0.80).
  - Require ETH 1h EMA20 slope sign non-zero; trade BTC in that sign direction.
  - Suppress entries when vol percentile is outside allowed band.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    vol_percentile_min: 0.4
    vol_percentile_max: 0.8
    vol_lookback_days: 20
  FAILURE_CONDITIONS:
  - Edge exists only in extreme-vol tails, not in mid band.
  - Vol percentile estimate is unstable around threshold boundaries.
  - Trade density drops below minimum confidence levels.
  EXPECTED_FAILURE_MODE: 'Band-selection misspecification: filtered sample is cleaner
    but too small, reducing robustness and fold consistency.'
  entry_rules:
  - Compute BTC realized-volatility percentile over rolling 20-day window.
  - Require 0.40 <= vol_percentile < 0.80.
  - Require ETH 1h EMA20 slope sign non-zero and trade BTC in that sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC realized-volatility percentile feature
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      vol_percentile_min: 0.4
      vol_percentile_max: 0.8
      vol_lookback_days: 20
      trade_direction: signed
  allowed_variants: []
  status: planned
- id: H89
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Signed-slope magnitude symmetry gate with mirrored long/short triggers.
  rationale: Constrain asymmetry by enforcing identical absolute-threshold logic for
    both directions.
  WHY: Test symmetry/asymmetry robustness by forcing mirrored long and short entry
    thresholds on ETH signed slope.
  RULES:
  - Compute ETH 1h EMA20 signed slope z-score (or standardized slope feature).
  - Enter BTC long when signed_slope_z >= +1.0.
  - Enter BTC short when signed_slope_z <= -1.0.
  - No trade in center band (-1.0, +1.0).
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 6
    signed_slope_z_long_min: 1.0
    signed_slope_z_short_max: -1.0
    center_no_trade_band: true
  FAILURE_CONDITIONS:
  - Directional drift makes mirrored thresholds suboptimal.
  - One side dominates while the opposite side contributes persistent losses.
  - Standardization instability causes threshold jitter.
  EXPECTED_FAILURE_MODE: 'Symmetry penalty: balanced rule sacrifices direction-specific
    edge and converges toward near-zero net expectancy.'
  entry_rules:
  - Standardize ETH 1h EMA20 signed slope into z-score feature.
  - Trigger BTC long when signed_slope_z >= +1.0.
  - Trigger BTC short when signed_slope_z <= -1.0.
  - Do not trade when -1.0 < signed_slope_z < +1.0.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - ETH 1h EMA20 signed slope standardized feature
  parameters:
    lookback_days: 180
    horizon_bars: 6
    fixed:
      signed_slope_z_long_min: 1.0
      signed_slope_z_short_max: -1.0
      enforce_directional_symmetry: true
  allowed_variants: []
  status: planned
- id: H90
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: High-conviction sparse-entry composite with explicit density cap.
  rationale: Measure trade-density versus edge tradeoff by combining stricter agreement
    filters into lower-frequency signals.
  WHY: Test whether lower trade density from multi-filter consensus improves robustness
    after costs.
  RULES:
  - Require BTC and ETH 1h EMA20 slope signs to agree and be non-zero.
  - Require ETH absolute 1h slope percentile >= 0.70.
  - Require BTC realized-vol percentile in [0.30, 0.70).
  - Enforce minimum spacing of 18 bars between new entries.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 10
    abs_eth_slope_percentile_min: 0.7
    vol_percentile_min: 0.3
    vol_percentile_max: 0.7
    min_entry_spacing_bars: 18
  FAILURE_CONDITIONS:
  - Trade count is too low to satisfy confidence gates.
  - Composite filter overfits to rare historical clusters.
  - Longer hold horizon increases variance and weakens edge retention.
  EXPECTED_FAILURE_MODE: 'Undertrading risk: higher per-trade expectancy cannot offset
    sparse opportunities and fold-level instability.'
  entry_rules:
  - Require BTC 1h EMA20 slope sign == ETH 1h EMA20 slope sign and both non-zero.
  - Require ETH absolute 1h EMA20 slope percentile >= 0.70.
  - 'Require BTC realized-volatility percentile: 0.30 <= vol_percentile < 0.70.'
  - Require at least 18 bars since prior entry.
  - Trade BTC in shared slope-sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - BTC 1h EMA20 slope sign
  - ETH 1h EMA20 slope sign and absolute percentile rank
  - BTC realized-volatility percentile feature
  parameters:
    lookback_days: 180
    horizon_bars: 10
    fixed:
      abs_eth_slope_percentile_min: 0.7
      btc_vol_percentile_min: 0.3
      btc_vol_percentile_max: 0.7
      min_entry_spacing_bars: 18
      consensus_gate: btc_eth_slope_sign_agreement
  allowed_variants: []
  status: planned
- id: H91
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Regime-transition impulse with transition-specific early exit.
  rationale: Isolate whether information lives in spread regime boundary crossings
    and whether shorter post-transition hold captures most signal.
  WHY: Test regime-transition entries/exits directly instead of steady-state occupancy.
  RULES:
  - Compute spread_pct regime state with short-tail <0.10, neutral [0.10,0.90), long-tail
    >=0.90.
  - Entry triggers only on regime-state transition bars.
  - Trade BTC in ETH 1h EMA20 slope sign direction, only if sign is non-zero.
  - Exit early at 4 bars if transition type is tail->neutral; otherwise hold full
    horizon.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    transition_exit_horizon_bars: 4
    regime_threshold_low: 0.1
    regime_threshold_high: 0.9
    transition_cooldown_bars: 10
  FAILURE_CONDITIONS:
  - Transition bars are too sparse for stable fold support.
  - Boundary transitions are noisy and mean-reverting before horizon completion.
  - Early-exit branch removes too much upside after true transitions.
  EXPECTED_FAILURE_MODE: 'Event-fragility: outcomes depend on a small number of boundary
    events and degrade with minor timestamp/state jitter.'
  entry_rules:
  - Detect spread_pct state transitions across 0.10 and 0.90 boundaries.
  - Enter only on first eligible 5m bar at transition with ETH 1h slope sign non-zero.
  - Trade BTC in ETH slope-sign direction.
  - Enforce 10-bar cooldown between transition entries.
  exit_rules:
  - If transition is tail->neutral, exit after 4 bars.
  - Otherwise, hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - H19 spread_pct regime state
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      regime_threshold_low: 0.1
      regime_threshold_high: 0.9
      transition_only: true
      transition_cooldown_bars: 10
      transition_exit_horizon_bars: 4
  allowed_variants: []
  status: planned
- id: H92
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Directional asymmetry test with long-side stricter confirmation than
    short-side.
  rationale: Measure whether edge is structurally asymmetric by applying intentionally
    different confirmation depth for long vs short entries.
  WHY: Test long/short asymmetry constraints using fixed, predeclared directional
    confirmation rules.
  RULES:
  - Long entries require ETH 1h slope sign positive for 3 consecutive completed 1h
    bars.
  - Short entries require ETH 1h slope sign negative for 1 completed 1h bar.
  - Trade BTC in ETH sign direction once confirmation is met.
  - Use symmetric fixed-horizon exits for both sides.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 6
    long_confirm_bars_1h: 3
    short_confirm_bars_1h: 1
    min_abs_slope_percentile: 0.5
  FAILURE_CONDITIONS:
  - Asymmetric gating overfilters one side and creates severe class imbalance.
  - Directional market regime changes invert which side should be strict.
  - Confirmation lag causes late entries and reduced move capture.
  EXPECTED_FAILURE_MODE: 'Asymmetry overfit: one branch dominates PnL while the other
    degrades robustness and fold consistency.'
  entry_rules:
  - Compute ETH 1h EMA20 slope sign and absolute slope percentile.
  - Require abs slope percentile >= 0.50.
  - 'For long: require sign=positive for last 3 completed 1h bars.'
  - 'For short: require sign=negative for last 1 completed 1h bar.'
  - Trade BTC in confirmed sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  - ETH 1h EMA20 absolute slope percentile
  parameters:
    lookback_days: 180
    horizon_bars: 6
    fixed:
      long_confirm_bars_1h: 3
      short_confirm_bars_1h: 1
      min_abs_slope_percentile: 0.5
      asymmetry_mode: directional_confirmation
  allowed_variants: []
  status: planned
- id: H93
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Volatility-conditioned participation with dual-band entry and shock
    blackout.
  rationale: Test whether excluding very low-vol and shock-vol bars improves execution
    robustness while retaining sufficient participation.
  WHY: Test volatility-conditioned participation using only existing realized-vol
    percentile and shock-derived features.
  RULES:
  - Allow entries only when BTC realized-vol percentile is in [0.25,0.75).
  - Blackout entries for 6 bars after ETH absolute return exceeds 95th percentile
    shock threshold.
  - Within eligible bars, trade BTC in ETH 1h slope sign direction (non-zero only).
  - Fixed horizon exits.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    btc_vol_percentile_min: 0.25
    btc_vol_percentile_max: 0.75
    eth_shock_percentile_threshold: 0.95
    post_shock_blackout_bars: 6
  FAILURE_CONDITIONS:
  - Blackout plus band filters collapse trade count below confidence thresholds.
  - Shock blackout removes highest-information continuation bars.
  - Vol percentile drift causes unstable participation rates across folds.
  EXPECTED_FAILURE_MODE: 'Over-filtering: cleaner but too sparse sample increases
    variance and reduces out-of-sample stability.'
  entry_rules:
  - 'Require BTC realized-vol percentile: 0.25 <= vol < 0.75.'
  - If ETH abs 1-bar return percentile >= 0.95, block new entries for next 6 bars.
  - Require ETH 1h EMA20 slope sign non-zero and trade BTC in that sign.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC realized-volatility percentile feature
  - ETH-USD 5m closes
  - ETH 1-bar absolute return percentile
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      btc_vol_percentile_min: 0.25
      btc_vol_percentile_max: 0.75
      eth_shock_percentile_threshold: 0.95
      post_shock_blackout_bars: 6
  allowed_variants: []
  status: planned
- id: H94
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Session-conditioned split with explicit weekend exclusion.
  rationale: Separate intraday session behavior from low-liquidity weekend structure
    using fixed UTC windows and calendar filter.
  WHY: Test time-window/session conditioning with a strict weekday-only participation
    rule.
  RULES:
  - Permit entries only Monday-Friday UTC.
  - Permit entries only during UTC 07:00-11:00 and 13:00-17:00 windows.
  - Require BTC/ETH 1h slope signs to agree and be non-zero.
  - Trade BTC in shared sign direction.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 6
    allowed_utc_windows:
    - 07:00-11:00
    - 13:00-17:00
    weekdays_only: true
    slope_sign_agreement_required: true
  FAILURE_CONDITIONS:
  - Weekday/session restriction cuts sample size too aggressively.
  - Signal behavior differs materially between the two windows, reducing pooled stability.
  - Agreement gate lags turning points and misses effective entries.
  EXPECTED_FAILURE_MODE: 'Calendar-window concentration: hypothesis works only in
    narrow subperiods and fails to generalize across folds.'
  entry_rules:
  - Filter calendar to Monday-Friday UTC.
  - Filter timestamp to UTC windows 07:00-11:00 and 13:00-17:00.
  - Require BTC 1h slope sign == ETH 1h slope sign and both non-zero.
  - Trade BTC in shared sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - UTC timestamp/day-of-week
  - BTC 1h EMA20 slope sign
  - ETH 1h EMA20 slope sign
  parameters:
    lookback_days: 180
    horizon_bars: 6
    fixed:
      weekdays_only: true
      allowed_utc_windows:
      - 07:00-11:00
      - 13:00-17:00
      slope_sign_agreement_required: true
  allowed_variants: []
  status: planned
- id: H95
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Trade-frequency governor with tiered conviction thresholds.
  rationale: Measure edge-quality tradeoff by explicitly controlling entry frequency
    via dynamic threshold tiers and spacing.
  WHY: Test trade-frequency control versus edge quality without using non-wired execution/microstructure
    fields.
  RULES:
  - Compute ETH signed slope standardized feature.
  - 'Use two conviction tiers: high (|z|>=1.25) and medium (|z|>=0.90).'
  - Allow medium-tier entries only if no trade occurred in last 24 bars; high-tier
    can bypass cooldown.
  - Direction follows ETH signed slope sign.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 10
    high_tier_abs_z_min: 1.25
    medium_tier_abs_z_min: 0.9
    medium_tier_cooldown_bars: 24
    global_min_spacing_bars: 6
  FAILURE_CONDITIONS:
  - Tier logic overcomplicates participation and introduces unstable regime dependence.
  - High-tier events are too rare to anchor performance.
  - Cooldown suppresses valid medium-tier continuation trades.
  EXPECTED_FAILURE_MODE: 'Control-friction tradeoff: reduced turnover improves per-trade
    quality but total opportunity set becomes too thin for robust edge.'
  entry_rules:
  - Standardize ETH 1h EMA20 signed slope into z-score.
  - If |z| >= 1.25, entry is allowed (subject to global spacing).
  - If 0.90 <= |z| < 1.25, require no entry in prior 24 bars.
  - Always enforce minimum 6-bar spacing between entries.
  - Trade BTC in ETH slope-sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - ETH 1h EMA20 signed slope standardized feature
  parameters:
    lookback_days: 365
    horizon_bars: 10
    fixed:
      high_tier_abs_z_min: 1.25
      medium_tier_abs_z_min: 0.9
      medium_tier_cooldown_bars: 24
      global_min_spacing_bars: 6
      tiered_frequency_governor: true
  allowed_variants: []
  status: planned
- id: H96
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Regime-boundary fade with transition-aware entry direction.
  rationale: Test whether spread regime crossings are overreactions that mean-revert
    versus continuing in ETH trend direction.
  WHY: Probe regime-transition behavior with explicit boundary-event logic and transition-type-dependent
    direction.
  RULES:
  - Detect spread_pct transitions across 0.10 and 0.90 boundaries.
  - For neutral->tail transitions, trade BTC opposite ETH slope sign (fade).
  - For tail->neutral transitions, trade BTC with ETH slope sign (normalize continuation).
  - Require ETH 1h slope sign non-zero; apply cooldown after each event.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 6
    regime_threshold_low: 0.1
    regime_threshold_high: 0.9
    transition_cooldown_bars: 14
    direction_map_mode: transition_type_dependent
  FAILURE_CONDITIONS:
  - Transition types are too sparse to sustain fold stability.
  - Fade mapping is wrong-signed during persistent trend phases.
  - Boundary noise creates false transitions and degraded signal quality.
  EXPECTED_FAILURE_MODE: 'Transition misclassification: small state jitter flips direction
    map and produces unstable out-of-sample behavior.'
  entry_rules:
  - Compute short-tail/neutral/long-tail states from spread_pct thresholds 0.10 and
    0.90.
  - Trigger only on state transition bars.
  - If transition enters a tail state, trade opposite ETH slope sign.
  - If transition exits a tail state to neutral, trade with ETH slope sign.
  - Enforce 14-bar cooldown between accepted entries.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - H19 spread_pct regime state
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 6
    fixed:
      regime_threshold_low: 0.1
      regime_threshold_high: 0.9
      transition_cooldown_bars: 14
      direction_map_mode: transition_type_dependent
  allowed_variants: []
  status: planned
- id: H97
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Regime-asymmetric long/short policy with side-specific participation.
  rationale: Test structural asymmetry by allowing longs in neutral/long-tail states
    while restricting shorts to short-tail stress states.
  WHY: Evaluate long/short asymmetry under explicit regime-conditioned side gating.
  RULES:
  - Long entries allowed only when spread_pct >= 0.10 and ETH slope sign is positive.
  - Short entries allowed only when spread_pct < 0.10 and ETH slope sign is negative.
  - Require ETH absolute slope percentile >= 0.60 for both sides.
  - Use symmetric fixed-horizon exit.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    long_regime_min: 0.1
    short_regime_max: 0.1
    min_abs_slope_percentile: 0.6
    min_spacing_bars: 8
  FAILURE_CONDITIONS:
  - One side is under-sampled and destabilizes aggregate behavior.
  - Regime map drifts and invalidates side assignment policy.
  - Asymmetric gating misses major moves outside designated regimes.
  EXPECTED_FAILURE_MODE: 'Policy imbalance: side-specific filters create persistent
    directional bias that fails across varying market regimes.'
  entry_rules:
  - Compute spread_pct regime state each bar.
  - Long when spread_pct >= 0.10 and ETH slope sign > 0.
  - Short when spread_pct < 0.10 and ETH slope sign < 0.
  - Require ETH absolute slope percentile >= 0.60.
  - Require at least 8 bars since prior entry.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - H19 spread_pct regime state
  - ETH 1h EMA20 slope sign and absolute slope percentile
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      long_regime_min: 0.1
      short_regime_max: 0.1
      min_abs_slope_percentile: 0.6
      min_spacing_bars: 8
      asymmetry_policy: regime_conditioned_sides
  allowed_variants: []
  status: planned
- id: H98
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Volatility-state switch between continuation and no-trade zones.
  rationale: Test whether mid/high vol supports directional transfer while low/shock
    extremes are better excluded.
  WHY: Evaluate volatility-conditioned participation with explicit state machine behavior
    instead of a single static band.
  RULES:
  - 'Define low vol: percentile < 0.20, mid vol: [0.20,0.70), high vol: [0.70,0.90),
    shock: >=0.90.'
  - Allow continuation entries only in mid/high states using ETH slope sign direction.
  - Block all entries in low and shock states.
  - Apply 4-bar re-enable delay after leaving shock state.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    vol_low_max: 0.2
    vol_mid_max: 0.7
    vol_high_max: 0.9
    shock_reenable_delay_bars: 4
  FAILURE_CONDITIONS:
  - State segmentation overcomplicates behavior and increases boundary churn.
  - High-vol continuation assumption fails in reversal-dominant periods.
  - Participation collapses due to prolonged shock/low-vol occupancy.
  EXPECTED_FAILURE_MODE: 'State-machine brittleness: minor percentile shifts flip
    eligibility and create unstable fold-level participation.'
  entry_rules:
  - Compute BTC realized-vol percentile state (low/mid/high/shock).
  - Permit entries only in mid/high states.
  - Require ETH 1h slope sign non-zero; trade BTC in that sign direction.
  - After shock->non-shock transition, wait 4 bars before allowing entries.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - BTC realized-volatility percentile feature
  - ETH-USD 5m closes
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      vol_low_max: 0.2
      vol_mid_max: 0.7
      vol_high_max: 0.9
      shock_reenable_delay_bars: 4
      participation_mode: vol_state_machine
  allowed_variants: []
  status: planned
- id: H99
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Session handoff hypothesis across UTC market-open transitions.
  rationale: Test whether edge concentrates at session handoffs rather than broad
    daytime windows.
  WHY: Evaluate time-window conditioning on fixed session-transition intervals with
    strict temporal gating.
  RULES:
  - 'Allow entries only in UTC handoff windows: 06:00-08:00 and 12:00-14:00.'
  - Require ETH slope sign change within prior 3 5m bars.
  - Require BTC/ETH slope sign agreement at entry bar.
  - Trade BTC in shared sign direction.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 6
    handoff_windows:
    - 06:00-08:00
    - 12:00-14:00
    recent_flip_lookback_bars: 3
    require_sign_agreement: true
  FAILURE_CONDITIONS:
  - Handoff windows are too narrow to sustain sample-size requirements.
  - Recent flip condition triggers mostly noise around open transitions.
  - Combined window+agreement filter overconstrains participation.
  EXPECTED_FAILURE_MODE: 'Temporal concentration risk: hypothesis depends on specific
    handoff microregimes and fails outside those pockets.'
  entry_rules:
  - Filter time to UTC windows 06:00-08:00 and 12:00-14:00.
  - Require ETH 1h slope sign to have flipped at least once in prior 3 bars.
  - Require BTC 1h slope sign == ETH 1h slope sign and non-zero.
  - Trade BTC in shared sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - UTC timestamp window filter
  - BTC 1h EMA20 slope sign
  - ETH 1h EMA20 slope sign series
  parameters:
    lookback_days: 365
    horizon_bars: 6
    fixed:
      handoff_windows:
      - 06:00-08:00
      - 12:00-14:00
      recent_flip_lookback_bars: 3
      require_sign_agreement: true
  allowed_variants: []
  status: planned
  signal_group: CA-5
- id: H100
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Quality-over-quantity quota model with daily entry budget.
  rationale: Test edge-quality tradeoff by hard-capping trade frequency while admitting
    only top-conviction slope signals.
  WHY: Measure whether explicit quota controls improve robustness compared with unconstrained
    signal throughput.
  RULES:
  - Rank candidate entries by ETH absolute slope percentile each UTC day.
  - Allow at most 2 entries per day, taking highest-ranked eligible signals first.
  - Require BTC/ETH slope sign agreement and non-zero sign.
  - Require BTC realized-vol percentile in [0.30,0.85) to avoid dull/shock tails.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 10
    daily_entry_quota: 2
    min_abs_slope_percentile: 0.75
    btc_vol_percentile_min: 0.3
    btc_vol_percentile_max: 0.85
  FAILURE_CONDITIONS:
  - Quota rejects too many valid opportunities in trend-persistent periods.
  - Ranking by absolute slope percentile overweights transient spikes.
  - Daily batching introduces path dependence and unstable fold behavior.
  EXPECTED_FAILURE_MODE: 'Quota path dependency: day-level selection order dominates
    outcomes and reduces consistency across walkforward folds.'
  entry_rules:
  - Generate candidates when BTC/ETH 1h slope signs agree and are non-zero.
  - Require ETH absolute slope percentile >= 0.75.
  - 'Require BTC realized-vol percentile: 0.30 <= vol < 0.85.'
  - Within each UTC day, keep top 2 candidates by ETH absolute slope percentile.
  - Trade BTC in shared slope-sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - BTC realized-volatility percentile feature
  - ETH 1h EMA20 slope sign and absolute slope percentile
  - UTC day partition for quota accounting
  parameters:
    lookback_days: 180
    horizon_bars: 10
    fixed:
      daily_entry_quota: 2
      min_abs_slope_percentile: 0.75
      btc_vol_percentile_min: 0.3
      btc_vol_percentile_max: 0.85
      selection_mode: daily_top_ranked
  allowed_variants: []
  status: planned
- id: P01
  family: portfolio_construction
  class: tradeable_hypothesis
  logic_hash: 844d7cfc1215e95793d72ae09562b4f30ae5ffd8561d1b22e6c688336276577e
  description: Deduplicated core equal-weight portfolio over paper-candidate signal
    set.
  rationale: Separate portfolio-layer dedup test from standalone signal hypotheses
    by removing one near-duplicate member and preserving fixed-weight aggregation.
  WHY: Test whether reducing internal redundancy (H77/H79 pair) improves portfolio
    coherence without introducing adaptive weighting.
  RULES:
  - 'Candidate base set is fixed: H76,H77,H79,H81,H78,H82.'
  - 'Dedup rule is fixed and non-adaptive: keep H77, exclude H79.'
  - Combine active strategy signals with equal weights across included members.
  - Apply existing portfolio risk caps and one-position-per-strategy constraints already
    used by paper framework.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    candidate_universe:
    - H76
    - H77
    - H79
    - H81
    - H78
    - H82
    dedup_keep: H77
    dedup_drop: H79
    weighting_mode: equal_weight_active
    max_active_strategies: 5
  FAILURE_CONDITIONS:
  - Dropped member contained unique conditional coverage despite apparent duplication.
  - Equal-weight aggregation overconcentrates during correlated bursts.
  - Reduced member count lowers diversification without improving coherence.
  EXPECTED_FAILURE_MODE: 'False dedup assumption: removed strategy contributes distinct
    edge in specific regimes, producing net degradation.'
  entry_rules:
  - 'Read member strategy directional intents from fixed included set: H76,H77,H81,H78,H82.'
  - Normalize each active member to unit notional vote and average equally.
  - Open/adjust portfolio exposure in averaged signal direction subject to existing
    caps.
  exit_rules:
  - Exit when combined active signal crosses zero or no member remains active.
  - Apply standard portfolio risk-stop behavior from existing execution framework.
  required_inputs:
  - Strategy signal states for H76,H77,H79,H81,H78,H82
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - Existing portfolio cap/exposure controls
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      candidate_universe:
      - H76
      - H77
      - H79
      - H81
      - H78
      - H82
      dedup_keep: H77
      dedup_drop: H79
      weighting_mode: equal_weight_active
      cap_policy: existing_portfolio_caps
  allowed_variants: []
  status: planned
- id: P02
  family: portfolio_construction
  class: tradeable_hypothesis
  logic_hash: 5535157cdade86e64f5b941703ae72115ee9d0484fd7379be9fe3031135d37c7
  description: Risk-adjusted rank-weight blend across fixed candidate universe.
  rationale: Test whether deterministic rank-weighting over recent quality metrics
    improves allocation discipline versus static equal weights.
  WHY: Evaluate a portfolio-construction layer that allocates by recent risk-adjusted
    quality while keeping strategy definitions frozen.
  RULES:
  - 'Candidate base set is fixed: H76,H77,H79,H81,H78,H82.'
  - 'Compute per-strategy score over trailing 30 calendar days: score_i = mean(5m
    strategy returns) / stdev(5m strategy returns).'
  - Clip negative scores to 0, then normalize by L1 sum to obtain active weights.
  - If all scores are <=0, fall back to equal-weight across currently active members.
  PARAMETERS:
    lookback_days: 180
    rank_window_days: 30
    score_formula: mean_over_std
    score_clip_floor: 0.0
    normalization: L1_on_positive_scores
    fallback_weighting: equal_weight_active
  FAILURE_CONDITIONS:
  - Ranking window too short leads to unstable weight turnover.
  - Risk-adjusted score overreacts to volatility compression artifacts.
  - Weight concentration breaches practical diversification intent.
  EXPECTED_FAILURE_MODE: 'Estimator noise: recent-window score instability causes
    excessive allocation churn and inconsistent portfolio behavior.'
  entry_rules:
  - Generate member intents from fixed universe strategies.
  - Rank members by trailing 30-day risk-adjusted score.
  - Allocate weights by positive-score normalization; apply to active intents only.
  - Apply fallback equal weighting when all scores are non-positive.
  exit_rules:
  - Exit or resize as member intents/weights update each rebalance step.
  - Flatten when no members are active.
  required_inputs:
  - Strategy signal states and return streams for H76,H77,H79,H81,H78,H82
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - Existing portfolio cap/exposure controls
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      candidate_universe:
      - H76
      - H77
      - H79
      - H81
      - H78
      - H82
      rank_window_days: 30
      score_formula: mean_over_std
      score_clip_floor: 0.0
      normalization: L1_on_positive_scores
      fallback_weighting: equal_weight_active
  allowed_variants: []
  status: planned
- id: P03
  family: portfolio_construction
  class: tradeable_hypothesis
  logic_hash: 820e748c47ca7e73c9d8e1e9080cdbdef63533c52f58397149da73c01e57f3aa
  description: Consensus-strength gated portfolio activation.
  rationale: Test whether requiring stronger cross-strategy directional agreement
    reduces noisy exposure periods.
  WHY: Evaluate a deterministic consensus gate as a portfolio-level filter above fixed
    standalone signals.
  RULES:
  - 'Candidate base set is fixed: H76,H77,H79,H81,H78,H82.'
  - 'Consensus metric: C = abs(sum(sign_i * w_i)) / sum(active_w_i), with sign_i in
    {-1,+1} and equal base weights.'
  - Open/maintain exposure only when C >= 0.67 and at least 3 members are active.
  - Direction is sign(sum(sign_i * w_i)); otherwise remain flat.
  PARAMETERS:
    lookback_days: 180
    consensus_threshold: 0.67
    min_active_members: 3
    base_weighting: equal_weight
    rebalance_interval_bars: 1
  FAILURE_CONDITIONS:
  - Gate suppresses too many trades and underutilizes valid weak-consensus edges.
  - Consensus spikes occur late after much of move already occurred.
  - Correlated false agreement produces concentrated wrong-way exposure.
  EXPECTED_FAILURE_MODE: 'Over-gating: participation collapses in mixed regimes, leaving
    sparse trades and unstable fold support.'
  entry_rules:
  - Compute member directional signs from fixed universe each bar.
  - Compute consensus metric C using equal base weights across active members.
  - Enter only when C >= 0.67 and active member count >= 3.
  - Trade in consensus direction.
  exit_rules:
  - Exit when C drops below 0.67 or active member count falls below 3.
  - Exit when consensus direction flips sign.
  required_inputs:
  - Strategy signal states for H76,H77,H79,H81,H78,H82
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - Existing portfolio cap/exposure controls
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      candidate_universe:
      - H76
      - H77
      - H79
      - H81
      - H78
      - H82
      consensus_metric: abs_signed_weighted_sum_over_active_weight
      consensus_threshold: 0.67
      min_active_members: 3
      base_weighting: equal_weight
  allowed_variants: []
  status: planned
- id: P04
  family: portfolio_construction
  class: tradeable_hypothesis
  logic_hash: f7906b66cfdb5bb1b44c141bb8d33a2157e4e92ffbee80501391bf6f09f66afc
  description: Session-aware blend using H81/H82 specialists plus core signals.
  rationale: Test explicit time-window specialization by routing session windows to
    specialist strategies while preserving a deterministic core fallback.
  WHY: Evaluate whether session-aware blending improves construction robustness without
    altering standalone signal definitions.
  RULES:
  - 'Core set is fixed: H76,H77,H79,H78.'
  - 'Session specialist mapping: UTC 08:00-16:00 -> H81 specialist; UTC 16:00-24:00
    -> H82 specialist.'
  - During mapped session, blend core equal-weight + session specialist with 60/40
    core/specialist split.
  - 'Fallback behavior: outside mapped windows or if specialist inactive, use core
    equal-weight only.'
  PARAMETERS:
    lookback_days: 180
    session_map:
      08:00-16:00: H81
      16:00-24:00: H82
    core_set:
    - H76
    - H77
    - H79
    - H78
    core_weight_share: 0.6
    specialist_weight_share: 0.4
    fallback_mode: core_only_equal_weight
  FAILURE_CONDITIONS:
  - Session specialists underperform outside their assumed windows with abrupt handoff
    effects.
  - Core/specialist blend introduces discontinuities at session boundaries.
  - Fallback triggers too frequently, reducing intended specialization benefit.
  EXPECTED_FAILURE_MODE: 'Boundary-friction: session handoffs and specialist inactivity
    create unstable exposure transitions.'
  entry_rules:
  - Compute core aggregate signal from H76,H77,H79,H78 with equal active weights.
  - If current UTC time in 08:00-16:00 and H81 active, combine core and H81 via 60/40
    weights.
  - If current UTC time in 16:00-24:00 and H82 active, combine core and H82 via 60/40
    weights.
  - Otherwise use fallback core-only aggregate.
  exit_rules:
  - Resize/flip when blended aggregate direction changes.
  - Flatten when neither core nor mapped specialist provides active signal.
  required_inputs:
  - Strategy signal states for H76,H77,H79,H81,H78,H82
  - UTC timestamp session mapping
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - Existing portfolio cap/exposure controls
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      candidate_universe:
      - H76
      - H77
      - H79
      - H81
      - H78
      - H82
      core_set:
      - H76
      - H77
      - H79
      - H78
      session_map:
        08:00-16:00: H81
        16:00-24:00: H82
      core_weight_share: 0.6
      specialist_weight_share: 0.4
      fallback_mode: core_only_equal_weight
  allowed_variants: []
  status: planned
- id: H101
  family: volatility_state
  class: tradeable_hypothesis
  logic_hash: 6e1ae8b27ab3447e5071ee5c42f3bac423378d3416122cf3539aae4d8eb95b24
  description: ATR expansion with realized-volatility containment continuation.
  rationale: Test directional continuation when local range expands but realized volatility
    remains below shock state.
  WHY: Use ATR14 and RV48 jointly to detect controlled expansion regimes distinct
    from slope-flip logic.
  RULES:
  - Require ATR14 percentile in [0.65, 0.90).
  - Require RV48 percentile in [0.40, 0.80).
  - Require BTC 5m close above vwap48 for long, below vwap48 for short.
  - Trade in the sign of close-vs-vwap48 displacement.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    atr14_pct_min: 0.65
    atr14_pct_max: 0.9
    rv48_pct_min: 0.4
    rv48_pct_max: 0.8
    direction_source: close_vs_vwap48_sign
  FAILURE_CONDITIONS:
  - ATR expansion signals late-stage exhaustion instead of continuation.
  - RV containment band excludes too many opportunities.
  - VWAP displacement direction is unstable in chop.
  EXPECTED_FAILURE_MODE: Volatility-state aliasing causes entries during transient
    range bursts with poor follow-through.
  entry_rules:
  - Compute rolling percentiles for atr14 and rv48.
  - Allow entries only when 0.65 <= atr14_pct < 0.90 and 0.40 <= rv48_pct < 0.80.
  - If close > vwap48 enter long; if close < vwap48 enter short.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - atr14 feature
  - rv48 feature
  - vwap48 feature
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      atr14_pct_min: 0.65
      atr14_pct_max: 0.9
      rv48_pct_min: 0.4
      rv48_pct_max: 0.8
      direction_source: close_vs_vwap48_sign
  allowed_variants: []
  status: planned
- id: H102
  family: volatility_state
  class: tradeable_hypothesis
  logic_hash: a89f73a0227b0b45606c70dbdf6c769daceb6c5d446fb85c01dba91377fe91b0
  description: Low-volatility compression breakout from vwap48 band.
  rationale: Test breakout behavior when ATR and RV jointly indicate compressed state.
  WHY: Orthogonal compression-breakout hypothesis based on atr14/rv48 thresholds and
    vwap48 band escape.
  RULES:
  - Require ATR14 percentile < 0.30 and RV48 percentile < 0.35.
  - Define vwap48 distance band using absolute z-normalized distance over 20-day window.
  - Trigger only when |dist_to_vwap48_z| crosses above 1.2 from below.
  - Trade in breakout direction sign(dist_to_vwap48).
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    atr14_pct_max: 0.3
    rv48_pct_max: 0.35
    vwap_distance_z_breakout: 1.2
    distance_norm_window_days: 20
  FAILURE_CONDITIONS:
  - Compression persists and breakouts fail quickly.
  - Distance normalization is unstable across volatility regimes.
  - Breakout threshold overfilters and reduces trade density.
  EXPECTED_FAILURE_MODE: False breakout clustering during low-liquidity ranges creates
    repeated whipsaw entries.
  entry_rules:
  - Require atr14_pct < 0.30 and rv48_pct < 0.35.
  - Compute standardized vwap48 distance z score.
  - Enter when abs(z) crosses from <=1.2 to >1.2, in z sign direction.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - atr14 feature
  - rv48 feature
  - vwap48 feature
  - vwap distance standardized series
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      atr14_pct_max: 0.3
      rv48_pct_max: 0.35
      vwap_distance_z_breakout: 1.2
      distance_norm_window_days: 20
  allowed_variants: []
  status: planned
- id: H103
  family: volatility_state
  class: tradeable_hypothesis
  logic_hash: e9ae2605460071d4552c3312730e7190bf5fd4e49b31e5d5994a6e35848657b2
  description: Shock-reversion after high RV48 with ATR deceleration.
  rationale: Test post-shock reversal when realized volatility remains elevated but
    range expansion decelerates.
  WHY: Use rv48 shock state with atr14 slope deceleration to capture exhaustion-style
    reversals.
  RULES:
  - Require RV48 percentile >= 0.90.
  - Require ATR14 first difference < 0 for last 3 bars.
  - Require close-vwap48 distance sign unchanged for last 2 bars.
  - Trade opposite the current close-vwap48 distance sign.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    rv48_pct_min: 0.9
    atr14_decline_bars: 3
    distance_sign_stability_bars: 2
    direction_mode: opposite_vwap_distance_sign
  FAILURE_CONDITIONS:
  - Shock regimes trend longer than expected.
  - ATR deceleration is too noisy as exhaustion proxy.
  - Opposite-sign entries catch continuation legs.
  EXPECTED_FAILURE_MODE: Persistent high-volatility trends overwhelm reversal premise
    and generate serial stop-like losses.
  entry_rules:
  - Require rv48_pct >= 0.90.
  - Require atr14[t]-atr14[t-1] < 0 for 3 consecutive bars.
  - Require sign(close-vwap48) stable for last 2 bars.
  - Enter opposite sign(close-vwap48).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - atr14 feature
  - rv48 feature
  - vwap48 feature
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      rv48_pct_min: 0.9
      atr14_decline_bars: 3
      distance_sign_stability_bars: 2
      direction_mode: opposite_vwap_distance_sign
  allowed_variants: []
  status: planned
- id: H104
  family: volatility_state
  class: tradeable_hypothesis
  logic_hash: 702b061c7f8a0da22a74512e3df502a6371e70d2dfdd5cf335b7eaff71de5155
  description: Balanced-volatility carry with ATR/RV ratio filter.
  rationale: Test whether a stable ATR14-to-RV48 ratio identifies more persistent
    short-horizon directional carry.
  WHY: Introduce volatility-ratio regime logic orthogonal to pure slope-flip entries.
  RULES:
  - Compute ratio r = atr14 / rv48 (scaled to comparable units).
  - Require ratio percentile in [0.45, 0.70).
  - Require absolute close-vwap48 distance percentile in [0.50, 0.85).
  - Trade in sign(close-vwap48).
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    atr_rv_ratio_pct_min: 0.45
    atr_rv_ratio_pct_max: 0.7
    abs_vwap_dist_pct_min: 0.5
    abs_vwap_dist_pct_max: 0.85
  FAILURE_CONDITIONS:
  - Ratio filter is not stable across changing volatility baselines.
  - Distance filter may capture overextended moves near reversal.
  - Scaled ratio construction may be implementation-sensitive.
  EXPECTED_FAILURE_MODE: Regime drift in ratio distribution causes degraded portability
    and inconsistent entry quality.
  entry_rules:
  - Compute scaled atr14/rv48 ratio percentile.
  - Require 0.45 <= ratio_pct < 0.70.
  - Require 0.50 <= abs_dist_vwap48_pct < 0.85.
  - Enter in sign(close-vwap48).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - atr14 feature
  - rv48 feature
  - vwap48 feature
  - scaled atr14/rv48 ratio feature
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      atr_rv_ratio_pct_min: 0.45
      atr_rv_ratio_pct_max: 0.7
      abs_vwap_dist_pct_min: 0.5
      abs_vwap_dist_pct_max: 0.85
  allowed_variants: []
  status: planned
- id: H105
  family: efficiency_mean_reversion
  class: tradeable_hypothesis
  logic_hash: 843ed3546de9b23b6835dcfe1f04842bac6846baeba71af23963add2209b9be7
  description: Low-efficiency VWAP pullback mean-reversion.
  rationale: Test reversion to vwap48 during low directional efficiency states.
  WHY: Use er20 and vwap48 distance directly for mean-reversion logic, not trend sign
    transfer.
  RULES:
  - Require er20 <= 0.25.
  - Require |dist_to_vwap48_z| >= 1.5.
  - Trade opposite sign(dist_to_vwap48).
  - Suppress entries if rv48 percentile >= 0.85.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    er20_max: 0.25
    abs_vwap_dist_z_min: 1.5
    rv48_pct_max_for_entry: 0.85
  FAILURE_CONDITIONS:
  - Low-er20 states can still exhibit directional drift.
  - VWAP distance extremes may signal breakouts, not reversion.
  - RV suppression may remove the most profitable reversion events.
  EXPECTED_FAILURE_MODE: Distance-based reversals become trend-traps when market transitions
    from low to high efficiency.
  entry_rules:
  - Require er20 <= 0.25.
  - Require abs(standardized close-vwap48 distance) >= 1.5.
  - Require rv48_pct < 0.85.
  - Enter opposite sign(close-vwap48).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - er20 feature
  - rv48 feature
  - vwap48 feature
  - vwap distance standardized series
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      er20_max: 0.25
      abs_vwap_dist_z_min: 1.5
      rv48_pct_max_for_entry: 0.85
      direction_mode: opposite_vwap_distance_sign
  allowed_variants: []
  status: planned
- id: H106
  family: efficiency_mean_reversion
  class: tradeable_hypothesis
  logic_hash: b01ef75d8a81ede0d69c932378494de6e6a76e8ecb8d3e333f3b061462541498
  description: High-efficiency VWAP continuation with distance cap.
  rationale: Test continuation when er20 indicates directional persistence but entry
    avoids extreme overextension.
  WHY: Complement H105 by testing efficiency-conditioned continuation using er20 +
    bounded vwap48 displacement.
  RULES:
  - Require er20 >= 0.60.
  - Require |dist_to_vwap48_z| in [0.6, 1.8).
  - Require atr14 percentile in [0.35, 0.85).
  - Trade in sign(dist_to_vwap48).
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    er20_min: 0.6
    abs_vwap_dist_z_min: 0.6
    abs_vwap_dist_z_max: 1.8
    atr14_pct_min: 0.35
    atr14_pct_max: 0.85
  FAILURE_CONDITIONS:
  - High efficiency may lag and occur near trend exhaustion.
  - Distance cap may still allow overextended entries.
  - ATR band may remove valid quiet-trend continuation.
  EXPECTED_FAILURE_MODE: Late continuation entries after efficiency spikes create
    poor reward-to-risk under fixed horizon.
  entry_rules:
  - Require er20 >= 0.60.
  - Require 0.6 <= abs(standardized close-vwap48 distance) < 1.8.
  - Require 0.35 <= atr14_pct < 0.85.
  - Enter in sign(close-vwap48).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - er20 feature
  - atr14 feature
  - vwap48 feature
  - vwap distance standardized series
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      er20_min: 0.6
      abs_vwap_dist_z_min: 0.6
      abs_vwap_dist_z_max: 1.8
      atr14_pct_min: 0.35
      atr14_pct_max: 0.85
      direction_mode: vwap_distance_sign
  allowed_variants: []
  status: planned
- id: H107
  family: efficiency_mean_reversion
  class: tradeable_hypothesis
  logic_hash: 0581f2833120d195bcef25f6b0a6214551d1313a15d87794b8c47516fb2597b8
  description: ER regime-switch trigger between reversion and continuation.
  rationale: Test deterministic state-machine switching around er20 midpoint with
    vwap distance confirmation.
  WHY: Introduce explicit ER-driven mode switching to reduce one-regime dependency.
  RULES:
  - If er20 < 0.40, use mean-reversion direction (opposite dist_to_vwap48 sign).
  - If er20 >= 0.40, use continuation direction (same dist_to_vwap48 sign).
  - Require abs(dist_to_vwap48_z) >= 0.9 in both modes.
  - Block entries when rv48 percentile >= 0.92.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    er20_switch_threshold: 0.4
    abs_vwap_dist_z_min: 0.9
    rv48_pct_max: 0.92
    mode_switch: er20_state_machine
  FAILURE_CONDITIONS:
  - Switch threshold causes excessive mode-flipping around boundary.
  - Single threshold may not generalize across volatility regimes.
  - Distance requirement may not differentiate high-noise states.
  EXPECTED_FAILURE_MODE: Boundary churn near er20 threshold generates contradictory
    entries and elevated turnover noise.
  entry_rules:
  - Require abs(standardized close-vwap48 distance) >= 0.9 and rv48_pct < 0.92.
  - If er20 < 0.40 enter opposite sign(close-vwap48).
  - If er20 >= 0.40 enter same sign(close-vwap48).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - er20 feature
  - rv48 feature
  - vwap48 feature
  - vwap distance standardized series
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      er20_switch_threshold: 0.4
      abs_vwap_dist_z_min: 0.9
      rv48_pct_max: 0.92
      mode_switch: er20_state_machine
  allowed_variants: []
  status: planned
- id: H108
  family: cross_asset_divergence
  class: tradeable_hypothesis
  logic_hash: ef4f1006b7876d1fb11990db54c7c33cfa7edfdf143877b3e8fc9cacbb4ef143
  description: BTC/ETH efficiency divergence convergence trade.
  rationale: Test whether large ER20 gaps between BTC and ETH revert via BTC adjustment.
  WHY: Use cross-asset disagreement on efficiency state rather than slope-sign transfer.
  RULES:
  - Compute delta_er = er20_btc - er20_eth.
  - Require |delta_er| percentile >= 0.85.
  - If delta_er > 0 enter BTC short; if delta_er < 0 enter BTC long.
  - Require BTC |dist_to_vwap48_z| >= 0.7 and rv48 percentile < 0.90.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    abs_delta_er_pct_min: 0.85
    btc_abs_vwap_dist_z_min: 0.7
    btc_rv48_pct_max: 0.9
    direction_mode: opposite_delta_er_sign
  FAILURE_CONDITIONS:
  - ER divergence can persist structurally rather than mean-revert.
  - BTC may not be the adjustment leg in divergence episodes.
  - Distance filter may not isolate true dislocations.
  EXPECTED_FAILURE_MODE: Persistent cross-asset decoupling invalidates convergence
    assumption and causes repeated contrarian losses.
  entry_rules:
  - Compute delta_er = er20(BTC) - er20(ETH) and its rolling percentile.
  - Require abs(delta_er)_pct >= 0.85.
  - Require abs(BTC standardized close-vwap48 distance) >= 0.7 and BTC rv48_pct <
    0.90.
  - 'Enter opposite sign(delta_er): short if positive, long if negative.'
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - ETH-USD 5m OHLCV
  - er20 feature for BTC and ETH
  - rv48 feature for BTC
  - vwap48 feature for BTC
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      abs_delta_er_pct_min: 0.85
      btc_abs_vwap_dist_z_min: 0.7
      btc_rv48_pct_max: 0.9
      direction_mode: opposite_delta_er_sign
  allowed_variants: []
  status: planned
- id: H109
  family: cross_asset_divergence
  class: tradeable_hypothesis
  logic_hash: 6f1bd1e15cb60bc98ae6b5defea53ceaba2db3c208d966147bc9250eae10a5b5
  description: ATR-state disagreement with ETH-leading continuation map.
  rationale: Test BTC direction when ETH enters higher ATR state while BTC remains
    in lower ATR state.
  WHY: Cross-asset volatility-regime disagreement hypothesis using ATR14 state asymmetry.
  RULES:
  - Define high ATR state as atr14 percentile >= 0.70 and low ATR state as < 0.50.
  - Trigger when ETH is high-ATR and BTC is low-ATR.
  - Require ETH close-vwap48 sign non-zero and BTC rv48 percentile in [0.30,0.85).
  - Trade BTC in ETH close-vwap48 sign direction.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    atr_high_pct_min: 0.7
    atr_low_pct_max: 0.5
    btc_rv48_pct_min: 0.3
    btc_rv48_pct_max: 0.85
  FAILURE_CONDITIONS:
  - ETH ATR lead may not transfer to BTC directionally.
  - State mismatch can occur during noisy cross-asset episodes.
  - RV band may exclude valid transmission periods.
  EXPECTED_FAILURE_MODE: Regime-lead assumption breaks when ETH and BTC respond to
    different catalysts.
  entry_rules:
  - Require atr14_pct(ETH) >= 0.70 and atr14_pct(BTC) < 0.50.
  - Require 0.30 <= rv48_pct(BTC) < 0.85.
  - Enter BTC in sign(close(ETH)-vwap48(ETH)).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - ETH-USD 5m OHLCV
  - atr14 feature for BTC and ETH
  - rv48 feature for BTC
  - vwap48 feature for ETH
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      atr_high_pct_min: 0.7
      atr_low_pct_max: 0.5
      btc_rv48_pct_min: 0.3
      btc_rv48_pct_max: 0.85
      direction_source: eth_close_vs_vwap48_sign
  allowed_variants: []
  status: planned
- id: H110
  family: cross_asset_divergence
  class: tradeable_hypothesis
  logic_hash: a9898c4340aa73058177a2760aae9334060d381c4d8809ae08c6b6416fd9884d
  description: VWAP dislocation disagreement with BTC snapback direction.
  rationale: Test whether opposite BTC/ETH vwap48 displacement signs indicate temporary
    dislocation resolved via BTC snapback.
  WHY: Cross-asset disagreement family using vwap48 dislocation polarity rather than
    same-direction transfer.
  RULES:
  - Require sign(close-vwap48) for BTC and ETH to be opposite.
  - Require both |dist_to_vwap48_z| >= 1.0.
  - Require er20_btc <= 0.50 and er20_eth <= 0.50.
  - Trade BTC opposite its own close-vwap48 sign.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    abs_vwap_dist_z_min: 1.0
    er20_btc_max: 0.5
    er20_eth_max: 0.5
    direction_mode: btc_snapback
  FAILURE_CONDITIONS:
  - Opposite-sign dislocations can persist during broad market bifurcation.
  - Low-ER gating may still include unstable micro-trends.
  - Snapback assumption fails in momentum spillover states.
  EXPECTED_FAILURE_MODE: Dislocation persistence extends beyond fixed horizon, turning
    snapback entries into trend fade losses.
  entry_rules:
  - Require opposite signs between BTC and ETH close-vwap48 distances.
  - Require abs(BTC zdist) >= 1.0 and abs(ETH zdist) >= 1.0.
  - Require er20(BTC) <= 0.50 and er20(ETH) <= 0.50.
  - Enter BTC opposite sign(close(BTC)-vwap48(BTC)).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - ETH-USD 5m OHLCV
  - er20 feature for BTC and ETH
  - vwap48 feature for BTC and ETH
  - vwap distance standardized series for BTC and ETH
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      abs_vwap_dist_z_min: 1.0
      er20_btc_max: 0.5
      er20_eth_max: 0.5
      direction_mode: btc_snapback
  allowed_variants: []
  status: planned
- id: H111
  family: cross_asset_divergence
  class: tradeable_hypothesis
  logic_hash: 8723e7067f208150586d357179c7652d9052bbb896e75a5c0356f2bde68b249d
  description: BTC-ETH spread dislocation mean-reversion under mid-volatility regimes.
  rationale: Test a cross-asset dislocation family that is orthogonal to single-asset
    threshold logic.
  WHY: Target short-horizon spread shocks between BTC and ETH expected to mean-revert
    when both assets are in non-extreme RV states.
  RULES:
  - Define 5m spread return s_t = ret1_btc - ret1_eth.
  - Compute rolling z-score of spread over 96 bars.
  - 'Entry gate: abs(z_spread) >= 2.0.'
  - 'Regime gate: rv48_pct_btc and rv48_pct_eth both in [0.25, 0.80).'
  - 'Session gate: only evaluate entries during UTC 08:00-22:00.'
  - 'Direction: if z_spread >= 2.0 short BTC; if z_spread <= -2.0 long BTC.'
  - No new entry while position is open; apply 2-bar cooldown after exit.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    spread_z_window_bars: 96
    z_entry_threshold: 2.0
    rv48_pct_min: 0.25
    rv48_pct_max: 0.8
    session_start_utc: 08:00
    session_end_utc: '22:00'
    cooldown_bars: 2
    direction_mode: opposite_spread_z_sign
  FAILURE_CONDITIONS:
  - Lead-lag assumption is unstable and spread shocks continue trending instead of
    reverting.
  - Session gate removes too many opportunities and produces insufficient sample/folds.
  - Spread signals are too correlated with existing momentum logic and provide no
    independent edge after costs.
  EXPECTED_FAILURE_MODE: During sustained directional markets, spread dislocations
    persist and mean-reversion entries are repeatedly run over.
  entry_rules:
  - Compute spread return s_t = ret1_btc - ret1_eth and z_spread over 96 bars.
  - Require abs(z_spread) >= 2.0.
  - Require 0.25 <= rv48_pct_btc < 0.80 and 0.25 <= rv48_pct_eth < 0.80.
  - Require UTC hour in [08:00, 22:00).
  - 'Enter opposite spread sign: z_spread > 0 => BTC short, z_spread < 0 => BTC long.'
  - Apply 2-bar cooldown after each closed trade.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - ETH-USD 5m OHLCV
  - ret1 feature for BTC and ETH
  - rv48 percentile features for BTC and ETH
  - UTC timestamp series
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      spread_definition: btc_ret1_minus_eth_ret1
      spread_z_window_bars: 96
      z_entry_threshold: 2.0
      rv48_pct_min: 0.25
      rv48_pct_max: 0.8
      session_start_utc: 08:00
      session_end_utc: '22:00'
      cooldown_bars: 2
      direction_mode: opposite_spread_z_sign
      exit_mode: horizon_only
  allowed_variants: []
  status: planned
- id: H112
  family: volatility_state
  class: tradeable_hypothesis
  logic_hash: 701b3cce95526b2725819ac5baab6ad4e4a4e2a04e56a17340adb7f8871a279d
  description: ATR-to-RV percentile ratio carry in balanced volatility bands.
  rationale: Test directional carry when short-range pressure (ATR pct) dominates
    realized variance without entering shock tails.
  WHY: Add an orthogonal single-asset volatility-ratio family distinct from spread-dislocation
    and prior divergence hypotheses.
  RULES:
  - Compute ratio r = atr14_pct / max(rv48_pct, eps).
  - Require rv48_pct in [0.30, 0.75).
  - Require ratio percentile in [0.60, 0.90).
  - Require abs(dist_to_vwap48_z) in [0.8, 2.0).
  - Trade in sign(dist_to_vwap48) direction.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    rv48_pct_min: 0.3
    rv48_pct_max: 0.75
    atr_rv_pct_ratio_pct_min: 0.6
    atr_rv_pct_ratio_pct_max: 0.9
    abs_vwap_dist_z_min: 0.8
    abs_vwap_dist_z_max: 2.0
    direction_mode: vwap_distance_sign
  FAILURE_CONDITIONS:
  - ATR/RV percentile ratio fails to transport across volatility baselines.
  - Ratio concentration near tails causes unstable entry clustering.
  - VWAP distance filter admits late entries in exhausted moves.
  EXPECTED_FAILURE_MODE: Volatility-regime drift degrades ratio calibration and produces
    inconsistent follow-through.
  entry_rules:
  - Compute atr14_pct and rv48_pct, then r = atr14_pct / max(rv48_pct, eps).
  - Require 0.30 <= rv48_pct < 0.75.
  - Require ratio percentile in [0.60, 0.90).
  - Require 0.8 <= abs(dist_to_vwap48_z) < 2.0.
  - Enter in sign(close-vwap48).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - atr14 feature
  - rv48 feature
  - vwap48 feature
  - standardized close-vwap48 distance
  - rolling percentile of atr14_pct/rv48_pct ratio
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      rv48_pct_min: 0.3
      rv48_pct_max: 0.75
      atr_rv_pct_ratio_pct_min: 0.6
      atr_rv_pct_ratio_pct_max: 0.9
      abs_vwap_dist_z_min: 0.8
      abs_vwap_dist_z_max: 2.0
      direction_mode: vwap_distance_sign
  allowed_variants: []
  status: planned
- id: H113
  family: range_structure
  class: tradeable_hypothesis
  logic_hash: b29d50c3518c49fe6c7e9dafc0a4fbeb185b41e1e70f554f5c53be552b589b7d
  description: 24-bar breakout continuation with ER and RV guards.
  rationale: Test structural range breaks only when directional efficiency supports
    continuation and volatility remains mid-band.
  WHY: Introduce a distinct range-structure family candidate to diversify away from
    spread and volatility-ratio predicates.
  RULES:
  - Define prior range using rolling 24-bar high/low excluding current bar.
  - Trigger when close crosses above prior high or below prior low.
  - Require er20 >= 0.45 and rv48_pct in [0.35, 0.85).
  - Reject overextended states when abs(dist_to_vwap48_z) >= 2.2.
  - Trade in breakout direction.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    breakout_lookback_bars: 24
    er20_min: 0.45
    rv48_pct_min: 0.35
    rv48_pct_max: 0.85
    abs_vwap_dist_z_max: 2.2
    direction_mode: breakout_sign
  FAILURE_CONDITIONS:
  - Breakouts fail in chop despite ER filter.
  - ER threshold lags and admits late entries after move exhaustion.
  - RV mid-band gate excludes too many valid trend transitions.
  EXPECTED_FAILURE_MODE: False-breakout clustering around local ranges creates adverse
    churn and weak post-cost edge.
  entry_rules:
  - Compute prior_high_24 and prior_low_24 from trailing 24 bars (excluding current
    bar).
  - Long when close > prior_high_24; short when close < prior_low_24.
  - Require er20 >= 0.45.
  - Require 0.35 <= rv48_pct < 0.85.
  - Require abs(dist_to_vwap48_z) < 2.2.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - rolling 24-bar prior high/low
  - er20 feature
  - rv48 feature
  - standardized close-vwap48 distance
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      breakout_lookback_bars: 24
      er20_min: 0.45
      rv48_pct_min: 0.35
      rv48_pct_max: 0.85
      abs_vwap_dist_z_max: 2.2
      direction_mode: breakout_sign
  allowed_variants: []
  status: planned
- id: H114
  family: cross_asset_regime
  class: tradeable_hypothesis
  logic_hash: e44fe83f677b56516a9f0c500905d1cc68961715cacddbae57a0d3f8f98ee45e
  description: Cross-asset slope disagreement continuation in neutral spread regimes.
  rationale: Test whether ETH slope leads BTC when assets disagree, but only outside
    spread-tail stress states.
  WHY: Use cross-asset slope disagreement plus spread neutralization to isolate transfer
    effects from tail-regime noise.
  RULES:
  - Compute ETH and BTC 1h EMA20 slope signs.
  - 'Require cross-asset disagreement: eth_slope_sign_1h != btc_slope_sign_1h and
    both non-zero.'
  - Require spread_pct in [0.20, 0.80).
  - Require ETH slope absolute percentile >= 0.65.
  - Trade BTC in ETH slope sign direction.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    spread_pct_min: 0.2
    spread_pct_max: 0.8
    eth_slope_abs_pct_min: 0.65
    direction_source: eth_slope_sign
  FAILURE_CONDITIONS:
  - ETH slope does not lead BTC when disagreement persists for structural reasons.
  - Neutral spread gate overfilters and lowers trade count.
  - High ETH slope magnitude can coincide with reversal, not continuation.
  EXPECTED_FAILURE_MODE: Extended disagreement windows decouple assets and invalidate
    ETH-led direction transfer.
  entry_rules:
  - Require eth_slope_sign_1h != btc_slope_sign_1h and both signs non-zero.
  - Require 0.20 <= spread_pct < 0.80.
  - Require eth_slope_abs_pct_1h >= 0.65.
  - Enter BTC in sign(eth_slope_sign_1h).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m closes
  - ETH-USD 5m closes
  - BTC 1h EMA20 slope sign
  - ETH 1h EMA20 slope sign
  - ETH slope absolute percentile
  - spread_pct feature
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      spread_pct_min: 0.2
      spread_pct_max: 0.8
      eth_slope_abs_pct_min: 0.65
      direction_source: eth_slope_sign
  allowed_variants: []
  status: planned
- id: H115
  family: shock_structure
  class: tradeable_hypothesis
  logic_hash: b14b876c138afea101913f52ce37bbf7d0d3e6220ec844ac089a2dffda7c8759
  description: Delayed BTC fade after isolated ETH shock impulse.
  rationale: Test one-bar delayed BTC mean-reversion after ETH-only shock events when
    BTC is not simultaneously shocked.
  WHY: Separate cross-asset shock-transfer from same-bar reaction and enforce isolation
    from broad market shock states.
  RULES:
  - Define ETH shock as ret1_abs_eth_pct >= 0.95.
  - 'Require BTC is not in concurrent shock state: ret1_abs_btc_pct < 0.90.'
  - Require BTC rv48_pct in [0.35, 0.85).
  - Apply one-bar delay after ETH shock then trade BTC opposite ETH shock direction.
  - Apply minimum 10-bar spacing between entries.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    eth_shock_pct_min: 0.95
    btc_shock_pct_max: 0.9
    btc_rv48_pct_min: 0.35
    btc_rv48_pct_max: 0.85
    entry_delay_bars: 1
    min_signal_gap_bars: 10
    direction_mode: opposite_eth_shock_direction
  FAILURE_CONDITIONS:
  - ETH shock direction continues and delayed fade entries are late.
  - Shock isolation condition under-samples valid transfer events.
  - RV gating misses critical post-shock adjustment periods.
  EXPECTED_FAILURE_MODE: Trend-following shock cascades overwhelm delayed reversion
    logic and produce serial losses.
  entry_rules:
  - Trigger when ret1_abs_eth_pct >= 0.95 and ret1_abs_btc_pct < 0.90.
  - Require 0.35 <= rv48_pct_btc < 0.85.
  - Enter one bar after trigger.
  - Direction is opposite sign(ret1_eth on trigger bar).
  - Enforce at least 10 bars between entries.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - ETH-USD 5m OHLCV
  - ret1_abs percentile features for BTC and ETH
  - rv48 percentile feature for BTC
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      eth_shock_pct_min: 0.95
      btc_shock_pct_max: 0.9
      btc_rv48_pct_min: 0.35
      btc_rv48_pct_max: 0.85
      entry_delay_bars: 1
      min_signal_gap_bars: 10
      direction_mode: opposite_eth_shock_direction
  allowed_variants: []
  status: planned
- id: H116
  family: volatility_conditioning
  class: tradeable_hypothesis
  logic_hash: b277ecab8cfe1b3eb5cfd571896876f28e956690282df875f1a87b7b85c29c8e
  description: Compression-to-expansion breakout with cross-asset bar-direction confirmation.
  rationale: Test volatility expansion continuation only after explicit compression
    and dual-asset directional agreement.
  WHY: Introduce a volatility transition state machine distinct from static percentile
    gates and pure slope-sign transfer.
  RULES:
  - 'Require BTC true-range percentile in low regime for prior 6 bars: tr_btc_pct
    <= 0.25.'
  - Trigger expansion when current tr_btc_pct crosses above 0.60.
  - Require ETH bar direction agrees with BTC bar direction on trigger bar.
  - Require BTC close-vwap48 sign equals BTC bar direction.
  - Trade in BTC trigger-bar direction.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    compression_tr_pct_max: 0.25
    compression_bars: 6
    expansion_tr_pct_cross: 0.6
    confirm_with_eth_bar_direction: true
    confirm_with_vwap_sign: true
    direction_source: btc_bar_direction
  FAILURE_CONDITIONS:
  - Expansion follows noise bursts rather than true breakouts.
  - Dual confirmation suppresses too many signals.
  - Compression windows fail to represent latent energy states.
  EXPECTED_FAILURE_MODE: False expansions after compression produce whipsaw breakouts
    with insufficient follow-through.
  entry_rules:
  - Require tr_btc_pct <= 0.25 for prior 6 bars.
  - Require current tr_btc_pct crosses above 0.60.
  - Require sign(bar_return_btc) == sign(bar_return_eth).
  - Require sign(close_btc-vwap48_btc) equals sign(bar_return_btc).
  - Enter in sign(bar_return_btc).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - ETH-USD 5m OHLCV
  - BTC true-range percentile
  - BTC/ETH bar-return sign
  - BTC vwap48 distance sign
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      compression_tr_pct_max: 0.25
      compression_bars: 6
      expansion_tr_pct_cross: 0.6
      confirm_with_eth_bar_direction: true
      confirm_with_vwap_sign: true
      direction_source: btc_bar_direction
  allowed_variants: []
  status: planned
- id: H117
  family: mean_reversion
  class: tradeable_hypothesis
  logic_hash: 2a12bb19292db000ce2beb2bb43b1d87eed41c37ae3b1db3799e1ed4db450013
  description: Dual-asset VWAP extreme convergence reversion.
  rationale: Test BTC snapback when both BTC and ETH are jointly displaced from VWAP
    in the same direction under low efficiency.
  WHY: Use synchronized cross-asset overextension as a stricter reversion context
    than single-asset VWAP distance.
  RULES:
  - Require BTC dist_to_vwap48_z <= -1.8 for long or >= 1.8 for short.
  - Require ETH dist_to_vwap48_z has same sign and abs >= 1.2.
  - Require BTC er20 <= 0.35.
  - Require spread_pct in [0.15, 0.85).
  - Trade BTC opposite BTC dist_to_vwap48 sign.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    btc_abs_vwap_dist_z_min: 1.8
    eth_abs_vwap_dist_z_min: 1.2
    er20_max: 0.35
    spread_pct_min: 0.15
    spread_pct_max: 0.85
    direction_mode: opposite_btc_vwap_distance_sign
  FAILURE_CONDITIONS:
  - Joint displacement marks broad trend acceleration, not exhaustion.
  - ER filter is insufficient to screen transition regimes.
  - Spread neutral gate still includes unstable decoupling periods.
  EXPECTED_FAILURE_MODE: Multi-asset aligned displacement persists and turns convergence
    entries into repeated trend fades.
  entry_rules:
  - Require abs(dist_to_vwap48_z_btc) >= 1.8 and abs(dist_to_vwap48_z_eth) >= 1.2.
  - Require sign(dist_to_vwap48_btc) == sign(dist_to_vwap48_eth).
  - Require er20_btc <= 0.35.
  - Require 0.15 <= spread_pct < 0.85.
  - Enter BTC opposite sign(dist_to_vwap48_btc).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - ETH-USD 5m OHLCV
  - dist_to_vwap48_z for BTC and ETH
  - er20 for BTC
  - spread_pct feature
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      btc_abs_vwap_dist_z_min: 1.8
      eth_abs_vwap_dist_z_min: 1.2
      er20_max: 0.35
      spread_pct_min: 0.15
      spread_pct_max: 0.85
      direction_mode: opposite_btc_vwap_distance_sign
  allowed_variants: []
  status: planned
- id: H118
  family: volatility_state
  class: tradeable_hypothesis
  logic_hash: 60140cf7b7c3fab607c6ae00a5d0fcb6502ba1d8a0332025ce4213bcf8e380d1
  description: ATR-over-RV pressure carry with bounded VWAP displacement.
  rationale: Test continuation when local range pressure exceeds realized variance
    by a minimum margin.
  WHY: Differentiate from existing volatility-state rules by requiring explicit atr-minus-rv
    separation instead of pure bands.
  RULES:
  - Require rv48_pct in [0.45, 0.88).
  - Require atr14_pct in [0.55, 0.92).
  - Require atr14_pct - rv48_pct >= 0.10.
  - Require abs(dist_to_vwap48_z) in [0.7, 1.9).
  - Trade in sign(dist_to_vwap48).
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    rv48_pct_min: 0.45
    rv48_pct_max: 0.88
    atr14_pct_min: 0.55
    atr14_pct_max: 0.92
    atr_minus_rv_min: 0.1
    abs_vwap_dist_z_min: 0.7
    abs_vwap_dist_z_max: 1.9
    direction_mode: vwap_distance_sign
  FAILURE_CONDITIONS:
  - ATR-RV spread condition captures late volatility spikes.
  - Bounded displacement misses strongest follow-through segments.
  - Band interactions are unstable under volatility regime shifts.
  EXPECTED_FAILURE_MODE: Pressure states are short-lived and reverse before horizon
    completion.
  entry_rules:
  - Require 0.45 <= rv48_pct < 0.88.
  - Require 0.55 <= atr14_pct < 0.92.
  - Require atr14_pct - rv48_pct >= 0.10.
  - Require 0.7 <= abs(dist_to_vwap48_z) < 1.9.
  - Enter in sign(dist_to_vwap48).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - atr14 percentile feature
  - rv48 percentile feature
  - vwap48 distance z-score feature
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      rv48_pct_min: 0.45
      rv48_pct_max: 0.88
      atr14_pct_min: 0.55
      atr14_pct_max: 0.92
      atr_minus_rv_min: 0.1
      abs_vwap_dist_z_min: 0.7
      abs_vwap_dist_z_max: 1.9
      direction_mode: vwap_distance_sign
  allowed_variants: []
  status: planned
- id: H119
  family: efficiency_mean_reversion
  class: tradeable_hypothesis
  logic_hash: 0f833f58269e4481f786e3eff0082aba1649e009749618ca73c4f364e56d63bc
  description: Efficiency-collapse reversion after mid-efficiency states.
  rationale: Test reversion immediately after rapid ER deterioration from moderate
    efficiency regimes.
  WHY: Introduce an ER-delta trigger (state transition) rather than static ER threshold
    classification.
  RULES:
  - Require er20 in [0.25, 0.55).
  - Require 3-bar change in er20 <= -0.12.
  - Require abs(dist_to_vwap48_z) >= 1.1.
  - Require rv48_pct < 0.90.
  - Trade opposite dist_to_vwap48 sign.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    er20_min: 0.25
    er20_max: 0.55
    er20_delta3_max: -0.12
    abs_vwap_dist_z_min: 1.1
    rv48_pct_max: 0.9
    direction_mode: opposite_vwap_distance_sign
  FAILURE_CONDITIONS:
  - ER drops are noise and do not indicate exhaustion.
  - Moderate ER band includes mixed regimes with weak edge.
  - Distance filter triggers into momentum continuation.
  EXPECTED_FAILURE_MODE: Rapid ER decay occurs during regime transitions where trends
    extend despite apparent efficiency collapse.
  entry_rules:
  - Require 0.25 <= er20 < 0.55.
  - Require er20[t] - er20[t-3] <= -0.12.
  - Require abs(dist_to_vwap48_z) >= 1.1 and rv48_pct < 0.90.
  - Enter opposite sign(dist_to_vwap48).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - er20 feature
  - vwap48 distance z-score feature
  - rv48 percentile feature
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      er20_min: 0.25
      er20_max: 0.55
      er20_delta3_max: -0.12
      abs_vwap_dist_z_min: 1.1
      rv48_pct_max: 0.9
      direction_mode: opposite_vwap_distance_sign
  allowed_variants: []
  status: planned
- id: H120
  family: range_structure
  class: tradeable_hypothesis
  logic_hash: ca0df5814108bcedbaa1df63904eef82dc95b4855457938fd37cb30d436b20f2
  description: Inside-bar range break continuation with ER and RV gates.
  rationale: Test break continuation only when a tight inside-bar setup resolves with
    acceptable efficiency and volatility context.
  WHY: Use structural inside-bar setup plus breakout resolution to avoid generic prior-high/prior-low
    triggers.
  RULES:
  - Define 18-bar prior high/low excluding current bar.
  - 'Require inside-bar setup: prior bar high < prior_high_18 and prior bar low >
    prior_low_18.'
  - Entry on current close breakout above prior_high_18 or below prior_low_18.
  - Require er20 >= 0.40 and rv48_pct in [0.30, 0.80).
  - Trade in breakout direction.
  PARAMETERS:
    lookback_days: 180
    horizon_bars: 8
    breakout_lookback_bars: 18
    require_inside_bar_setup: true
    er20_min: 0.4
    rv48_pct_min: 0.3
    rv48_pct_max: 0.8
    direction_mode: breakout_sign
  FAILURE_CONDITIONS:
  - Inside-bar setups break frequently in low-liquidity chop.
  - ER filter misses true breakouts that start from low efficiency.
  - RV band truncates high-conviction expansion legs.
  EXPECTED_FAILURE_MODE: Frequent false range breaks produce churn before directional
    extension materializes.
  entry_rules:
  - Compute prior_high_18 and prior_low_18 from trailing 18 bars excluding current.
  - Require inside bar at t-1 inside [prior_low_18, prior_high_18].
  - Long if close_t > prior_high_18; short if close_t < prior_low_18.
  - Require er20 >= 0.40.
  - Require 0.30 <= rv48_pct < 0.80.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV
  - rolling 18-bar prior high/low
  - inside-bar detector
  - er20 feature
  - rv48 percentile feature
  parameters:
    lookback_days: 180
    horizon_bars: 8
    fixed:
      breakout_lookback_bars: 18
      require_inside_bar_setup: true
      er20_min: 0.4
      rv48_pct_min: 0.3
      rv48_pct_max: 0.8
      direction_mode: breakout_sign
  allowed_variants: []
  status: planned
- id: H121
  family: funding_regime
  class: tradeable_hypothesis
  description: Funding extreme long fade — short BTC when perp funding is crowded
    long and ETH slope is positive.
  rationale: 'When perpetual funding is at extreme positive levels (top 10% of 30-day
    rolling window), the market is maximally long and paying a daily carry tax to
    stay long. A normally bullish ETH slope signal (the H32 entry condition) becomes
    unreliable in this environment because there is no marginal buyer left. Fade the
    crowd.

    '
  WHY: Extreme funding = crowded positioning = mean reversion pressure. Use derivatives
    sentiment to gate spot entries.
  RULES:
  - Compute rolling 30-day percentile rank of BTC funding rate (window=8640 bars at
    5m = 90 settlements).
  - Require funding_btc_pct >= 0.90 (extreme long territory).
  - Require ETH 1h slope positive (eth_slope_sign_1h > 0).
  - Require spread_pct < 0.10 (short-tail regime, same condition as H32).
  - Enter SHORT BTC (fade the crowded long).
  - Require rv48_pct in [0.30, 0.80) to avoid trading into shock vol.
  FAILURE_CONDITIONS:
  - Extreme funding persists for days in strong trending markets — fade loses in sustained
    trends.
  - 30-day window may be too short to define extreme in structural bull markets.
  EXPECTED_FAILURE_MODE: Trending markets sustain high funding; fade logic triggers
    premature shorts that get squeezed.
  entry_rules:
  - Require funding_btc_pct >= 0.90 (rolling 8640-bar window).
  - Require eth_slope_sign_1h > 0.
  - Require spread_pct < 0.10.
  - Require 0.30 <= rv48_pct < 0.80.
  - Enter short BTC (signal_dir = -1).
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - ETH-USD 5m OHLCV (Coinbase)
  - ETH 1h EMA20 slope sign
  - ETH-BTC spread_pct (H19 regime)
  - funding_rate_btc from rc.funding_rates (Bybit)
  - funding_btc_pct rolling percentile
  - rv48_pct
  parameters:
    lookback_days: 365
    horizon_bars: 6
    fixed:
      funding_pct_threshold: 0.9
      funding_window_bars: 8640
      spread_pct_max: 0.1
      rv48_pct_min: 0.3
      rv48_pct_max: 0.8
      signal_dir: -1
  allowed_variants: []
  status: fail
  decision_date: '2026-02-22'
  decision_notes: 'FAIL. 365d: 70 trades, gross P>0=0.771 but WF only 3/11 folds positive.
    Extreme funding crowding does not reliably predict short-term mean reversion at
    5m/6-bar horizon.'
  prerequisites:
  - rc.funding_rates populated via scripts/backfill_derivatives.py
  - funding_regime family added to research_family_runner.py SUPPORTED_FAMILIES
  - load_frame() extended with app/db/derivatives.py merge_asof join
- id: H122
  family: funding_regime
  class: tradeable_hypothesis
  description: Funding sign flip momentum — trade in the direction of a BTC funding
    regime change.
  rationale: 'A flip from positive to negative funding (or vice versa) marks a structural
    shift in perpetual market positioning. These regime changes in sentiment often
    precede directional moves on spot as leveraged positions unwind or re-build.

    '
  WHY: Sentiment regime change (not just level) carries directional information —
    the transition matters more than the absolute value.
  RULES:
  - Detect when BTC funding sign changes from the prior 8h settlement.
  - 'Trade in direction of the new funding sign: positive flip = long, negative flip
    = short.'
  - Require rv48_pct in [0.20, 0.85).
  - 'Cooldown: no new entry within 2 * horizon bars of a prior flip entry.'
  FAILURE_CONDITIONS:
  - Funding oscillates sign in choppy low-vol environments producing many small flips
    with no follow-through.
  EXPECTED_FAILURE_MODE: High-frequency sign oscillation in sideways markets generates
    whipsaw entries.
  entry_rules:
  - Require funding_btc_flip == True (sign changed at most recent 8h settlement).
  - Long if new funding_sign > 0, short if new funding_sign < 0.
  - Require 0.20 <= rv48_pct < 0.85.
  - Cooldown gap of 2 * horizon bars from prior flip entry.
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - funding_rate_btc from rc.funding_rates (Bybit)
  - funding_btc_sign and funding_btc_flip computed features
  - rv48_pct
  parameters:
    lookback_days: 365
    horizon_bars: 6
    fixed:
      cooldown_multiplier: 2
      rv48_pct_min: 0.2
      rv48_pct_max: 0.85
  allowed_variants: []
  status: fail
  decision_date: '2026-02-22'
  decision_notes: FAIL. Funding sign flip does not predict returns. Gross mean=-0.00038,
    P>0=0.08 (negative before costs). Clean failure.
  prerequisites:
  - rc.funding_rates populated via scripts/backfill_derivatives.py
  - funding_regime family added to research_family_runner.py SUPPORTED_FAMILIES
  - load_frame() extended with app/db/derivatives.py merge_asof join
- id: H123
  family: funding_regime
  class: tradeable_hypothesis
  description: H32 core logic filtered by non-extreme funding — exclude crowded-long
    entries from H32 signal.
  rationale: 'H32 (frozen: trade BTC in ETH 1h slope direction when spread_pct < 0.10)
    passes at gross and 8bps. The hypothesis is that entries occurring when BTC perpetual
    funding is in extreme long territory are the drag on H32 performance. Removing
    those entries should improve the mean without touching any frozen H32 rules.

    '
  WHY: Test whether filtering crowded-long funding states out of H32 improves net
    performance. H32 itself stays frozen.
  RULES:
  - 'Apply exact H32 entry logic: ETH 1h slope sign direction when spread_pct < 0.10.'
  - 'Additional gate only: require funding_btc_pct < 0.85.'
  - All other H32 parameters unchanged.
  FAILURE_CONDITIONS:
  - If crowded-long entries are not the drag, filtering them just reduces n with no
    mean improvement.
  - Reduced sample size makes bootstrap CI too wide to confirm edge.
  EXPECTED_FAILURE_MODE: Reduced n drops below statistical significance threshold
    before mean improvement is confirmed.
  entry_rules:
  - Require spread_pct < 0.10 (ETH-BTC short-tail regime).
  - Require eth_slope_sign_1h != 0.
  - Enter in eth_slope_sign_1h direction.
  - 'Additional gate: require funding_btc_pct < 0.85.'
  exit_rules:
  - Hold for fixed horizon bars.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - ETH-USD 5m OHLCV (Coinbase)
  - ETH 1h EMA20 slope sign
  - ETH-BTC spread_pct (H19 regime)
  - funding_rate_btc from rc.funding_rates (Bybit)
  - funding_btc_pct rolling percentile
  parameters:
    lookback_days: 365
    horizon_bars: 6
    fixed:
      spread_pct_max: 0.1
      funding_pct_ceiling: 0.85
      funding_window_bars: 8640
  allowed_variants: []
  status: fail
  decision_date: '2026-02-22'
  decision_notes: 'FAIL on cost gate. 365d: gross P>0=1.000, WF 12/14 folds positive
    — signal is real. But ~7 trades/day × 8bps cost exceeds +0.065% gross edge. Next:
    H124 with tighter spread threshold to reduce to 1-2 trades/day.'
  prerequisites:
  - rc.funding_rates populated via scripts/backfill_derivatives.py
  - funding_regime family added to research_family_runner.py SUPPORTED_FAMILIES
- id: H124
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: H32 core logic with ultra-tight spread threshold — targeting 1-2 trades/day
    to clear 8bps cost gate.
  rationale: H123 (H32 + funding ceiling) confirmed gross edge (P>0=1.000, WF 12/14)
    but failed cost gate at ~7 trades/day. The spread_pct < 0.10 threshold is too
    permissive — it fires on moderately tight spreads that lack the edge needed to
    cover 8bps. Restricting to spread_pct in the bottom 3rd rolling percentile (approximately
    < 0.03-0.05) targets only the most extreme ETH-BTC convergence events, reducing
    frequency to ~1-2 trades/day where per-trade gross edge should be sufficient.
  WHY: Test whether the H32 signal survives costs when restricted to only the most
    extreme spread compression events.
  RULES:
  - Apply exact H32 entry logic — ETH 1h slope sign direction.
  - Require spread_pct >= 0.97 (long) or spread_pct <= 0.03 (short) — extreme tail only.
  - Retain H123 funding ceiling — require funding_btc_pct < 0.85.
  - All other H32 parameters unchanged (horizon 6 bars).
  FAILURE_CONDITIONS:
  - Extreme spread threshold produces too few trades to reach statistical significance.
  - Extreme spread events are driven by microstructure noise, not regime — edge
    does not generalize.
  - Gross edge at this threshold is higher but variance is also higher — net result
    still fails cost gate.
  EXPECTED_FAILURE_MODE: Sample size drops below significance threshold before cost-adjusted
    mean becomes positive.
  entry_rules:
  - Require spread_pct >= 0.97 (long entries) or spread_pct <= 0.03 (short entries).
  - Require eth_slope_sign_1h != 0.
  - Enter in eth_slope_sign_1h direction.
  - Require funding_btc_pct < 0.85.
  exit_rules:
  - Hold for fixed 6 bars (30 min). No early exit.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - ETH-USD 5m OHLCV (Coinbase)
  - ETH 1h EMA20 slope sign
  - spread_pct feature
  - funding_btc_pct from rc.funding_rates (Hyperliquid)
  parameters:
    lookback_days: 365
    horizon_bars: 6
    fixed:
      spread_pct_long_min: 0.97
      spread_pct_short_max: 0.03
      funding_pct_ceiling: 0.85
      funding_window_bars: 8640
      direction_mode: eth_slope_sign
  allowed_variants: []
  status: borderline
  decision_date: '2026-02-23'
  decision_notes: 'BORDERLINE. Tighter threshold (0.97/0.03) reduced frequency to
    2.1/day as intended. Gross: P>0=1.000, WF 12/14. bps8: P>0=0.863, WF 9/14,
    CI=[-0.018%, +0.064%] straddles zero. Edge exists but too thin to clear 8bps
    cleanly. No further variants — moving to RF-generated H125+.'
  source: Successor to H123. Tighter spread threshold to reduce frequency from ~7/day
    to ~1-2/day and clear 8bps cost gate.
- id: H125
  family: momentum
  class: tradeable_hypothesis
  description: BTC VWAP z-score momentum signal gated by HMM TRENDING regime.
  rationale: RF walk-forward (7/7 folds, all three horizons) consistently identifies
    vwap_z as the strongest predictive feature. Positive direction across folds means
    price above VWAP predicts further upside in trending markets — a momentum filter,
    not mean reversion. Regime gate isolates the signal to periods where momentum
    is expected to persist.
  WHY: Test whether BTC price position relative to session VWAP, in a confirmed trending
    regime, generates a positive 60min forward return edge.
  RULES:
  - Compute vwap_z as rolling z-score of (close - vwap) over 8640 bars (365d window).
  - Require vwap_z above 70th rolling percentile (price stretched above VWAP).
  - Require HMM regime label = TRENDING at current bar.
  - Enter long only.
  - Hold for fixed horizon of 12 bars (60 min).
  FAILURE_CONDITIONS:
  - VWAP z-score reverts rapidly in trending markets, giving negative forward return.
  - Signal fires too frequently to survive 8bps cost gate.
  - TRENDING regime label lag causes stale entries at regime transitions.
  EXPECTED_FAILURE_MODE: Entries cluster at local exhaustion points within trending
    legs, producing mean reversion rather than continuation.
  entry_rules:
  - Compute vwap_z = rolling_zscore(close - vwap, window=8640).
  - Compute vwap_z_pct = rolling rank percentile of vwap_z over 8640 bars.
  - Require vwap_z_pct >= 0.70.
  - Require rc.regime_labels.regime = 'TRENDING' at current bar timestamp.
  - Enter long at next bar open.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - vwap_z feature from load_frame()
  - rc.regime_labels populated via scripts/ml/hmm_regime_discovery.py
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      vwap_z_pct_min: 0.70
      regime_gate: TRENDING
      direction: long
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL. Gross barely positive (P>0=0.838, WF 7/11) but bps8 decisively
    negative — P>0=0.000, CI=[-0.095%,-0.034%]. vwap_z momentum in TRENDING does
    not generate enough gross edge to survive 8bps costs.'
  source: RF walk-forward H6/H12/H24 — 7/7 folds all horizons. Pre-committed direction
    positive (momentum), threshold 70th pct, horizon 12 bars.
- id: H126
  family: cross_asset
  class: tradeable_hypothesis
  description: ETH distance from 48h VWAP (z-scored) as BTC long signal, gated by
    TRENDING regime.
  rationale: RF walk-forward (7/7 folds at H6 and H12, 6/7 at H24) consistently
    surfaces dist_to_vwap48_z_eth as a cross-asset predictor of BTC forward returns.
    When ETH is stretched above its 48h VWAP in a trending regime, BTC tends to follow
    — consistent with ETH leading cross-asset momentum.
  WHY: Test whether ETH VWAP deviation, used as a cross-asset momentum signal, predicts
    BTC 60min forward returns in trending regimes.
  RULES:
  - Compute dist_to_vwap48_z_eth as rolling z-score of ETH (close - vwap_48h) over
    8640 bars.
  - Require dist_to_vwap48_z_eth above 70th rolling percentile.
  - Require HMM regime label = TRENDING at current bar.
  - Enter long BTC only.
  - Hold for fixed horizon of 12 bars (60 min).
  FAILURE_CONDITIONS:
  - ETH VWAP deviation does not lead BTC — contemporaneous, not predictive.
  - Signal collapses in cost gate due to high trade frequency.
  - Regime filter insufficient to isolate edge from RANGING noise.
  EXPECTED_FAILURE_MODE: ETH/BTC co-movement is too tight — ETH above VWAP is already
    priced into BTC at entry.
  entry_rules:
  - Compute dist_to_vwap48_z_eth = rolling_zscore(eth_close - eth_vwap_48h, window=8640).
  - Compute dist_to_vwap48_z_eth_pct = rolling rank percentile over 8640 bars.
  - Require dist_to_vwap48_z_eth_pct >= 0.70.
  - Require rc.regime_labels.regime = 'TRENDING' at current bar timestamp.
  - Enter long BTC at next bar open.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - ETH-USD 5m OHLCV (Coinbase)
  - dist_to_vwap48_z_eth feature from load_frame()
  - rc.regime_labels populated via scripts/ml/hmm_regime_discovery.py
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      dist_to_vwap48_z_eth_pct_min: 0.70
      regime_gate: TRENDING
      direction: long
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL. No gross signal — mean=-0.001%, P>0=0.484, WF 6/12. ETH
    VWAP deviation in TRENDING does not predict BTC forward returns. bps8 decisively
    negative.'
  source: RF walk-forward — 7/7 folds H6 and H12, 6/7 H24. Pre-committed direction
    positive, threshold 70th pct, horizon 12 bars.
- id: H127
  family: funding_regime
  class: tradeable_hypothesis
  description: High BTC funding rate percentile as momentum signal, gated by TRENDING
    regime.
  rationale: RF walk-forward (5/7 folds at H6, 7/7 at H12, 7/7 at H24) consistently
    identifies funding_btc_pct with positive direction. High perpetual funding means
    longs are paying shorts — leveraged long positioning in a confirmed trending regime
    signals momentum continuation, not crowding risk. Regime gate distinguishes this
    from the crowded-long failure mode seen in H121/H122.
  WHY: Test whether elevated BTC funding, conditioned on a confirmed TRENDING regime,
    predicts positive 60min forward returns — a leveraged-positioning momentum signal.
  RULES:
  - Compute funding_btc_pct as rolling percentile of hourly funding rate over 8640
    bars (365d window).
  - Require funding_btc_pct above 70th rolling percentile.
  - Require HMM regime label = TRENDING at current bar.
  - Enter long only.
  - Hold for fixed horizon of 12 bars (60 min).
  FAILURE_CONDITIONS:
  - High funding in trending regimes already signals late-cycle crowding, producing
    mean reversion.
  - Funding data lag (hourly vs 5m bars) introduces stale entries.
  - Signal frequency too low to establish statistical significance.
  EXPECTED_FAILURE_MODE: Funding peaks coincide with exhaustion tops — the same crowding
    failure seen in H121/H122, just in a trending regime context.
  entry_rules:
  - Load funding_btc_pct from rc.funding_rates via compute_funding_features().
  - Compute rolling rank percentile over 8640 bars.
  - Require funding_btc_pct >= 0.70.
  - Require rc.regime_labels.regime = 'TRENDING' at current bar timestamp.
  - Enter long at next bar open.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - funding_btc_pct from rc.funding_rates (Hyperliquid)
  - rc.regime_labels populated via scripts/ml/hmm_regime_discovery.py
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      funding_btc_pct_min: 0.70
      regime_gate: TRENDING
      direction: long
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL. High funding + TRENDING is too rare — n=168, 0.8/day. Gross
    P>0=0.648, WF 5/10, no meaningful edge. bps8 P>0=0.001. Sample too small to
    confirm or deny signal.'
  source: RF walk-forward — 5/7 folds H6, 7/7 H12, 7/7 H24. Most direction-stable
    new signal. Pre-committed positive, threshold 70th pct, horizon 12 bars.
- id: H128
  family: volatility_state
  class: tradeable_hypothesis
  description: High BTC 48h realized vol percentile as trend continuation signal,
    gated by TRENDING regime.
  rationale: RF walk-forward (7/7 folds at H6 and H12, 6/7 at H24) consistently
    identifies rv48_pct_btc. Direction is positive at H12 and H24 — high realized
    vol in a confirmed trending regime signals an active trend with sustained directional
    movement. Using a 2h horizon (24 bars) per trend signal type in REGIME_FRAMEWORK.md.
  WHY: Test whether elevated BTC 48h realized vol, in a confirmed trending regime,
    predicts positive 2h forward returns.
  RULES:
  - Compute rv48_pct_btc as rolling percentile of 48h realized vol over 8640 bars.
  - Require rv48_pct_btc above 70th rolling percentile.
  - Require HMM regime label = TRENDING at current bar.
  - Enter long only.
  - Hold for fixed horizon of 24 bars (2h).
  FAILURE_CONDITIONS:
  - High vol in trending regime = late-stage trend exhaustion, not continuation.
  - 24-bar hold is too long — edge evaporates before exit.
  - TRENDING + high vol overlaps with VOLATILE regime mislabeling.
  EXPECTED_FAILURE_MODE: High realized vol marks the end of trending legs rather than
    the middle, producing negative 2h forward returns.
  entry_rules:
  - Compute rv48_pct_btc = rolling rank percentile of rv_48h_btc over 8640 bars.
  - Require rv48_pct_btc >= 0.70.
  - Require rc.regime_labels.regime = 'TRENDING' at current bar timestamp.
  - Enter long at next bar open.
  exit_rules:
  - Hold for fixed 24 bars (2h). No early exit.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - rv48_pct_btc feature from load_frame()
  - rc.regime_labels populated via scripts/ml/hmm_regime_discovery.py
  parameters:
    lookback_days: 365
    horizon_bars: 24
    fixed:
      rv48_pct_btc_min: 0.70
      regime_gate: TRENDING
      direction: long
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL on cost gate. Gross signal exists (P>0=0.923, WF 8/11,
    mean=+0.052%) but bps8 negative — P>0=0.208, CI=[-0.099%,+0.042%]. Closest
    of the RF batch to passing. Gross edge real but too thin at 24-bar horizon.'
  source: RF walk-forward — 7/7 folds H6 and H12, 6/7 H24. Horizon pre-committed
    to 24 bars (trend type per REGIME_FRAMEWORK.md). Direction positive, threshold
    70th pct.
- id: H129
  family: volatility_state
  class: tradeable_hypothesis
  description: High ETH 14-bar ATR percentile as cross-asset volatility expansion
    signal, gated by TRENDING regime.
  rationale: RF walk-forward (6/7 folds at H6 and H12, 7/7 at H24) consistently
    identifies atr14_pct_eth with positive direction across all three horizons —
    the most direction-consistent volatility candidate. High ETH ATR in a trending
    regime indicates active cross-asset expansion.
  WHY: Test whether elevated ETH short-term ATR, in a confirmed TRENDING regime,
    predicts positive BTC 60min forward returns.
  RULES:
  - Compute atr14_pct_eth as rolling percentile of ETH 14-bar ATR over 8640 bars.
  - Require atr14_pct_eth above 70th rolling percentile.
  - Require HMM regime label = TRENDING at current bar.
  - Enter long BTC only.
  - Hold for fixed horizon of 12 bars (60 min).
  FAILURE_CONDITIONS:
  - High ETH ATR signals two-sided volatility, not directional — BTC path is random.
  - Cross-asset ATR lead is contemporaneous, not predictive.
  - Regime gate overlaps too tightly with VOLATILE mislabels.
  EXPECTED_FAILURE_MODE: High ETH ATR in TRENDING is symmetric — up and down moves
    equally likely, producing zero-mean forward returns.
  entry_rules:
  - Compute atr14_pct_eth = rolling rank percentile of ETH atr14 over 8640 bars.
  - Require atr14_pct_eth >= 0.70.
  - Require rc.regime_labels.regime = 'TRENDING' at current bar timestamp.
  - Enter long BTC at next bar open.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - ETH-USD 5m OHLCV (Coinbase)
  - atr14_pct_eth feature from load_frame()
  - rc.regime_labels populated via scripts/ml/hmm_regime_discovery.py
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      atr14_pct_eth_pct_min: 0.70
      regime_gate: TRENDING
      direction: long
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL. No gross signal — mean=-0.013%, P>0=0.257, WF 5/11. ETH
    ATR in TRENDING produces two-sided volatility as expected in failure mode. bps8
    decisively negative P>0=0.000.'
  source: RF walk-forward — 6/7 folds H6 and H12, 7/7 H24. Most direction-consistent
    volatility signal. Pre-committed positive, threshold 70th pct, horizon 12 bars.
- id: H130
  family: mean_reversion
  class: tradeable_hypothesis
  description: BTC VWAP z-score mean-reversion in TRENDING regime — short when price
    stretched above VWAP, long when stretched below.
  rationale: Regime-conditional RF (TRENDING bars only, 5/5 folds) identifies vwap_z
    with NEGATIVE direction — the opposite of what full-dataset RF suggested. Within
    a trending regime, price stretched above VWAP mean-reverts back toward the trend
    line rather than continuing. This corrects the H125 direction error and is grounded
    in the regime-specific signal discovery.
  WHY: Test whether vwap_z mean-reversion (short above, long below) generates a
    positive 60min forward return edge specifically within TRENDING regimes.
  RULES:
  - Compute vwap_z rolling percentile over 8640 bars.
  - Long signal when vwap_z below 30th pct (price stretched below VWAP in trend).
  - Short signal when vwap_z above 70th pct (price stretched above VWAP in trend).
  - Require HMM regime = TRENDING.
  - Hold for fixed 12 bars (60 min).
  FAILURE_CONDITIONS:
  - Trending regimes sustain directional moves without mean-reversion — entries
    against trend are stopped out.
  - VWAP deviation in trends is asymmetric — below works, above does not (or vice versa).
  - Trade frequency too high to survive 8bps costs.
  EXPECTED_FAILURE_MODE: Trend continuation dominates — stretches above VWAP extend
    further before reverting, producing losses on the short side.
  entry_rules:
  - Compute vwap_z_pct = rolling rank percentile of vwap_z over 8640 bars.
  - Long when vwap_z_pct <= 0.30 AND regime = TRENDING.
  - Short when vwap_z_pct >= 0.70 AND regime = TRENDING.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - vwap_z feature from load_frame()
  - rc.regime_labels populated via scripts/ml/hmm_regime_discovery.py
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      vwap_z_pct_long_max: 0.30
      vwap_z_pct_short_min: 0.70
      regime_gate: TRENDING
      direction: mean_reversion
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL. No gross signal — mean=-0.019%, P>0=0.061, WF 2/12. vwap_z
    mean-reversion in TRENDING does not hold as a simple rule. Trend continuation
    dominates both sides.'
  source: Regime-conditional RF TRENDING 5/5 folds — direction NEGATIVE. Corrects
    H125 direction error. Pre-committed both sides, threshold 70th/30th pct, horizon 12 bars.
- id: H131
  family: mean_reversion
  class: tradeable_hypothesis
  description: BTC VWAP z-score momentum in RANGING regime — long when price above
    VWAP, short when below.
  rationale: Regime-conditional RF (RANGING bars only, 6/6 folds) identifies vwap_z
    with POSITIVE direction and SHAP=0.243 — the strongest signal found in any regime.
    In ranging markets, price above VWAP signals intra-range momentum continuation
    (not mean reversion), consistent with price oscillating within the range and
    building on short-term direction.
  WHY: Test whether vwap_z long-above/short-below generates a positive 60min forward
    return edge specifically within RANGING regimes.
  RULES:
  - Compute vwap_z rolling percentile over 8640 bars.
  - Long when vwap_z above 70th pct AND regime = RANGING.
  - Short when vwap_z below 30th pct AND regime = RANGING.
  - Hold for fixed 12 bars (60 min).
  FAILURE_CONDITIONS:
  - RANGING regime contains enough chop that vwap_z position has no directional
    predictive power.
  - Signal frequency too high — too many entries within a ranging day.
  - Short side loses while long side wins (asymmetric).
  EXPECTED_FAILURE_MODE: Range oscillation is too fast — price above VWAP reverts
    within the 12-bar hold, producing churn not continuation.
  entry_rules:
  - Compute vwap_z_pct = rolling rank percentile of vwap_z over 8640 bars.
  - Long when vwap_z_pct >= 0.70 AND regime = RANGING.
  - Short when vwap_z_pct <= 0.30 AND regime = RANGING.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - vwap_z feature from load_frame()
  - rc.regime_labels populated via scripts/ml/hmm_regime_discovery.py
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      vwap_z_pct_long_min: 0.70
      vwap_z_pct_short_max: 0.30
      regime_gate: RANGING
      direction: momentum
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL. No gross signal — mean=-0.008%, P>0=0.166, WF 5/13. vwap_z
    momentum in RANGING fires 6.9/day with no directional edge. Range oscillation
    is symmetric.'
  source: Regime-conditional RF RANGING 6/6 folds — SHAP=0.243, strongest signal
    found. Direction POSITIVE. Pre-committed both sides, threshold 70th/30th pct, horizon 12 bars.
- id: H132
  family: funding_regime
  class: tradeable_hypothesis
  description: High BTC funding percentile as long signal in VOLATILE regime.
  rationale: Regime-conditional RF (VOLATILE bars only, 5/5 folds) identifies funding_btc_pct
    with positive direction and SHAP=0.153 — the top feature in the volatile regime.
    In volatile markets, elevated funding means leveraged longs are paying heavily
    to maintain positions. Rather than signaling crowding risk (as in calm markets),
    high funding in volatility signals committed directional conviction — the market
    is volatile AND longs are paying up, consistent with an upside move.
  WHY: Test whether high BTC funding in VOLATILE regimes predicts positive 60min
    forward returns — a conviction signal in chaotic conditions.
  RULES:
  - Compute funding_btc_pct rolling percentile over 8640 bars.
  - Long when funding_btc_pct above 70th pct AND regime = VOLATILE.
  - Long only (no short side — volatile regime shorts have outsized risk).
  - Hold for fixed 12 bars (60 min).
  FAILURE_CONDITIONS:
  - High funding in volatile markets = leveraged longs about to be liquidated
    (cascade risk). Signal fires just before reversals.
  - Sample size too small — VOLATILE + high funding is a rare combination.
  - Volatile regime creates too much noise to see signal over 12 bars.
  EXPECTED_FAILURE_MODE: Liquidation cascade — high funding in volatile markets
    precedes forced selling, producing negative forward returns.
  entry_rules:
  - Load funding_btc_pct from rc.funding_rates via compute_funding_features().
  - Compute rolling rank percentile over 8640 bars.
  - Long when funding_btc_pct >= 0.70 AND regime = VOLATILE.
  - Enter long only.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - funding_btc_pct from rc.funding_rates (Hyperliquid)
  - rc.regime_labels populated via scripts/ml/hmm_regime_discovery.py
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      funding_btc_pct_min: 0.70
      regime_gate: VOLATILE
      direction: long
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL. No gross signal — mean=-0.019%, P>0=0.156, WF 2/13. High
    funding in VOLATILE does not predict direction — liquidation cascade risk dominates.
    bps8 P>0=0.000, WF 0/13.'
  source: Regime-conditional RF VOLATILE 5/5 folds — top feature, SHAP=0.153,
    direction POSITIVE. Long-only. Pre-committed 70th pct, horizon 12 bars.
- id: H133
  family: volatility_state
  class: tradeable_hypothesis
  description: High BTC 14-bar ATR percentile as long signal in RANGING regime.
  rationale: Regime-conditional RF (RANGING bars only, 6/6 folds) identifies atr14_pct_btc
    with positive direction and SHAP=0.099. In ranging markets, a high ATR reading
    signals a volatility expansion within the range — bars are moving more than usual,
    which predicts the start of a directional leg (breakout from range or strong
    intra-range move) rather than continued chop.
  WHY: Test whether elevated BTC ATR within a RANGING regime predicts positive 60min
    forward returns — a volatility expansion signal in consolidation.
  RULES:
  - Compute atr14_pct_btc rolling percentile over 8640 bars.
  - Long when atr14_pct_btc above 70th pct AND regime = RANGING.
  - Long only.
  - Hold for fixed 12 bars (60 min).
  FAILURE_CONDITIONS:
  - High ATR in ranging = noise/whipsaw, not directional expansion. No persistent
    edge after the spike.
  - Long-only misses the symmetric two-sided nature of ATR expansion in ranges.
  - Signal fires on both directions equally — long bias produces zero-mean returns.
  EXPECTED_FAILURE_MODE: ATR expansion in ranging regime is symmetric — price moves
    up and down with equal probability, producing zero net edge long-only.
  entry_rules:
  - Compute atr14_pct_btc = rolling rank percentile of BTC atr14 over 8640 bars.
  - Long when atr14_pct_btc >= 0.70 AND regime = RANGING.
  - Enter long only.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - atr14_pct_btc feature from load_frame()
  - rc.regime_labels populated via scripts/ml/hmm_regime_discovery.py
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      atr14_pct_btc_min: 0.70
      regime_gate: RANGING
      direction: long
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL. No gross signal — mean=-0.013%, P>0=0.298, WF 1/13. ATR
    expansion in RANGING is symmetric — long-only bias has no edge. bps8 P>0=0.000,
    WF 0/13.'
  source: Regime-conditional RF RANGING 6/6 folds — SHAP=0.099, direction POSITIVE.
    Long-only. Pre-committed 70th pct, horizon 12 bars.

- id: H134
  family: volatility_state
  class: tradeable_hypothesis
  description: Two-feature short signal in VOLATILE regime — ETH extended above 48h VWAP
    AND BTC realized volatility spiking.
  rationale: SHAP interaction analysis (VOLATILE-only RF, 5/5 folds) found that
    dist_to_vwap48_z_eth and rv48_pct_btc have a synergistic interaction effect.
    Quadrant analysis on out-of-sample test windows showed mean_fwd_r = -0.167%
    per 60min bar (5/5 folds, 80% directional consistency) when BOTH features are
    in the top 30% during VOLATILE regime bars. This is the first multi-feature rule
    from the interaction pipeline — both features are non-CA (genuine new discovery).
  WHY: Test whether the confluence of ETH being extended above its 48h anchor AND
    BTC volatility spiking produces reliable mean-reversion short edge in volatile
    regimes. Logic — volatility spike + ETH overextension signals near-term peak.
  RULES:
  - Gate on regime = VOLATILE (from rc.regime_labels).
  - Compute dist_to_vwap48_z_eth rolling 70th percentile over 8640 bars.
  - Compute rv48_pct_btc rolling 70th percentile (already a rolling percentile).
  - Short when dist_to_vwap48_z_eth > 70th pct AND rv48_pct_btc > 70th pct AND
    regime = VOLATILE.
  - Short only. Hold 12 bars (60 min).
  FAILURE_CONDITIONS:
  - ETH extension + BTC vol spike may occur at start of a rally (not a reversal) —
    if it's a breakout, shorting gets crushed.
  - VOLATILE regime label lags by one fold step — stale label at regime transitions.
  - Two conditions reduce sample size dramatically — may be too few trades to clear
    statistical gates.
  entry_rules:
  - dist_to_vwap48_z_eth_pct = rolling 8640-bar percentile of dist_to_vwap48_z_eth.
  - Short when dist_to_vwap48_z_eth_pct >= 0.70 AND rv48_pct_btc >= 0.70 AND
    hmm_regime == VOLATILE.
  - Short only (signal = -1.0).
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - dist_to_vwap48_z_eth feature from load_frame()
  - rv48_pct_btc feature from load_frame()
  - rc.regime_labels populated via scripts/ml/hmm_regime_discovery.py
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      dist_to_vwap48_z_eth_pct_min: 0.70
      rv48_pct_btc_min: 0.70
      regime_gate: VOLATILE
      direction: short
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL. Gross signal real but weak: mean=2.64bps, P>0=0.826, WF
    P>0=0.848. Signal direction correct (52.9% win rate short) but magnitude too small
    to clear 8bps cost gate. bps8 mean=-5.36bps, P>0=0.027. Root cause: percentile
    threshold computed across all bars (not VOLATILE-only) dilutes the condition
    vs RF quadrant analysis which used VOLATILE-bar-only percentiles. Signal is real
    but insufficient edge at 8bps cost.'
  source: RF SHAP interaction pipeline (VOLATILE-only, 5/5 folds). dist_to_vwap48_z_eth
    HIGH AND rv48_pct_btc HIGH → SHORT. mean_fwd_r=-0.167%, 80% direction consistent.
    Pre-committed 70th pct thresholds, horizon 12 bars.

- id: H135
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Two-feature long signal — BTC strongly outperforming ETH (spread_pct > p70)
    AND BTC temporarily below its short-term VWAP (vwap_z < p30). Buy the dip within
    the cross-asset trend.
  rationale: Full-dataset RF SHAP interaction analysis (7/7 folds) found spread_pct
    and vwap_z to be a top-interacting pair. Quadrant analysis showed the
    (spread_pct HIGH, vwap_z LOW) quadrant has mean_fwd_r = +0.081% per 60min bar,
    7/7 folds, 71% direction consistent. This is a CA signal plus oversold VWAP
    filter — when BTC is in a strong cross-asset trend (high spread_pct) AND temporarily
    oversold short-term (low vwap_z), price tends to recover. Extends the existing
    CA family by adding a mean-reversion timing layer.
  WHY: Test whether adding a vwap_z dip filter to the CA trend signal (spread_pct)
    concentrates the edge — fires only when the trend is intact AND price has pulled
    back. Raw mean_r = +8.1bps per 60min bar — right at the 8bps cost threshold.
  RULES:
  - Compute spread_pct rolling percentile over 8640 bars (already computed).
  - Compute vwap_z rolling percentile over 8640 bars.
  - Long when spread_pct > 70th pct AND vwap_z < 30th pct.
  - Long only. No regime gate. Hold 12 bars (60 min).
  FAILURE_CONDITIONS:
  - vwap_z < p30 after a large CA trend move may be early — the dip continues.
  - Overlap with existing CA signals reduces new information content.
  - Mean_r of +8.1bps is right at the cost threshold — likely BORDERLINE at bps8.
  entry_rules:
  - vwap_z_pct = rolling 8640-bar percentile of vwap_z.
  - Long when spread_pct >= 0.70 AND vwap_z_pct <= 0.30.
  - Long only (signal = +1.0). No regime gate.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - spread_pct feature from load_frame()
  - vwap_z feature from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      spread_pct_min: 0.70
      vwap_z_pct_max: 0.30
      direction: long
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL (cost-constrained). Strong gross signal: mean=5.48bps, P>0=0.996,
    CI=[+1.5, +9.5]bps. WF gross P>0=0.960. Signal is very real — P>0=0.996 is
    extremely high confidence. But mean=5.48bps < 8bps cost gate. bps8 mean=-2.52bps,
    P>0=0.108. Win rate = 57.3% (meaningful directional accuracy). This is the first
    multi-feature hypothesis to show a strong gross signal since the interaction
    pipeline was added. The CA+VWAP dip combination is genuinely predictive —
    gap is ~2.5bps. Next step: tighten to p85/p15 thresholds → fewer but higher
    magnitude trades.'
  source: Full-dataset RF SHAP interaction pipeline (7/7 folds). spread_pct HIGH AND
    vwap_z LOW → LONG. mean_fwd_r=+0.081%, 71% direction consistent. Pre-committed
    70th/30th pct thresholds, horizon 12 bars.

- id: H136
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Tighter version of H135 — BTC strongly outperforming ETH (spread_pct > p85)
    AND BTC more deeply below its short-term VWAP (vwap_z < p15). Higher threshold
    means fewer but larger-magnitude trades.
  rationale: H135 showed gross P>0=0.996 with mean=5.48bps — clearly real signal
    but 2.5bps below the 8bps cost gate. The natural next step is to tighten both
    thresholds from p70/p30 to p85/p15, forcing the signal to fire only when both
    conditions are more extreme. At more extreme percentiles, the
    expected per-trade return is higher (larger mean reversion), which may clear
    the cost gate. Pre-committed to p85/p15 — no further optimization if this fails.
  WHY: Test whether tightening the CA+VWAP dip thresholds from p70/p30 to p85/p15
    pushes the mean return from ~5.5bps to >8bps, crossing the cost gate.
  RULES:
  - Compute spread_pct rolling percentile over 8640 bars (already computed).
  - Compute vwap_z rolling percentile over 8640 bars.
  - Long when spread_pct > 85th pct AND vwap_z < 15th pct.
  - Long only. No regime gate. Hold 12 bars (60 min).
  FAILURE_CONDITIONS:
  - Tighter thresholds reduce sample size too much — insufficient trades for
    statistical power.
  - The mean return does not increase with tighter thresholds (flat across extremes).
  - Only a couple more bps of improvement needed — if this still fails, the
    combination fundamentally cannot clear 8bps at any reasonable frequency.
  entry_rules:
  - vwap_z_pct = rolling 8640-bar percentile of vwap_z.
  - Long when spread_pct >= 0.85 AND vwap_z_pct <= 0.15.
  - Long only (signal = +1.0). No regime gate.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - spread_pct feature from load_frame()
  - vwap_z feature from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      spread_pct_min: 0.85
      vwap_z_pct_max: 0.15
      direction: long
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL. Tighter thresholds made things WORSE: n=101 (0.3/day),
    gross mean=3.90bps (DOWN from H135 5.48bps), P>0=0.760 (DOWN from 0.996),
    CI=[-7.7, +14.6]bps (very wide). WF gross P>0=0.546. The more extreme conditions
    pick up unusual events (wide spread at reversal tops) rather than the clean
    trend+dip combination. Too few trades for reliable statistics. Conclusion:
    spread_pct + vwap_z combination cannot clear 8bps cost at any practical threshold.
    The 5.48bps gross edge (H135) is the ceiling for this strategy. CLOSE FAMILY.'
  source: Derived from H135 (FAIL cost-constrained). Tighter thresholds p85/p15
    to attempt higher per-trade magnitude. Horizon 12 bars.

- id: H137
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: RANGING regime — BTC volatility spike AND BTC outperforming ETH
    (atr14_pct_btc > p70 AND spread_pct > p70) → LONG BTC. Breakout from range
    signal supported by cross-asset confirmation.
  rationale: Regime-conditional RF (RANGING-only, 4/6 folds) SHAP interaction
    analysis found atr14_pct_btc and spread_pct have synergistic effects. Quadrant
    analysis showed (atr14_pct_btc HIGH, spread_pct HIGH) in RANGING has mean_fwd_r
    = +0.109% per 60min bar, 4/6 folds, 75% direction consistent. This is the
    highest raw mean_r of any CA-overlap pair rule (10.9bps). Logic — in ranging
    markets, a volatility spike signals a potential breakout. When that spike is
    accompanied by BTC outperforming ETH (positive CA signal), the breakout is
    more likely to be upward.
  WHY: Test whether combining elevated BTC ATR with a bullish CA signal in RANGING
    regime generates >8bps edge. The 10.9bps raw estimate gives headroom above cost.
  RULES:
  - Gate on regime = RANGING.
  - Compute atr14_pct_btc rolling percentile (already computed, use directly).
  - Compute spread_pct rolling percentile (already computed, use directly).
  - Long when atr14_pct_btc >= 0.70 AND spread_pct >= 0.70 AND regime = RANGING.
  - Long only. Hold 12 bars (60 min).
  FAILURE_CONDITIONS:
  - High ATR in RANGING is symmetric — long-only misses downward breakouts.
  - spread_pct signal already captured by existing CA family (H65 etc.) — no
    additional information from ATR.
  - 4/6 folds is weak sample for an interaction rule.
  entry_rules:
  - Long when atr14_pct_btc >= 0.70 AND spread_pct >= 0.70 AND hmm_regime == RANGING.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - atr14_pct_btc and spread_pct features from load_frame()
  - rc.regime_labels populated via scripts/ml/hmm_regime_discovery.py
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      atr14_pct_btc_min: 0.70
      spread_pct_min: 0.70
      regime_gate: RANGING
      direction: long
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL. Same pattern as H134-H136 — gross signal present but
    too small: mean=4.06bps, P>0=0.835, CI=[-4.0, +12.9]bps. 158 trades (0.4/day)
    due to RANGING gate + dual condition. BPS8 P>0=0.181. WF gross P>0=0.693.
    Reducing sample with RANGING gate causes same cost-insufficient pattern as
    other multi-feature regime rules. CA signal still dominant — ATR filter does
    not add enough incremental edge.'
  source: RF SHAP interaction (RANGING-only, 4/6 folds). atr14_pct_btc HIGH AND
    spread_pct HIGH in RANGING → LONG. mean_fwd_r=+0.109%, 75% consistent. Pre-committed
    70th pct thresholds, horizon 12 bars.

- id: H138
  family: volatility_state
  class: tradeable_hypothesis
  description: RANGING regime — low ETH 14-bar ATR AND low BTC funding percentile
    (atr14_pct_eth < p30 AND funding_btc_pct < p30) → SHORT BTC. Calm ETH + bearish
    funding in ranging market signals downward resolution.
  rationale: Regime-conditional RF (RANGING-only, 3/6 folds) SHAP interaction
    analysis found atr14_pct_eth and funding_btc_pct have synergistic effects.
    Quadrant analysis showed (atr14_pct_eth LOW, funding_btc_pct LOW) in RANGING
    has mean_fwd_r = -0.083% per 60min bar, 3/6 folds, 100% direction consistent.
    Both are non-CA features — genuine new discovery. Logic — in ranging markets,
    when ETH is calm (low ATR = not at an expansion point) AND funding is bearish
    (longs paying little or shorts being paid), the market is positioned for a
    downward move rather than an upward one.
  WHY: First non-CA multi-feature SHORT rule in RANGING regime. Low ETH vol +
    bearish funding as a downward bias signal. Test whether it clears 8bps cost.
  RULES:
  - Gate on regime = RANGING.
  - Compute atr14_pct_eth rolling percentile (already computed, use directly).
  - Compute funding_btc_pct rolling percentile (already computed, use directly).
  - Short when atr14_pct_eth < 30th pct AND funding_btc_pct < 30th pct AND
    regime = RANGING.
  - Short only. Hold 12 bars (60 min).
  FAILURE_CONDITIONS:
  - Only 3/6 folds is weak — the pattern may be noise in a limited subset.
  - Low funding + low vol is a common combination with no directional edge.
  - Short-only misses symmetric mean-reversion bounces.
  entry_rules:
  - Short when atr14_pct_eth <= 0.30 AND funding_btc_pct <= 0.30 AND hmm_regime == RANGING.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - atr14_pct_eth and funding_btc_pct features from load_frame()
  - rc.regime_labels populated via scripts/ml/hmm_regime_discovery.py
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      atr14_pct_eth_max: 0.30
      funding_btc_pct_max: 0.30
      regime_gate: RANGING
      direction: short
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL. No gross signal: mean=-0.66bps, P>0=0.314. Win rate=49.2%
    (signal direction WRONG). BPS8 mean=-8.66bps, P>0=0.000. The regime-filtered
    RF (RANGING-only) computed percentile thresholds on RANGING bars only, but the
    runner uses all-bars rolling percentiles → different condition selected than RF
    expected. Also 3/6 folds is too small a sample for the 100% consistency to be
    meaningful. Root cause: percentile mismatch between regime-filtered RF and
    all-bars runner for non-percentile features. Lesson: only use full-dataset RF
    rules (no --regime flag) for hypothesis derivation.'
  source: RF SHAP interaction (RANGING-only, 3/6 folds, 100% consistent). atr14_pct_eth
    LOW AND funding_btc_pct LOW in RANGING → SHORT. mean_fwd_r=-0.083%, 100% consistent.
    Pre-committed 30th pct thresholds, horizon 12 bars.

- id: H139
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: Full-dataset two-feature SHORT — BTC high realized volatility
    (rv48_pct_btc > p70) AND BTC underperforming ETH (spread_pct < p30).
    Volatile BTC in a relative underperformance vs ETH signals continued downside.
  rationale: Full-dataset RF SHAP interaction analysis (7/7 folds) found rv48_pct_btc
    and spread_pct to be a top-interacting pair. Quadrant analysis on out-of-sample
    test windows showed (rv48_pct_btc HIGH, spread_pct LOW) has mean_fwd_r = -0.099%
    per 60min bar, 7/7 folds, 71% direction consistent. Both features are percentile-form
    (no z-score mismatch). This is the only full-dataset pair rule with raw mean_r
    above the 8bps cost threshold (-9.9bps). Logic — high BTC realized volatility
    while BTC is lagging ETH suggests BTC is in a volatile downswing, likely to
    continue near-term.
  WHY: Test whether high BTC vol + bearish CA signal produces >8bps SHORT edge.
    Both features are pure percentile form — runner thresholds should match RF
    estimates closely (no z-score mismatch). Raw estimate -9.9bps vs 8bps cost gate.
  RULES:
  - Compute rv48_pct_btc rolling percentile (already computed, use directly).
  - Compute spread_pct rolling percentile (already computed, use directly).
  - Short when rv48_pct_btc >= 0.70 AND spread_pct <= 0.30.
  - Short only. No regime gate. Hold 12 bars (60 min).
  FAILURE_CONDITIONS:
  - High volatility + low spread may fire at the bottom of a move (before bounce).
  - spread_pct < p30 is a weaker CA condition than the original H65 family
    (which used p10 thresholds with slope confirmation). May not have sufficient
    edge without slope confirmation.
  - Direction inconsistency (71% folds agree, 2/7 disagree). Edge may be
    conditional on unstable regime mix.
  entry_rules:
  - Short when rv48_pct_btc >= 0.70 AND spread_pct <= 0.30.
  - Short only (signal = -1.0). No regime gate.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - rv48_pct_btc and spread_pct features from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      rv48_pct_btc_min: 0.70
      spread_pct_max: 0.30
      direction: short
  allowed_variants: []
  status: fail
  decision_date: '2026-02-23'
  decision_notes: 'FAIL (cost-constrained). Same pattern as H135. Gross signal
    extremely strong: mean=5.24bps, P>0=0.995, CI=[+1.4, +9.1]bps. WF gross
    mean=6.12bps, P>0=0.998 — among the strongest WF gross evidence we have seen.
    But bps8 mean=-2.76bps, P>0=0.085. n=970 trades (2.7/day). This completes
    the interaction pipeline hypothesis exploration: H135 (P>0=0.996, 5.48bps)
    and H139 (P>0=0.998 WF, 6.12bps WF) are both real signals hitting the same
    ~5-6bps ceiling. The multi-feature signals at p70/p30 thresholds consistently
    produce ~5-6bps gross — 2-3bps below 8bps cost gate. This appears to be a
    fundamental ceiling for this feature combination class at these thresholds.
    No further attempts at this class are warranted without a cost reduction or
    a fundamentally different feature set.'
  source: Full-dataset RF SHAP interaction pipeline (7/7 folds). rv48_pct_btc HIGH
    AND spread_pct LOW → SHORT. mean_fwd_r=-0.099%, 71% direction consistent.
    Pre-committed 70th/30th pct thresholds, horizon 12 bars. Both features are
    already percentile-form — no z-score/percentile mismatch with runner.

- id: H140
  family: funding_regime
  class: tradeable_hypothesis
  status: FAIL
  decision_date: "2026-02-23"
  decision_notes: "FAIL (cost-constrained). n=1461, 4.0/day, gross=0.94bps P>0=0.801
    (BORDERLINE), WF+ 12/18 folds. P>0_bps8=0.000. The over-short extreme does
    produce a slight positive gross edge but nowhere near the 8bps cost gate.
    Consistent with the structural ~5-6bps ceiling we see on all rate/momentum signals."
  description: Extreme negative BTC funding (shorts paying large premium) predicts
    mean reversion LONG. When market is maximally over-short, short squeeze risk is
    elevated.
  rationale: Perpetual funding rates measure the cost of holding a directional position.
    When funding is very negative (< p10 of 30d history), shorts are paying longs a
    large premium to maintain their position. This indicates an over-short market
    where the crowd is heavily positioned for downside. Historically in crypto,
    over-short extremes tend to self-correct via short covering rallies.
  WHY: Test whether extreme negative funding (over-short condition) predicts positive
    BTC forward returns. Directional opposite of H121 (extreme positive funding fade
    which failed with negative gross). Over-short markets may be more predictive than
    over-long due to more violent short squeeze dynamics.
  RULES:
  - Enter LONG when funding_btc_pct <= 0.10 (bottom decile of 30d rolling history).
  - Persistent signal — hold as long as condition is true, horizon=8 bars per entry.
  - No regime gate. No slope confirmation required.
  FAILURE_CONDITIONS:
  - If the market is in a sustained downtrend, negative funding can persist while
    price continues lower (trend-following shorts remain profitable).
  - H121 (extreme positive funding fade) failed with negative gross — if the
    symmetric positive version fails directionally, this version may also be wrong.
  entry_rules:
  - Long when funding_btc_pct <= 0.10.
  - Signal = +1.0. Hold for horizon bars.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - funding_btc_pct from rc.funding_rates via Hyperliquid
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      funding_btc_pct_max: 0.10
      direction: long
  allowed_variants: []
  source: Theory-first. Anchor candidate for FR family — extreme negative funding
    (over-short) as standalone LONG signal. First test of this mechanism.

- id: H141
  family: funding_regime
  class: tradeable_hypothesis
  description: Extreme positive BTC funding (crowded longs) combined with ETH slope
    flipping negative predicts amplified SHORT. Leveraged longs are forced to unwind
    when trend reverses.
  rationale: CA-1 (ETH slope flip) generates ~0.34%/trade on average. When the market
    is simultaneously crowded long (high funding), a downward slope flip is amplified
    by the forced unwind of leveraged positions. The funding level acts as a crowding
    multiplier for the CA signal — more longs need to exit quickly, creating stronger
    selling pressure.
  WHY: Test whether CA slope flip DOWN, when conditioned on extreme positive funding,
    produces higher per-trade edge than CA-1 alone. Hypothesis is that the crowding
    amplifies the move beyond the baseline CA-1 mean of 0.34%.
  RULES:
  - Enter SHORT when ALL conditions met at same bar.
  - Condition 1 — crowded longs: funding_btc_pct >= 0.85.
  - Condition 2 — slope flip down: eth_slope_sign_1h == -1 AND prior bar was +1.
  - Event-triggered (dedup gap = horizon bars).
  FAILURE_CONDITIONS:
  - Reduces frequency significantly vs CA-1 (requires both conditions simultaneously).
  - Extreme funding often occurs in trending up markets — slope flips down may
    be brief corrections rather than trend reversals in that context.
  - H123 (CA + funding gate) failed cost-constrained. This is a different mechanism
    (extreme funding as amplifier vs. exclusion gate) but may face same issue.
  entry_rules:
  - Short when funding_btc_pct >= 0.85 AND eth_slope flips from +1 to -1.
  - Signal = -1.0. Event-triggered with dedup gap = horizon.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - funding_btc_pct from rc.funding_rates via Hyperliquid
  - eth_slope_sign_1h from load_frame() 1h feature set
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      funding_btc_pct_min: 0.85
      slope_direction: down_flip
      direction: short
  allowed_variants: []
  status: INCONCLUSIVE
  decision_date: "2026-02-23"
  decision_notes: "INCONCLUSIVE (n=16, too few trades). gross=17.64bps P>0=0.863,
    WF+ 1/5 folds, P>0_bps8=0.702. The concept is promising — extreme crowding +
    slope flip yields high per-trade returns — but 16 trades in 365d is insufficient
    for statistical conclusions. The extreme funding (p85) + slope flip combo is rare.
    Consider loosening to p80 or testing on longer dataset before drawing conclusions.
    Do NOT iterate thresholds yet — this is the anchor test."
  source: Theory-first. Tests whether extreme crowding amplifies CA-1 slope flip
    signal on the short side.

- id: H142
  family: funding_regime
  class: tradeable_hypothesis
  description: ETH perpetual funding much higher than BTC funding predicts SHORT BTC.
    When ETH is more crowded long than BTC in the perps market, ETH longs are at
    higher risk of forced liquidation which drags BTC lower.
  rationale: The cross-asset funding spread (ETH funding minus BTC funding) measures
    relative positioning crowding. When ETH perp longs are paying a much higher
    premium than BTC perp longs, ETH is more over-leveraged. ETH corrections tend
    to drag BTC lower (they are 0.85+ correlated). Additionally, extreme ETH crowding
    relative to BTC may compress the ETH-BTC spread (the CA signal basis), which
    itself predicts a bearish environment for BTC.
  WHY: First test of cross-asset funding spread as a directional signal. Tests a
    genuinely new data dimension — the relative positioning across assets in the
    derivatives market.
  RULES:
  - Compute funding_spread = funding_rate_eth - funding_rate_btc.
  - Compute funding_spread_pct = rolling 30d percentile rank of funding_spread.
  - Enter SHORT when funding_spread_pct >= 0.80.
  - Persistent signal. No slope confirmation required.
  FAILURE_CONDITIONS:
  - High ETH funding vs BTC could reflect ETH-specific bullish catalyst (not crowding).
  - Correlation between ETH and BTC corrections may not be tight enough at 8-bar horizon.
  - If H121 (BTC-only extreme funding fade) failed, cross-asset variant may too.
  entry_rules:
  - Short when funding_spread_pct >= 0.80 (ETH much more crowded than BTC).
  - Signal = -1.0. Hold for horizon bars.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - funding_rate_btc and funding_rate_eth from rc.funding_rates via Hyperliquid
  - funding_spread_pct computed in compute_funding_features()
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      funding_spread_pct_min: 0.80
      direction: short
  allowed_variants: []
  status: FAIL
  decision_date: "2026-02-23"
  decision_notes: "FAIL (no gross edge). n=2400, 6.6/day, gross=-0.10bps P>0=0.443,
    WF+ 5/18 folds. ETH-BTC funding spread divergence has no directional edge.
    When ETH perps are more crowded than BTC, the short BTC prediction is wrong —
    the spread likely reflects genuine ETH overperformance vs. BTC rotation, not
    imminent correction. The mechanism is economically flawed."
  source: Theory-first. Cross-asset funding spread as a new directional signal.
    Requires funding_spread_pct feature added to compute_funding_features().

- id: H143
  family: funding_regime
  class: tradeable_hypothesis
  description: BTC funding sign and ETH slope sign consensus — both point in same
    direction simultaneously — predicts continuation in that direction.
  rationale: CA-1 uses only ETH slope momentum. Funding sign reflects market positioning
    consensus. When both agree (slope positive AND funding positive = bullish
    positioning AND upward momentum), the signal should have higher conviction than
    either alone. This is a conjunction of two independent information sources —
    price-derived momentum and derivatives-market sentiment.
  WHY: Test whether requiring both ETH slope direction AND BTC funding sign to agree
    produces a higher-quality CA-type signal. The hypothesis is that funding sign
    filters out CA signals where market participants are positioned against the move
    (which may be shorter-lived).
  RULES:
  - Enter when eth_slope_sign_1h != 0 AND funding_btc_sign != 0 AND both are equal.
  - Direction = eth_slope_sign_1h (same as CA-1 direction).
  - Persistent signal — active whenever both conditions agree.
  FAILURE_CONDITIONS:
  - Funding sign changes infrequently vs slope sign — agreement may be coincidental.
  - This is a less stringent filter than H141 (extreme level vs. any positive sign).
  - High frequency if slope and funding are broadly correlated (both often positive
    in bull markets) — may not improve over CA-1 meaningfully.
  entry_rules:
  - Long when eth_slope_sign_1h == +1 AND funding_btc_sign == +1.
  - Short when eth_slope_sign_1h == -1 AND funding_btc_sign == -1.
  - Signal = eth_slope_sign_1h. Persistent.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h from load_frame() 1h features
  - funding_btc_sign from rc.funding_rates via Hyperliquid
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      direction: consensus
  allowed_variants: []
  status: FAIL
  decision_date: "2026-02-23"
  decision_notes: "FAIL (cost-constrained). n=6821, 18.7/day, gross=1.63bps P>0=1.000,
    WF+ 16/20 folds. Real directional signal with P>0=1.000 (certain) but only 1.63bps
    gross — far below the 8bps cost gate. Same structural cost ceiling as all momentum
    signals. The funding+slope consensus fires too often (every time they re-agree after
    disagreement ~hourly) with too little per-trade edge. DO NOT iterate thresholds."
  source: Theory-first. Funding sign + slope sign consensus as a higher-conviction
    CA variant. Tests whether derivatives positioning confirms price momentum.

- id: H144
  family: funding_regime
  class: tradeable_hypothesis
  description: Sustained extreme positive BTC funding (3+ consecutive hours) combined
    with ETH slope flipping negative. Deep, persistent crowding leads to a more
    violent forced unwind than a single-period funding spike.
  rationale: H141 tests extreme funding at the moment of slope flip. H144 adds the
    requirement that funding has been extreme for 3+ consecutive hours — meaning the
    crowding is deep and systematic, not a brief spike. Markets with sustained crowded-
    long positioning contain more accumulated forced-sell pressure. When the slope
    eventually flips negative in this context, the unwind should be more severe and
    last longer than a single-period crowding event.
  WHY: Tests whether sustained (vs. instantaneous) extreme funding amplifies the
    CA slope flip signal more than H141. Requires 3h × 12 bars = 36 consecutive
    bars with funding_btc_pct >= 0.85 before the slope flip trigger.
  RULES:
  - Compute sustained_extreme = rolling(36).min(funding_btc_pct >= 0.85) == 1.0.
  - Trigger: eth_slope_sign_1h flips from +1 to -1 AND sustained_extreme is True.
  - Event-triggered (dedup gap = horizon bars). Signal = -1.0 (SHORT only).
  - Horizon = 12 bars (60 min) — slightly longer than CA-1 given deeper crowding.
  FAILURE_CONDITIONS:
  - Very few signals (requires 3+ consecutive hours of extreme funding AND a slope flip
    in that window). Small n may make statistical inference unreliable.
  - Sustained extreme funding tends to occur during prolonged bull runs — slope
    flips within those periods may be brief and followed by resumption of trend.
  entry_rules:
  - Short when funding_btc_pct >= 0.85 for 3+ consecutive hours AND slope flips down.
  - Signal = -1.0. Event-triggered with dedup gap = horizon.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - funding_btc_pct from rc.funding_rates via Hyperliquid
  - eth_slope_sign_1h from load_frame() 1h feature set
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      funding_btc_pct_min: 0.85
      sustained_hours: 3
      direction: short
  allowed_variants: []
  status: INCONCLUSIVE
  decision_date: "2026-02-23"
  decision_notes: "INCONCLUSIVE (n=11, too few trades). gross=4.94bps P>0=0.543,
    WF+ 0/5 folds, P>0_bps8=0.387. Only 11 trades in 365d — the condition (3h of
    sustained extreme funding AND a slope flip) is extremely rare. No conclusions
    possible. DO NOT iterate. Accept that this mechanism requires OI or liquidation
    data to confirm crowding rather than just funding duration."
  source: Theory-first. Extended version of H141 requiring sustained (not just
    instantaneous) extreme funding before the slope flip trigger.

- id: H145
  family: volume_state
  class: tradeable_hypothesis
  description: High volume ETH slope flip — same CA-1 signal but only when volume
    is expanding (BTC volume > p80). High-volume directional moves have more
    conviction and are more likely to continue.
  rationale: CA-1 fires on every ETH slope flip regardless of volume. Volume expansion
    during a slope flip signals that large participants are actively driving the move —
    not just noise or thin-book artifacts. High-volume flips should have higher
    per-trade edge because the move is being backed by real capital flows. This is
    a quality filter on the CA-1 anchor signal.
  WHY: Test whether gating CA-1 by high volume (volume_btc_pct >= 0.80) increases
    per-trade mean return at the cost of reduced frequency. If high-volume flips
    average meaningfully more than the CA-1 baseline of 0.34%, this is the anchor
    for the volume_state signal family.
  RULES:
  - Enter when eth_slope_sign_1h flips direction (same as CA-1).
  - Additional gate: volume_btc_pct >= 0.80 at the time of the flip.
  - Direction = direction of new slope sign. Event-triggered.
  FAILURE_CONDITIONS:
  - Reduces frequency significantly — fewer qualifying flips.
  - High volume may simply indicate high volatility, not directional conviction
    (volume spikes on both directions equally during volatile periods).
  - If CA-1 already captures all edge from the slope flip, volume adds no signal.
  entry_rules:
  - Long/Short when eth_slope flips AND volume_btc_pct >= 0.80.
  - Direction = new eth_slope_sign. Event-triggered with dedup gap = horizon.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h from load_frame() 1h features
  - volume_btc_pct computed in load_frame() from candle volume
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      volume_btc_pct_min: 0.80
  allowed_variants: []
  status: PASS
  signal_group: VS-1
  decision_date: "2026-02-23"
  decision_notes: "PASS — VS-1 ANCHOR. n=136, 0.4/day, gross=26.19bps P>0=1.000,
    WF+ 15/18 folds (83%), P>0_bps8=1.000. Clears cost gate convincingly. High-volume
    ETH slope flips carry ~3x the per-trade edge of unfiltered CA-1 signals. Volume
    filters out noise flips — only slope changes backed by real capital flows persist.
    NEXT: run robustness checks (odd/even subsample, p75/p85 threshold variants,
    1-bar lag). If robustness holds, expand VS family."
  source: Theory-first. Volume filter on CA-1 as anchor for volume_state family.
    Tests whether high-volume slope flips have more edge than average flips.

- id: H146
  family: volume_state
  class: tradeable_hypothesis
  description: Volume breakout — BTC price closes above prior 12-bar rolling high
    on expanding volume (> p75). Classic momentum breakout with volume confirmation.
  rationale: A price breakout above a recent high on expanding volume signals that
    buyers are absorbing available supply at higher prices with genuine participation.
    In crypto, breakout moves on high volume tend to continue as trend-followers
    and late buyers chase the move. This is a fundamentally different signal from
    the CA slope flip — it is price-structure based rather than cross-asset based.
  WHY: Test whether high-volume price breakouts predict continued upside over 8 bars
    in BTC. This is a classic momentum signal that has never been formally tested
    in this pipeline. Anchor candidate for volume_state breakout signals.
  RULES:
  - Enter LONG when close_btc > rolling_max(close_btc, 12 bars).shift(1).
  - Additional gate: volume_btc_pct >= 0.75.
  - LONG only (no symmetric short on breakdown — test directional first).
  - Event-triggered with dedup gap = horizon bars.
  FAILURE_CONDITIONS:
  - Breakouts often fail in ranging/choppy markets (false breakouts).
  - May fire at local highs just before reversals (classic retail trap).
  - If CA-1 is already capturing the same momentum, breakout adds no independent edge.
  entry_rules:
  - Long when close_btc > prior 12-bar high AND volume_btc_pct >= 0.75.
  - Signal = +1.0 (LONG only). Event-triggered.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - volume_btc_pct computed in load_frame() from candle volume
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      volume_btc_pct_min: 0.75
      lookback_bars: 12
      direction: long
  allowed_variants: []
  status: FAIL
  decision_date: "2026-02-23"
  decision_notes: "FAIL (no edge). n=2077, 5.7/day, gross=0.15bps P>0=0.555,
    WF+ 10/18 folds. Volume breakouts (price > 12-bar high on high volume) have no
    directional edge in BTC 5m bars. In crypto perps, breakouts above short lookback
    highs on high volume are common and not predictive — the signal fires during both
    genuine moves and noise spikes. The 12-bar lookback (1h) is too short to be
    meaningful structure."
  source: Theory-first. Volume breakout as anchor for volume_state breakout signals.
    Classic price-structure + volume confirmation momentum signal, untested in pipeline.

- id: H147
  family: volume_state
  class: tradeable_hypothesis
  description: Low volume large bar — fade (mean reversion). A large BTC bar on
    very low volume lacks genuine participation and is likely to reverse.
  rationale: Large price moves on unusually low volume are characteristic of thin-book
    artifacts, spoofing, or temporary liquidity imbalances rather than genuine directional
    conviction. In liquid markets, real directional moves require volume participation.
    When a large bar occurs with very low volume (< p20), the probability of mean
    reversion is elevated — the move lacks the supply/demand imbalance needed to sustain.
  WHY: Test whether large-move + low-volume bars predict reversal over the next 6 bars.
    Shorter horizon (6 bars = 30 min) than CA-type signals because mean reversion
    tends to be faster than momentum continuation. Anchor candidate for volume_state
    MR signals.
  RULES:
  - Identify bars where ret1_abs_btc_pct >= 0.80 (top 20% bar size) AND volume_btc_pct <= 0.20.
  - Enter in OPPOSITE direction to bar_dir_btc (fade the move).
  - Event-triggered with dedup gap = horizon bars.
  - Horizon = 6 bars (30 min) — shorter for mean reversion.
  FAILURE_CONDITIONS:
  - Low volume moves in crypto may be legitimate breakouts in off-peak hours (Asia session).
  - Defining large + low-volume jointly may produce very few qualifying events.
  - 5m bar_dir_btc may not reliably measure the direction of the move being faded.
  entry_rules:
  - Fade (go opposite to bar direction) when ret1_abs_btc_pct >= 0.80 AND volume_btc_pct <= 0.20.
  - Direction = -bar_dir_btc. Event-triggered.
  exit_rules:
  - Hold for fixed 6 bars (30 min). No early exit.
  required_inputs:
  - BTC-USD 5m OHLCV (Coinbase)
  - ret1_abs_btc_pct and bar_dir_btc from load_frame()
  - volume_btc_pct computed in load_frame() from candle volume
  parameters:
    lookback_days: 365
    horizon_bars: 6
    fixed:
      volume_btc_pct_max: 0.20
      ret1_abs_btc_pct_min: 0.80
      direction: fade
  allowed_variants: []
  status: FAIL
  decision_date: "2026-02-23"
  decision_notes: "FAIL (cost-constrained). n=403, 1.1/day, gross=2.41bps P>0=0.984,
    WF+ 12/18 folds. Real directional signal — low-volume large bars do tend to reverse
    — but only 2.41bps gross, far below 8bps cost gate. Same structural ceiling.
    The mean reversion is real but the magnitude is too small to be tradeable at 5m scale."
  source: Theory-first. Low-volume large bar fade as mean reversion anchor for
    volume_state family. Tests whether thin-book artifact moves reliably reverse.

- id: H159
  family: volume_state
  class: tradeable_hypothesis
  description: VS-1 odd-day subsample robustness check. Same signal as H145 (high-volume
    ETH slope flip) but evaluated only on odd calendar days.
  rationale: If VS-1 is real, it should hold across arbitrary temporal subsamples.
    An odd/even split gives two non-overlapping half-datasets. Both must pass gross
    gate for the signal to be considered robust rather than a lucky subset.
  WHY: Robustness check for H145 (VS-1 anchor). Pre-committed before seeing results.
  RULES:
  - Same as H145 (eth_slope_sign_1h flip + volume_btc_pct >= 0.80 + dedup gap = 8).
  - Evaluate only trades occurring on odd calendar days (dt.day % 2 == 1).
  entry_rules:
  - Identical to H145. Day filter applied at evaluation, not signal generation.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      volume_btc_pct_min: 0.80
      subsample: odd_days
  allowed_variants: []
  status: PASS
  signal_group: VS-1
  decision_date: "2026-02-24"
  decision_notes: "PASS — VS-1 robustness (odd-day subsample). n=65, gross=24.83bps
    P>0=1.000, WF+ 12/17, bps8_P>0=0.995. Signal holds on odd days alone — confirms
    VS-1 is not a temporal artifact concentrated in specific days."
  source: Robustness check for VS-1 (H145). Pre-committed odd-day subsample test.

- id: H160
  family: volume_state
  class: tradeable_hypothesis
  description: VS-1 even-day subsample robustness check. Same signal as H145 (high-volume
    ETH slope flip) but evaluated only on even calendar days.
  rationale: Symmetric to H159. Together H159+H160 confirm VS-1 is stable across
    arbitrary time partitions.
  WHY: Robustness check for H145 (VS-1 anchor). Pre-committed before seeing results.
  RULES:
  - Same as H145. Evaluate only trades occurring on even calendar days (dt.day % 2 == 0).
  entry_rules:
  - Identical to H145. Day filter applied at evaluation, not signal generation.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      volume_btc_pct_min: 0.80
      subsample: even_days
  allowed_variants: []
  status: BORDERLINE
  signal_group: VS-1
  decision_date: "2026-02-24"
  decision_notes: "BORDERLINE — VS-1 robustness (even-day subsample). n=71, gross=27.43bps
    P>0=1.000, WF+ 12/17, bps8_P>0=0.999. Passes bps8 with P>0=0.999 but WF fold
    count (12/17 = 71%) is marginal. Together with H159 (odd-day PASS), the signal
    clearly holds across both halves — BORDERLINE here is a WF count artifact of
    fewer trades per fold after day-filtering."
  source: Robustness check for VS-1 (H145). Pre-committed even-day subsample test.

- id: H161
  family: volume_state
  class: tradeable_hypothesis
  description: VS-1 with 1-bar execution lag. Signal fires at slope flip bar, but
    entry is on the NEXT bar's close (5 min later). Tests execution realism — if the
    signal requires entering on the same bar it fires, it may be look-ahead.
  rationale: CA-1 (H77) showed the signal survives next-bar execution. VS-1 should
    also be tested with a 1-bar lag because real execution at the exact flip bar close
    may be unrealistic during fast moves (which high-volume flips often are).
  WHY: Robustness check for H145. If 1-bar lag destroys the edge, the signal depends
    on unrealistic same-bar execution. If it survives, execution is realistic.
  RULES:
  - Same signal generation as H145. Enter on close[t+1] instead of close[t].
  - Horizon counts from t+1.
  entry_rules:
  - Same as H145. Entry shifted by 1 bar (next close after signal).
  exit_rules:
  - Hold for fixed 8 bars from entry bar. No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      volume_btc_pct_min: 0.80
      execution_lag_bars: 1
  allowed_variants: []
  status: PASS
  signal_group: VS-1
  decision_date: "2026-02-24"
  decision_notes: "PASS — VS-1 robustness (1-bar execution lag). n=136, gross=25.84bps
    P>0=1.000, WF+ 16/18, bps8_P>0=1.000. Edge is essentially unchanged from H145
    (26.19bps). Real-world execution at next bar close is viable. No look-ahead
    dependence. This is critical for deployment realism."
  source: Robustness check for VS-1 (H145). Execution lag test to confirm signal
    does not depend on same-bar entry.

- id: H162
  family: volume_state
  class: tradeable_hypothesis
  description: VS-1 with looser volume threshold (p75). Tests threshold sensitivity —
    does the VS-1 edge survive with slightly lower volume requirements?
  rationale: H145 uses volume_btc_pct >= 0.80. p75 includes more trades. If the edge
    is robust, it should hold at p75 (more trades, possibly lower per-trade return).
    A significant drop in edge at p75 suggests the high-volume bar requirement is not
    a robust filter but curve-fitted to p80.
  WHY: Threshold sensitivity check for VS-1. p80 was pre-committed. p75 adds 15th
    to 20th percentile volume trades. Looser threshold means more trades but may
    dilute edge.
  RULES:
  - eth_slope_sign_1h flip + volume_btc_pct >= 0.75 (instead of 0.80).
  - Otherwise identical to H145.
  entry_rules:
  - Long/Short when eth_slope flips AND volume_btc_pct >= 0.75.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      volume_btc_pct_min: 0.75
  allowed_variants: []
  status: PASS
  signal_group: VS-1
  decision_date: "2026-02-24"
  decision_notes: "PASS — VS-1 threshold sensitivity (p75 volume gate). n=164, 0.5/day,
    gross=26.58bps P>0=1.000, WF+ 16/18, bps8_P>0=1.000. More trades than H145 (164
    vs 136) with essentially identical edge (26.58 vs 26.19bps). Edge is NOT sensitive
    to p80 vs p75 threshold — indicates robust volume mechanism not curve-fitted to p80."
  source: Robustness check for VS-1 (H145). Volume threshold sensitivity test
    (looser threshold p75 vs. anchor p80).

- id: H163
  family: volume_state
  class: tradeable_hypothesis
  description: VS-1 with tighter volume threshold (p85). Tests whether requiring
    even higher volume improves per-trade edge at the cost of frequency.
  rationale: H145 uses volume_btc_pct >= 0.80. p85 is stricter — only the highest-volume
    slope flips. If the mechanism is real, tighter volume filter should maintain or
    increase per-trade edge with fewer but higher-quality trades.
  WHY: Threshold sensitivity check for VS-1. p85 reduces trades but selects the most
    extreme volume events. Tighter threshold means fewer trades but potentially higher
    per-trade edge (or too few for significance).
  RULES:
  - eth_slope_sign_1h flip + volume_btc_pct >= 0.85 (instead of 0.80).
  - Otherwise identical to H145.
  entry_rules:
  - Long/Short when eth_slope flips AND volume_btc_pct >= 0.85.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      volume_btc_pct_min: 0.85
  allowed_variants: []
  status: PASS
  signal_group: VS-1
  decision_date: "2026-02-24"
  decision_notes: "PASS — VS-1 threshold sensitivity (p85 volume gate). n=104, 0.3/day,
    gross=32.23bps P>0=1.000, WF+ 16/18, bps8_P>0=1.000. Highest per-trade edge of
    any VS test: 32bps vs 26bps at p80. Tighter volume filter selects higher-conviction
    slope flips. This is consistent with the volume mechanism being real — the very
    highest-volume flips have the most reliable follow-through."
  source: Robustness check for VS-1 (H145). Volume threshold sensitivity test
    (tighter threshold p85 vs. anchor p80).

- id: H164
  family: volume_state
  class: tradeable_hypothesis
  description: VS-1 session-gated to EU/US overlap (08:00-16:00 UTC). High-volume
    ETH slope flip restricted to the highest-liquidity trading session.
  rationale: CA-1 showed its strongest variant (H84) is session-gated to 08-16 UTC
    with +0.38% vs +0.34% unfiltered. The same mechanism should apply to VS-1 — the
    EU/US overlap session has deeper order books, more institutional participation,
    and slope flips on high volume in this window should have the highest conviction.
  WHY: Test whether 08-16 UTC session restriction improves VS-1 per-trade edge, as
    it did for CA-1. Pre-committed before seeing results.
  RULES:
  - Same as H145 (eth_slope flip + volume_btc_pct >= 0.80 + dedup gap = 8).
  - Evaluate only trades occurring between 08:00 and 16:00 UTC.
  entry_rules:
  - Same as H145. Session filter applied at evaluation.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      volume_btc_pct_min: 0.80
      session_utc: "08-16"
  allowed_variants: []
  status: FAIL
  decision_date: "2026-02-24"
  decision_notes: "FAIL. n=54, 0.1/day, gross=25.08bps P>0=0.999, WF+ 10/17 (59%),
    bps8_P>0=0.986. The bps8 bootstrap is strong but WF fold pass count (10/17) is
    below the PASS threshold. Unlike CA-1 where H84 (08-16) was the strongest session,
    VS-1 in EU/US hours does not improve over all-hours. The EU/US session may have
    more contested high-volume slope flips (more participants fading momentum)."
  source: VS family expansion. Session-gated VS-1 (EU/US overlap). Mirrors H84 for CA-1.

- id: H165
  family: volume_state
  class: tradeable_hypothesis
  description: VS-1 session-gated to Asia session (00:00-08:00 UTC). High-volume
    ETH slope flip restricted to the Asian trading session.
  rationale: CA-1 showed positive but lower edge in Asia session (H83, +0.27%).
    Testing whether VS-1 shows similar session-specific behavior. Lower liquidity
    in Asia means high-volume bars are even more selective — fewer but potentially
    more meaningful flips.
  WHY: Complete the session breakdown for VS-1 (H164=EU/US, H165=Asia, H166=US only).
  RULES:
  - Same as H145. Evaluate only trades 00:00-08:00 UTC.
  entry_rules:
  - Same as H145. Session filter applied at evaluation.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      volume_btc_pct_min: 0.80
      session_utc: "00-08"
  allowed_variants: []
  status: INCONCLUSIVE
  decision_date: "2026-02-24"
  decision_notes: "INCONCLUSIVE (n=15, too few trades). gross=15.86bps P>0=0.966,
    WF+ 4/10, bps8_P>0=0.798. High-volume ETH slope flips in Asian session are very
    rare (0.04/day). Not enough observations to draw conclusions."
  source: VS family expansion. Session-gated VS-1 (Asia). Mirrors H83 for CA-1.

- id: H166
  family: volume_state
  class: tradeable_hypothesis
  description: VS-1 session-gated to US session (16:00-24:00 UTC). High-volume
    ETH slope flip restricted to the US trading hours.
  rationale: CA-1 showed strong edge in US session (H85, +0.36%). US session has
    high retail and momentum-trading activity; high-volume flips in this window
    may benefit from trend-following amplification.
  WHY: Complete the session breakdown for VS-1. Pre-committed before seeing results.
  RULES:
  - Same as H145. Evaluate only trades 16:00-24:00 UTC.
  entry_rules:
  - Same as H145. Session filter applied at evaluation.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      volume_btc_pct_min: 0.80
      session_utc: "16-24"
  allowed_variants: []
  status: BORDERLINE
  decision_date: "2026-02-24"
  decision_notes: "BORDERLINE. n=67, 0.2/day, gross=29.40bps P>0=1.000, WF+ 13/18
    (72%), bps8_P>0=1.000. Strong bps8 bootstrap but WF fold count marginal. US session
    (16-24 UTC) shows the highest per-trade edge of the session variants (29.40bps),
    but with too few trades per fold to confirm reliably. Worth revisiting with more data."
  source: VS family expansion. Session-gated VS-1 (US). Mirrors H85 for CA-1.

- id: H167
  family: volume_state
  class: tradeable_hypothesis
  description: VS-2 — high-volume ETH slope flip with 12-bar (60 min) hold instead
    of 8-bar (40 min). Tests whether volume-validated slope flips sustain their
    directional momentum longer than unfiltered flips.
  rationale: High-volume slope flips signal participation by larger players. Large
    players tend to hold positions longer than retail, so the momentum effect may
    persist for 60 minutes rather than 40. CA-1 with h=6 (H60/CA-3) vs h=8 (H65/CA-1)
    shows diminishing returns at shorter horizon. VS-1 may show the opposite — longer
    horizons work better when the flip is backed by real volume.
  WHY: Test whether VS-1 edge increases, decreases, or is stable at h=12. Anchor
    for VS-2 (volume_state, longer horizon). Pre-committed before seeing results.
  RULES:
  - eth_slope_sign_1h flip + volume_btc_pct >= 0.80. Hold for 12 bars (60 min).
  - Event-triggered with dedup gap = 12.
  entry_rules:
  - Same signal as H145. Hold extended to 12 bars.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      volume_btc_pct_min: 0.80
  allowed_variants: []
  status: PASS
  signal_group: VS-2
  decision_date: "2026-02-24"
  decision_notes: "PASS — VS-2 ANCHOR. n=136, 0.4/day, gross=38.63bps P>0=1.000,
    WF+ 17/18, bps8_P>0=1.000. Strongest VS anchor yet — h=12 gives 38bps vs 26bps
    at h=8 for VS-1. High-volume momentum persists for 60 min, not just 40. All 5
    robustness checks pass (H169-H173) with 34-45bps range. VS-2 at p85 (H173) gives
    45bps — the best single VS variant."
  source: VS family expansion. Longer horizon variant. VS-2 anchor candidate.
    Justification for h=12 pre-committed.

- id: H168
  family: volume_state
  class: tradeable_hypothesis
  description: VS-1 with ETH volume gate instead of BTC volume. High ETH volume
    during ETH slope flip — a self-referential volume confirmation.
  rationale: H145 uses BTC volume as the volume gate — BTC is the dominant asset
    and sets the floor for overall crypto market activity. ETH volume is more specific
    to the ETH market and may be a purer signal for ETH slope changes. When ETH itself
    shows high volume at the moment its slope flips, the move is being driven by
    genuine ETH-specific demand (not just BTC spillover). Hypothesis is that ETH
    volume gate is at least as strong as BTC volume gate for ETH slope signals.
  WHY: Test whether ETH volume is equivalent, better, or worse than BTC volume as
    the gate for VS-1. These two are correlated but not identical — ETH-specific
    volume spikes may occur independently of BTC volume. Pre-committed before results.
  RULES:
  - eth_slope_sign_1h flip + volume_eth_pct >= 0.80 (ETH volume, not BTC).
  - Event-triggered with dedup gap = 8.
  entry_rules:
  - Long/Short when eth_slope flips AND volume_eth_pct >= 0.80.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_eth_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      volume_eth_pct_min: 0.80
  allowed_variants: []
  status: BORDERLINE
  decision_date: "2026-02-24"
  decision_notes: "BORDERLINE. n=117, 0.3/day, gross=24.46bps P>0=1.000, WF+ 14/18
    (78%), bps8_P>0=0.999. ETH volume gate gives slightly lower edge than BTC volume
    gate (H145: 26.19bps vs H168: 24.46bps). Real signal but WF fold count just below
    PASS threshold. ETH and BTC volumes are correlated (~0.8), so the ETH gate selects
    similar but slightly different events. BTC volume appears to be the better gate."
  source: VS family expansion. ETH volume gate instead of BTC. Tests whether the
    asset-specific volume provides equivalent or stronger confirmation for ETH signals.

- id: H169
  family: volume_state
  class: tradeable_hypothesis
  description: VS-2 odd-day subsample robustness check. Same signal as H167 (h=12
    high-volume ETH slope flip) evaluated only on odd calendar days.
  rationale: VS-2 (H167) produced 38.63bps PASS. Robustness check mirrors H159 for VS-1.
  WHY: Confirm VS-2 holds on arbitrary temporal subsamples. Pre-committed.
  RULES:
  - Same as H167 (h=12, vol_btc_pct >= 0.80). Odd days only (dt.day % 2 == 1).
  entry_rules:
  - Identical to H167. Day filter applied at evaluation.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      volume_btc_pct_min: 0.80
      subsample: odd_days
  allowed_variants: []
  status: PASS
  signal_group: VS-2
  decision_date: "2026-02-24"
  decision_notes: "PASS — VS-2 robustness (odd-day). n=65, gross=36.35bps P>0=1.000,
    WF+ 12/17, bps8_P>0=1.000. Signal holds strongly on odd days alone."
  source: Robustness check for VS-2 (H167). Odd-day subsample.

- id: H170
  family: volume_state
  class: tradeable_hypothesis
  description: VS-2 even-day subsample robustness check. Same signal as H167 (h=12)
    evaluated only on even calendar days.
  rationale: Symmetric to H169. Together H169+H170 confirm VS-2 is stable across
    arbitrary time partitions.
  WHY: Confirm VS-2 holds on even-day subsample. Pre-committed.
  RULES:
  - Same as H167 (h=12, vol_btc_pct >= 0.80). Even days only (dt.day % 2 == 0).
  entry_rules:
  - Identical to H167. Day filter applied at evaluation.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      volume_btc_pct_min: 0.80
      subsample: even_days
  allowed_variants: []
  status: PASS
  signal_group: VS-2
  decision_date: "2026-02-24"
  decision_notes: "PASS — VS-2 robustness (even-day). n=71, gross=40.72bps P>0=1.000,
    WF+ 13/17, bps8_P>0=1.000. Even higher than anchor on even days (40.72 vs 38.63)."
  source: Robustness check for VS-2 (H167). Even-day subsample.

- id: H171
  family: volume_state
  class: tradeable_hypothesis
  description: VS-2 with 1-bar execution lag. Signal fires at slope flip bar, entry
    on the NEXT bar close. Tests execution realism for the 12-bar hold variant.
  rationale: VS-1 survived 1-bar lag (H161). VS-2 should be tested independently
    because the longer 12-bar hold may have different sensitivity to entry timing.
  WHY: Execution realism check for VS-2. Pre-committed before seeing results.
  RULES:
  - Same signal as H167 (h=12, vol >= p80). Enter on close[t+1].
  entry_rules:
  - Same as H167. Entry shifted by 1 bar.
  exit_rules:
  - Hold for fixed 12 bars from entry bar. No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      volume_btc_pct_min: 0.80
      execution_lag_bars: 1
  allowed_variants: []
  status: PASS
  signal_group: VS-2
  decision_date: "2026-02-24"
  decision_notes: "PASS — VS-2 robustness (1-bar lag). n=136, gross=34.94bps P>0=1.000,
    WF+ 17/18, bps8_P>0=1.000. 1-bar lag costs ~3.7bps (38.63→34.94) but still
    comfortably passes. VS-2 execution at next bar close is viable."
  source: Robustness check for VS-2 (H167). Execution lag test.

- id: H172
  family: volume_state
  class: tradeable_hypothesis
  description: VS-2 with p75 volume threshold (12-bar hold). Tests threshold
    sensitivity for the longer-horizon variant.
  rationale: VS-1 at p75 (H162) showed same edge as p80 — not curve-fitted. Testing
    whether VS-2 shows the same stability at p75. With h=12, p75 adds more trades
    while potentially diluting from high-quality p80+ events.
  WHY: Threshold sensitivity check for VS-2. Pre-committed.
  RULES:
  - eth_slope_sign_1h flip + volume_btc_pct >= 0.75, hold 12 bars.
  entry_rules:
  - Long/Short when eth_slope flips AND volume_btc_pct >= 0.75.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      volume_btc_pct_min: 0.75
  allowed_variants: []
  status: PASS
  signal_group: VS-2
  decision_date: "2026-02-24"
  decision_notes: "PASS — VS-2 robustness (p75 vol). n=164, 0.5/day, gross=39.88bps
    P>0=1.000, WF+ 17/18, bps8_P>0=1.000. More trades (164 vs 136) with same edge.
    Not curve-fitted to p80."
  source: Robustness check for VS-2 (H167). Volume threshold sensitivity (p75).

- id: H173
  family: volume_state
  class: tradeable_hypothesis
  description: VS-2 with p85 volume threshold (12-bar hold). Tests whether
    highest-volume-only selection improves per-trade edge at h=12.
  rationale: VS-1 at p85 (H163) gave 32bps vs 26bps at p80 — tighter is better.
    Testing whether this pattern holds at h=12. If p85 at h=12 gives even higher
    edge, p85+h=12 may be the best VS variant overall.
  WHY: Threshold sensitivity check for VS-2. Pre-committed.
  RULES:
  - eth_slope_sign_1h flip + volume_btc_pct >= 0.85, hold 12 bars.
  entry_rules:
  - Long/Short when eth_slope flips AND volume_btc_pct >= 0.85.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      volume_btc_pct_min: 0.85
  allowed_variants: []
  status: PASS
  signal_group: VS-2
  decision_date: "2026-02-24"
  decision_notes: "PASS — VS-2 robustness (p85 vol). n=104, 0.3/day, gross=45.29bps
    P>0=1.000, WF+ 17/18, bps8_P>0=1.000. Best single VS variant: 45bps at h=12+p85.
    Tighter volume filter selects highest-conviction flips which sustain for 60 min."
  source: Robustness check for VS-2 (H167). Volume threshold sensitivity (p85).

- id: H174
  family: funding_regime
  class: tradeable_hypothesis
  description: H141 revisit — loosen extreme funding threshold from p85 to p80 to get
    more trades. Same mechanism — extreme crowded longs + ETH slope flip DOWN → SHORT.
    Pre-committed horizon h=8 (direct comparison to H141).
  rationale: H141 (p85 threshold) produced only n=16 trades in 365d — insufficient
    to confirm or reject. The mechanism is sound (crowded longs + reversal = amplified
    DOWN move). Loosening to p80 should roughly double trade count while preserving
    the core economics. Pre-committed horizon h=8 to match H141 exactly.
  WHY: Resolve H141 INCONCLUSIVE by increasing sample size. p80 is the standard high-percentile
    threshold (matching VS-1 volume gate approach). If this passes, h=12 variant (H175)
    is the natural next test.
  RULES:
  - Enter SHORT when funding_btc_pct >= 0.80 AND eth_slope_sign_1h flips from +1 to -1.
  - Event-triggered (dedup gap = horizon bars). Signal = -1.0 (SHORT only).
  - Horizon = 8 bars (40 min). Pre-committed.
  FAILURE_CONDITIONS:
  - p80 is a less extreme filter than p85 — may dilute the crowding signal with
    non-extreme funding bars where forced unwind is weaker.
  - If gross is ~17bps (same as H141) it means p80 adds marginal trades with same
    quality. If gross drops significantly, p80 includes too many noise events.
  entry_rules:
  - Short when funding_btc_pct >= 0.80 AND eth_slope flips from +1 to -1.
  - Signal = -1.0. Event-triggered with dedup gap = horizon.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - funding_btc_pct from rc.funding_rates via Hyperliquid
  - eth_slope_sign_1h from load_frame() 1h feature set
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      funding_btc_pct_min: 0.80
      slope_direction: down_flip
      direction: short
  allowed_variants: []
  status: INCONCLUSIVE
  decision_date: "2026-02-24"
  decision_notes: "INCONCLUSIVE (n=25, too few trades). runner gross=20.10bps P>0=0.983,
    WF+ gross=9.41bps P>0=0.895, WF bps8=1.41bps P>0=0.579. WF fold detail: 9/15
    folds have ZERO trades, only 3/15 folds positive (20% WF+). p80 added ~9 trades
    vs H141 (25 vs 16) and confirmed the gross signal is real (P>0 improved from
    0.863 to 0.983), but still too few events per year for reliable WF statistics.
    The FR (extreme funding + slope flip) mechanism fires ~1x/month — insufficient
    for validation on 365d data. Needs 2+ years of data or a looser threshold (p75)
    or multi-asset combination (BTC+ETH funding both extreme). Do NOT iterate further
    without new data or fundamentally different approach."
  source: H141 revisit. Anchor test for FR family at p80 threshold.

- id: H175
  family: funding_regime
  class: tradeable_hypothesis
  description: H174 at longer horizon — extreme crowded longs (p80) + ETH slope flip
    DOWN → SHORT, held for 12 bars (60 min). Tests whether the forced-unwind momentum
    sustains for 60 min as VS-2 showed for volume-gated flips.
  rationale: VS-2 (volume-gated slope flip, h=12) outperformed VS-1 (h=8) by ~12bps.
    The same principle may apply here — when a funding-crowded market reverses, the
    forced unwind takes 60+ min as leveraged longs are systematically liquidated.
    Pre-committed horizon h=12 before running, justified by VS-2 evidence.
  WHY: If H174 (p80, h=8) produces enough trades and a positive gross signal, H175
    tests whether extending the hold from 40 to 60 min captures more of the forced-
    unwind move. Batch with H174 since both are pre-committed.
  RULES:
  - Enter SHORT when funding_btc_pct >= 0.80 AND eth_slope_sign_1h flips from +1 to -1.
  - Event-triggered (dedup gap = 12 bars). Signal = -1.0 (SHORT only).
  - Horizon = 12 bars (60 min). Pre-committed (motivated by VS-2 pattern).
  FAILURE_CONDITIONS:
  - Forced-unwind momentum from funding crowding may be faster than volume momentum
    (funding liquidations concentrate in first few bars, not sustained for 60 min).
  - Lower n than CA-1 means more variance — h=12 may not meaningfully differ from h=8.
  entry_rules:
  - Short when funding_btc_pct >= 0.80 AND eth_slope flips from +1 to -1.
  - Signal = -1.0. Event-triggered with dedup gap = horizon.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - funding_btc_pct from rc.funding_rates via Hyperliquid
  - eth_slope_sign_1h from load_frame() 1h feature set
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      funding_btc_pct_min: 0.80
      slope_direction: down_flip
      direction: short
  allowed_variants: []
  status: FAIL
  decision_date: "2026-02-24"
  decision_notes: "FAIL (WF collapses at h=12). n=25, runner gross=20.23bps P>0=0.932,
    WF gross=0.20bps P>0=0.531, WF bps8=-7.80bps P>0=0.238. Unlike VS-2 (where h=12
    significantly improved edge vs h=8), the forced-unwind mechanism from funding
    crowding does NOT sustain at 60 min. The momentum is front-loaded — if it exists,
    it happens in the first 40 min (h=8), not later. h=12 adds noise and loses WF
    consistency. Confirmed: this mechanism is fast (not sustained). Do not test h>8
    for FR signals."
  source: H175 = H174 at h=12. Pre-committed horizon motivated by VS-2 pattern.

# ─────────────────────────────────────────────────────────────────────────────
# OI + LIQUIDATIONS FAMILY (H176-H180) — Gate.io data, unlocked 2026-02-24
# ─────────────────────────────────────────────────────────────────────────────

- id: H176
  family: oi_liq
  class: tradeable_hypothesis
  description: OI-gated ETH slope flip. Same structure as VS-1 (volume-gated slope
    flip) but using BTC open interest percentile as the quality filter instead of
    volume. High OI means many leveraged positions exist — a slope flip when OI is
    elevated forces more of those positions to close, amplifying the move.
  rationale: VS-1 proved that filtering slope flips by a quality signal (volume) selects
    higher-conviction moves. OI is an independent quality filter — it measures how many
    leveraged positions exist, not how much volume is trading. High OI + slope flip
    should force OI reduction (deleveraging), creating directional pressure beyond
    the slope change alone. Anchor test for OI family.
  WHY: Test whether OI is an independent signal amplifier for CA slope flips, similar
    to how volume works in VS-1. If both OI and volume independently amplify slope flips,
    they may also compound (future hypothesis — OI + volume gate together).
  RULES:
  - Enter when eth_slope_sign_1h flips (from +1 to -1 or -1 to +1) AND oi_btc_pct >= 0.80.
  - Direction = eth_slope_sign_1h. Event-triggered. Dedup gap = horizon.
  - Horizon = 8 bars (40 min). Pre-committed (VS-1 baseline for direct comparison).
  FAILURE_CONDITIONS:
  - OI data from Gate.io may not perfectly represent global OI.
  - OI moves slowly — percentile threshold may be persistently high or low for weeks.
  - If OI is always high in bull markets, the gate may not be selective enough.
  entry_rules:
  - Long/Short when eth_slope flips AND oi_btc_pct >= 0.80.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h from load_frame()
  - oi_btc_pct from rc.open_interest via Gate.io (loaded by load_frame())
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      oi_btc_pct_min: 0.80
  allowed_variants: []
  status: PASS
  decision_date: "2026-02-24"
  decision_notes: "PASS — n=217, 0.65/day, gross=16.4bps P>0=1.000, WF+ gross 15.1bps 14/18 folds,
    WF bps8=7.1bps P>0=0.995 folds 13/18. OI gate independently amplifies slope flips.
    Borderline bps8 fold count (13/18) — needs robustness checks before permanent shortcode.
    Treat as OI-1 candidate. No signal_group assigned until robustness confirmed."
  source: Theory-first. OI-gated slope flip, analogous to VS-1 volume gate.

- id: H177
  family: oi_liq
  class: tradeable_hypothesis
  description: Long liquidation cascade SHORT continuation. When extreme long-side
    liquidations occurred in the prior 1h window, go SHORT for the next 40 minutes.
    Theory is that liquidation cascades have follow-through — one round of forced
    selling triggers more margin calls.
  rationale: Forced liquidations create a specific price dynamic — they front-run
    normal sellers because they are mechanical (margin system forces them regardless
    of price). When the prior 1h had extreme long liquidations (top 10% historically),
    the market is in a deleveraging event. These events often continue in three phases —
    (1) the initial liq triggers more stops, (2) remaining longs add more margin but are
    eventually overwhelmed, (3) price discovery overshoots down.
  WHY: First direct liquidation signal test. Liquidation data is new to the pipeline.
    Onset-triggered on the first bar after an extreme liq window becomes observable.
    SHORT only — long liquidations are inherently bearish.
  RULES:
  - Enter SHORT on onset of long_liq_btc_pct >= 0.90 (first 5m bar when condition
    becomes true, i.e. when a fresh hourly liq value exceeds p90).
  - Dedup gap = horizon. Horizon = 8 bars (40 min). Pre-committed.
  FAILURE_CONDITIONS:
  - Liq data is 1h resolution carry-forwarded to 5m. Onset fires once per 1h window.
  - By the time we observe extreme liq, most of the cascade may have already happened.
  - If the liq event is followed by a bounce (mean reversion after forced selling),
    the SHORT will lose money.
  entry_rules:
  - Short on onset of long_liq_btc_pct >= 0.90.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - long_liq_btc_pct from rc.liquidations via Gate.io
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      long_liq_btc_pct_min: 0.90
      direction: short
  allowed_variants: []
  signal_group: "LQ-1"
  status: PASS
  decision_date: "2026-02-24"
  decision_notes: "PASS (ANCHOR) — n=1437, 4.31/day, gross=20.0bps P>0=1.000, WF+ gross 18/18 folds,
    WF bps8=11.7bps P>0=1.000 folds 16/18. Long liquidation cascade drives SHORT continuation.
    Assigned LQ-1 as liquidation family anchor signal."
  source: Theory-first. First direct liquidation cascade signal.

- id: H178
  family: oi_liq
  class: tradeable_hypothesis
  description: Short liquidation squeeze LONG continuation. When extreme short-side
    liquidations occurred in the prior 1h window, go LONG for the next 40 minutes.
    Forced short covering creates buying pressure that tends to continue.
  rationale: Symmetric to H177 but for short squeezes. When shorts are forced to
    cover at scale, the buying pressure can propagate in three phases —
    (1) initial forced covering, (2) more shorts capitulate voluntarily, (3) longs pile on. The squeeze dynamic
    is often more explosive than a cascade because shorts must buy to close.
  WHY: Symmetric test to H177. Both are anchor candidates for the liquidation family.
    If squeezes (H178) have more edge than cascades (H177), it informs direction for
    future combined signals.
  RULES:
  - Enter LONG on onset of short_liq_btc_pct >= 0.90 (first 5m bar after fresh
    extreme short liquidation hour).
  - Dedup gap = horizon. Horizon = 8 bars (40 min). Pre-committed.
  FAILURE_CONDITIONS:
  - Same timing issue as H177 — by the time liq is observable, move may be done.
  - Squeezes may reverse fast if the underlying bearish pressure reasserts.
  entry_rules:
  - Long on onset of short_liq_btc_pct >= 0.90.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - short_liq_btc_pct from rc.liquidations via Gate.io
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      short_liq_btc_pct_min: 0.90
      direction: long
  allowed_variants: []
  signal_group: "LQ-2"
  status: PASS
  decision_date: "2026-02-24"
  decision_notes: "PASS (ANCHOR) — n=1474, 4.44/day, gross=16.0bps P>0=1.000, WF+ gross 18/18 folds,
    WF bps8=6.8bps P>0=1.000 folds 17/18. Short squeeze buying pressure sustains for 40 min.
    Assigned LQ-2 as symmetric anchor to LQ-1. Short squeeze mirror of cascade."
  source: Theory-first. Symmetric to H177, short squeeze continuation.

- id: H179
  family: oi_liq
  class: tradeable_hypothesis
  description: Liquidation-gated ETH slope flip SHORT. Long liquidations elevated
    (>= p70 in prior 1h) AND ETH slope flips down. Combines the cascade signal with
    the CA slope flip confirmation — both point bearish simultaneously.
  rationale: H177 is a pure cascade signal (high liq → SHORT). CA-1 is a pure
    momentum signal (slope flip → direction). H179 combines them — when the recent
    1h had elevated long liquidations AND momentum just turned negative, the bearish
    case is confirmed from two independent mechanisms. The liquidation context means
    there is deleveraging pressure behind the slope change, not just a noise flip.
    Uses p70 (not p90) to allow more events than H177 while maintaining quality.
  WHY: Liq + slope flip is the OI/liq analog to the funding + slope flip (H141/H174)
    that showed real gross alpha. Liquidation data is more timely and actionable than
    funding rate data. Pre-commit h=8 (consistent with FR slope flip experiments).
  RULES:
  - Enter SHORT when eth_slope_sign_1h flips from +1 to -1 AND long_liq_btc_pct >= 0.70.
  - Event-triggered on slope flip (liq is a gate, not the trigger).
  - Dedup gap = horizon. Horizon = 8 bars (40 min). Pre-committed.
  FAILURE_CONDITIONS:
  - p70 liq filter may not be selective enough (many bars have moderate liquidations).
  - SHORT-only signal — only captures bearish moves.
  - If liq is elevated for many hours, multiple slope flips may be gated in.
  entry_rules:
  - Short when eth_slope flips from +1 to -1 AND long_liq_btc_pct >= 0.70.
  exit_rules:
  - Hold for fixed 8 bars (40 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h from load_frame()
  - long_liq_btc_pct from rc.liquidations via Gate.io
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      long_liq_btc_pct_min: 0.70
      slope_direction: down_flip
      direction: short
  allowed_variants: []
  signal_group: "LQ-3"
  status: PASS
  decision_date: "2026-02-24"
  decision_notes: "PASS — n=166, 0.51/day, gross=31.0bps P>0=1.000, WF+ gross 16/17 folds,
    WF bps8=18.7bps P>0=1.000 folds 14/17. Liq gate elevates edge of slope flip.
    Combines cascade context (LQ family) with CA momentum confirmation. Assigned LQ-3."
  source: Theory-first. Liq-gated slope flip, analogous to FR-gated (H174).

- id: H180
  family: oi_liq
  class: tradeable_hypothesis
  description: VS-2 with liquidation confirmation. VS-2 (volume p80 + slope flip,
    h=12) filtered to only fire when total liquidations are also elevated (>= p70).
    Tests whether the highest-quality VS flips — those backed by both volume expansion
    AND forced liquidation activity — have even stronger edge than VS-2 alone.
  rationale: VS-2 (H167) produces 38bps on average, and VS-2 at p85 (H173) produces
    45bps. The core insight is that not all slope flips with volume are equal — some
    are driven by discretionary large buyers/sellers, others by forced liquidations.
    When BOTH occur simultaneously, the move has two independent engines —
    (1) active capital flow (volume) and (2) mechanical forced liquidation (liq). The expected
    edge of the intersection is higher than either alone, at the cost of fewer trades.
  WHY: VS + liq is the natural multi-signal combination given both VS and liq data
    are now in the pipeline. Pre-commit h=12 (inheriting VS-2's optimal horizon).
    Pre-commit total_liq p70 (not p90 — need enough intersection with VS-2 signals).
  RULES:
  - Enter when eth_slope flips AND volume_btc_pct >= 0.80 AND total_liq_btc_pct >= 0.70.
  - Direction = eth_slope_sign_1h. Event-triggered. Dedup gap = horizon.
  - Horizon = 12 bars (60 min). Pre-committed (inherits VS-2 optimal horizon).
  FAILURE_CONDITIONS:
  - Triple filter may leave too few trades per WF fold for reliable statistics.
  - Gate.io liq and Coinbase volume measure different things — may not compound.
  - If liq_pct p70 is too common (fires 30% of bars), it adds little selectivity.
  entry_rules:
  - Long/Short when eth_slope flips AND volume_btc_pct >= 0.80 AND total_liq_btc_pct >= 0.70.
  exit_rules:
  - Hold for fixed 12 bars (60 min). No early exit.
  required_inputs:
  - BTC-USD and ETH-USD 5m OHLCV (Coinbase)
  - eth_slope_sign_1h and volume_btc_pct from load_frame()
  - total_liq_btc_pct from rc.liquidations via Gate.io
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      volume_btc_pct_min: 0.80
      total_liq_btc_pct_min: 0.70
  allowed_variants: []
  signal_group: "VS-3"
  status: PASS
  decision_date: "2026-02-24"
  decision_notes: "PASS (NEW ALL-TIME BEST) — n=85, 0.26/day, gross=60.5bps P>0=1.000,
    WF+ gross 16/17 folds, WF bps8=48.0bps P>0=1.000 folds 15/17. VS-2 gated by liq
    confirmation produces the highest per-trade return in all research history.
    Assigned VS-3 (extends VS family with liq confirmation layer)."
  source: VS-2 + liquidation confirmation. Multi-signal combination.

- id: H181
  family: oi_liq
  class: robustness_check
  description: H176 (OI-gated slope flip) odd-day subsample robustness check.
  rationale: OI-1 candidate has borderline WF bps8 fold count (13/18). Split by
    odd/even day to detect day-of-week or systematic bias. If both subsamples pass,
    confidence in H176 increases for permanent OI-1 shortcode assignment.
  WHY: Standard robustness protocol for borderline PASS signals before shortcode.
  RULES:
  - Same signal as H176. Entry filtered to odd calendar days only.
  - Horizon = 8 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      oi_btc_pct_min: 0.80
  allowed_variants: []
  status: BORDERLINE
  decision_date: "2026-02-24"
  decision_notes: "BORDERLINE — n=113, gross=17.1bps P>0=1.000, WF+ 11/18, WF bps8=8.0bps
    P>0=0.987 folds 11/18. Gross edge real but WF bps8 fold count marginal.
    Odd days retain edge but not reliably cost-positive."
  source: Robustness check for H176 (OI-1 candidate).

- id: H182
  family: oi_liq
  class: robustness_check
  description: H176 (OI-gated slope flip) even-day subsample robustness check.
  rationale: Complement to H181. Both halves must hold for OI-1 shortcode to be assigned.
  WHY: Standard robustness protocol.
  RULES:
  - Same signal as H176. Entry filtered to even calendar days only.
  - Horizon = 8 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      oi_btc_pct_min: 0.80
  allowed_variants: []
  status: FAIL
  decision_date: "2026-02-24"
  decision_notes: "FAIL — n=104, gross=15.6bps P>0=1.000 (gross real), but WF bps8=5.1bps
    folds 5/18. Even days collapse at cost. Confirms H176 is NOT ready for OI-1 shortcode
    assignment. The edge is asymmetrically concentrated in odd days."
  source: Robustness check for H176 (OI-1 candidate).

- id: H183
  family: oi_liq
  class: robustness_check
  description: H176 (OI-gated slope flip) 1-bar execution lag robustness check.
  rationale: Validates that the OI-1 edge survives a 1-bar (5 min) delay between
    signal and entry — confirming the signal is exploitable in practice.
  WHY: Standard execution realism check for all new signal anchors.
  RULES:
  - Same signal as H176. Entry at close[t+1] instead of close[t].
  - Horizon = 8 bars from entry bar.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      oi_btc_pct_min: 0.80
  allowed_variants: []
  status: BORDERLINE
  decision_date: "2026-02-24"
  decision_notes: "BORDERLINE — n=217, gross=14.9bps P>0=1.000, WF+ 15/18, WF bps8=5.1bps
    P>0=0.959 folds 9/18. Gross survives lag but cost-net edge is not reliable.
    Combined with H182 FAIL, confirms H176 is cost-constrained. OI-1 shortcode NOT assigned."
  source: Robustness check for H176 (OI-1 candidate).

- id: H184
  family: oi_liq
  class: robustness_check
  description: H177 (LQ-1) 1-bar execution lag robustness check.
  rationale: LQ-1 fires on onset of extreme long liquidation hour. Checks whether
    the cascade follow-through is robust to a 5-minute entry delay.
  WHY: LQ-1 is an ANCHOR signal — execution realism must be confirmed before
    deploying to paper trading.
  RULES:
  - Same signal as H177. Entry at close[t+1]. Direction = SHORT (-1.0). Horizon = 8.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      long_liq_btc_pct_min: 0.90
      direction: short
  allowed_variants: []
  status: PASS
  decision_date: "2026-02-24"
  decision_notes: "PASS — n=1437, gross=18.1bps P>0=1.000, WF+ 18/18, WF bps8=9.9bps
    P>0=1.000 folds 14/18. LQ-1 edge fully survives 1-bar execution lag.
    Drop from H177 (20.0bps) to 18.1bps is small. EXECUTION REALISTIC. Paper trade ready."
  source: Robustness check for H177 (LQ-1 anchor).

- id: H185
  family: oi_liq
  class: robustness_check
  description: H178 (LQ-2) 1-bar execution lag robustness check.
  rationale: LQ-2 fires on onset of extreme short liquidation hour. Checks whether
    the short-squeeze follow-through is robust to a 5-minute entry delay.
  WHY: LQ-2 is an ANCHOR signal — execution realism required before paper trading.
  RULES:
  - Same signal as H178. Entry at close[t+1]. Direction = LONG (+1.0). Horizon = 8.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      short_liq_btc_pct_min: 0.90
      direction: long
  allowed_variants: []
  status: BORDERLINE
  decision_date: "2026-02-24"
  decision_notes: "BORDERLINE — n=1474, gross=13.7bps P>0=1.000, WF+ 18/18, WF bps8=4.7bps
    P>0=0.997 folds 16/18. WF bps8 fold count (16/18) is very strong but WF mean is thin.
    Drop from H178 (16.0bps) to 13.7bps brings WF net mean below 8bps threshold.
    Edge is real and consistent — marginal execution constraint. Treat as deployable with
    awareness that actual fill quality will matter more than for LQ-1."
  source: Robustness check for H178 (LQ-2 anchor).

- id: H186
  family: oi_liq
  class: robustness_check
  description: H179 (LQ-3) 1-bar execution lag robustness check.
  rationale: LQ-3 combines liq context with slope flip. Verifies edge survives
    a 1-bar execution delay.
  WHY: Standard pre-paper-trade robustness check.
  RULES:
  - Same signal as H179. Entry at close[t+1]. Direction = SHORT. Horizon = 8.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      long_liq_btc_pct_min: 0.70
      slope_direction: down_flip
      direction: short
  allowed_variants: []
  status: PASS
  decision_date: "2026-02-24"
  decision_notes: "PASS — n=166, gross=31.3bps P>0=1.000, WF+ 17/17, WF bps8=20.9bps
    P>0=1.000 folds 15/17. LQ-3 edge is extremely robust to execution lag.
    Drop from H179 (31.0bps) is negligible. EXECUTION REALISTIC. Paper trade ready."
  source: Robustness check for H179 (LQ-3).

- id: H187
  family: oi_liq
  class: robustness_check
  description: H180 (VS-3) 1-bar execution lag robustness check.
  rationale: VS-3 fires on slope flip + volume p80 + liq p70. With only 0.26 trades/day
    and n=85, each trade matters. Confirms edge survives a 5-minute entry delay.
  WHY: VS-3 is the all-time best signal (60.5bps) — execution realism check is
    required before paper trading. VS-2 lag (H171) passed at 34.94bps, so VS-3
    likely passes too, but must verify given the triple-gate structure.
  RULES:
  - Same signal as H180. Entry at close[t+1]. Direction = eth_slope_sign_1h. Horizon = 12.
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      volume_btc_pct_min: 0.80
      total_liq_btc_pct_min: 0.70
  allowed_variants: []
  status: PASS
  decision_date: "2026-02-24"
  decision_notes: "PASS — n=85, gross=54.1bps P>0=1.000, WF+ 16/17, WF bps8=45.1bps
    P>0=1.000 folds 15/17. VS-3 edge barely affected by 1-bar lag (60.5 → 54.1bps,
    fold structure identical). EXECUTION REALISTIC. Paper trade ready."
  source: Robustness check for H180 (VS-3).
- id: H198
  family: exit_logic
  class: tradeable_hypothesis
  description: CA-1 + ATR stop loss (1.5× ATR14 from entry price).
  rationale: Test whether a dynamic volatility-scaled stop loss improves CA-1 net
    returns by cutting losses earlier on adverse moves while leaving winning trades
    to run to the 8-bar time exit.
  WHY: CA-1 has strong gross edge (34bps) but fixed-time exit surrenders gains on
    reversals. ATR stop provides volatility-adaptive downside protection.
  RULES:
  - Same entry as CA-1 (ETH slope flip, non-zero sign).
  - Exit early if price moves 1.5 × ATR14 against entry direction.
  - Otherwise hold up to 8 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      atr_stop_mult: 1.5
  allowed_variants: []
  status: active
- id: H199
  family: exit_logic
  class: tradeable_hypothesis
  description: CA-1 + Take profit at +25 basis points.
  rationale: Test whether locking in gains at 25bps improves CA-1 net returns
    by avoiding the give-back that occurs when strong moves reverse before the 8-bar
    time exit.
  WHY: CA-1 mean gross is 34bps but many trades overshoot then retrace. Taking
    profit at 25bps captures the bulk of typical moves without holding through reversals.
  RULES:
  - Same entry as CA-1 (ETH slope flip, non-zero sign).
  - Exit early when unrealized P&L reaches +25bps from entry.
  - Otherwise hold up to 8 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      tp_bps: 25.0
  allowed_variants: []
  status: active
- id: H200
  family: exit_logic
  class: tradeable_hypothesis
  description: CA-1 + Trailing stop (15bps trail from running peak).
  rationale: Test whether a trailing stop improves CA-1 net returns by capturing
    extended moves while cutting losses when the market reverses from the trade peak.
  WHY: CA-1 trades trend ETH slope flips. Extended moves may overshoot; trailing
    stop locks in gains dynamically without capping upside.
  RULES:
  - Same entry as CA-1 (ETH slope flip, non-zero sign).
  - Track running best price since entry. Exit when price pulls back 15bps × entry_px
    from the peak.
  - Otherwise hold up to 8 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      trail_bps: 15.0
  allowed_variants: []
  status: active
- id: H201
  family: exit_logic
  class: tradeable_hypothesis
  description: CA-1 + Combo exit (ATR stop OR take profit, 8-bar max).
  rationale: Test whether combining ATR stop loss with take profit (whichever fires
    first) outperforms either alone by providing both downside protection and gain
    capture.
  WHY: Realistic deployment scenario — cut losers via ATR stop, lock winners via
    TP. Most practical combo for CA-1.
  RULES:
  - Same entry as CA-1 (ETH slope flip, non-zero sign).
  - Exit early on ATR stop (1.5× ATR14) OR take profit (+25bps), whichever fires first.
  - Otherwise hold up to 8 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      atr_stop_mult: 1.5
      tp_bps: 25.0
  allowed_variants: []
  status: active
- id: H202
  family: exit_logic
  class: tradeable_hypothesis
  description: VS-2 + ATR stop loss (1.5× ATR14 from entry price).
  rationale: Test whether a dynamic ATR stop improves VS-2 net returns by cutting
    losses on the minority of trades that reverse hard after the high-volume slope flip.
  WHY: VS-2 has excellent gross (50bps) but 12-bar hold exposes trades to reversal
    risk. ATR stop provides volatility-adaptive downside protection.
  RULES:
  - Same entry as VS-2 (ETH slope flip AND volume_btc_pct ≥ 0.80).
  - Exit early if price moves 1.5 × ATR14 against entry direction.
  - Otherwise hold up to 12 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      atr_stop_mult: 1.5
  allowed_variants: []
  status: active
- id: H203
  family: exit_logic
  class: tradeable_hypothesis
  description: VS-2 + Take profit at +35 basis points.
  rationale: Test whether locking in gains at 35bps improves VS-2 net returns
    by capturing the majority of the expected move before potential give-back.
  WHY: VS-2 mean gross is ~50bps over 12 bars; many trades likely reach 35bps
    well before bar 12. Early lock-in could improve Sharpe.
  RULES:
  - Same entry as VS-2 (ETH slope flip AND volume_btc_pct ≥ 0.80).
  - Exit early when unrealized P&L reaches +35bps from entry.
  - Otherwise hold up to 12 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      tp_bps: 35.0
  allowed_variants: []
  status: active
- id: H204
  family: exit_logic
  class: tradeable_hypothesis
  description: VS-2 + Trailing stop (20bps trail from running peak).
  rationale: Test whether a trailing stop improves VS-2 net returns by locking
    in extended moves while cutting losses on reversals.
  WHY: VS-2 trades high-conviction slope flips backed by volume. A trailing stop
    could capture the full momentum move without capping upside.
  RULES:
  - Same entry as VS-2 (ETH slope flip AND volume_btc_pct ≥ 0.80).
  - Track running best price since entry. Exit when price pulls back 20bps × entry_px
    from the peak.
  - Otherwise hold up to 12 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      trail_bps: 20.0
  allowed_variants: []
  status: active
- id: H205
  family: exit_logic
  class: tradeable_hypothesis
  description: VS-2 + Combo exit (ATR stop OR take profit, 12-bar max).
  rationale: Test whether combining ATR stop and take profit improves VS-2 by
    providing both downside protection and gain capture in a single realistic strategy.
  WHY: Most realistic VS-2 deployment scenario. ATR stop limits drawdown; TP locks
    in gains. The 12-bar window provides full thesis-verification time if neither fires.
  RULES:
  - Same entry as VS-2 (ETH slope flip AND volume_btc_pct ≥ 0.80).
  - Exit early on ATR stop (1.5× ATR14) OR take profit (+35bps), whichever fires first.
  - Otherwise hold up to 12 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      atr_stop_mult: 1.5
      tp_bps: 35.0
  allowed_variants: []
  status: active
- id: H206
  family: exit_logic
  class: tradeable_hypothesis
  description: LQ-1 + Opposite signal exit (exit when long_liq_btc_pct drops below p50).
  rationale: Test whether exiting LQ-1 trades early when the liquidation thesis
    invalidates (long liq percentile drops below median) improves net returns.
  WHY: LQ-1 shorts on long liquidation cascades. If liquidations subside mid-trade
    (liq_pct < p50), the cascade is over — the short thesis is invalidated.
  RULES:
  - Same entry as LQ-1 (onset of long_liq_btc_pct ≥ 0.90, direction=-1).
  - Exit early if long_liq_btc_pct drops below 0.50 during the hold.
  - Otherwise hold up to 8 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      liq_exit_col: long_liq_btc_pct
      liq_exit_threshold: 0.5
  allowed_variants: []
  status: active
- id: H207
  family: exit_logic
  class: tradeable_hypothesis
  description: LQ-1 + ATR stop loss (1.5× ATR14).
  rationale: Test whether ATR-based stops improve LQ-1 returns by cutting losses
    when the cascade fails to produce the expected downward continuation.
  WHY: LQ-1 shorts are high-conviction but occasionally fail when cascades resolve
    quickly. ATR stop limits downside on failed cascade entries.
  RULES:
  - Same entry as LQ-1 (onset of long_liq_btc_pct ≥ 0.90, direction=-1).
  - Exit early if price moves 1.5 × ATR14 against the short direction.
  - Otherwise hold up to 8 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      atr_stop_mult: 1.5
  allowed_variants: []
  status: active
- id: H208
  family: exit_logic
  class: tradeable_hypothesis
  description: LQ-2 + Opposite signal exit (exit when short_liq_btc_pct drops below p50).
  rationale: Test whether exiting LQ-2 trades early when the short liquidation
    thesis invalidates (short liq percentile drops below median) improves net returns.
  WHY: LQ-2 longs on short liquidation cascades. If liquidations subside (short_liq_pct
    < p50), the squeeze is over — the long thesis is invalidated.
  RULES:
  - Same entry as LQ-2 (onset of short_liq_btc_pct ≥ 0.90, direction=+1).
  - Exit early if short_liq_btc_pct drops below 0.50 during the hold.
  - Otherwise hold up to 8 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      liq_exit_col: short_liq_btc_pct
      liq_exit_threshold: 0.5
  allowed_variants: []
  status: active
- id: H209
  family: exit_logic
  class: tradeable_hypothesis
  description: LQ-3 + Combo exit (slope reversal OR long liq thesis invalidation).
  rationale: Test whether exiting LQ-3 when slope flips bullish OR long liquidations
    subside below p50 improves net returns over the fixed 8-bar exit.
  WHY: LQ-3 shorts on slope-flip-down + high long liquidations. Two thesis legs —
    exit when either leg is invalidated (slope recovers or cascade ends).
  RULES:
  - Same entry as LQ-3 (slope flip down AND long_liq_btc_pct ≥ 0.70, direction=-1).
  - Exit early if eth_slope_sign_1h flips positive OR long_liq_btc_pct drops below 0.50.
  - Otherwise hold up to 8 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      liq_exit_col: long_liq_btc_pct
      liq_exit_threshold: 0.5
      slope_exit: true
  allowed_variants: []
  status: active
- id: H210
  family: exit_logic
  class: tradeable_hypothesis
  description: VS-3 + Combo exit (total liq thesis invalidation OR slope reversal).
  rationale: Test whether exiting VS-3 when total liquidations subside OR slope
    reverses improves net returns over the fixed 12-bar exit.
  WHY: VS-3 uses slope + volume + total liquidations for entry. Either leg reversing
    mid-trade invalidates the thesis — exit rather than hold to time.
  RULES:
  - Same entry as VS-3 (slope flip AND vol_btc_pct ≥ 0.80 AND total_liq_btc_pct ≥ 0.70).
  - Exit early if total_liq_btc_pct drops below 0.50 OR eth_slope_sign_1h reverses.
  - Otherwise hold up to 12 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      liq_exit_col: total_liq_btc_pct
      liq_exit_threshold: 0.5
      slope_exit: true
  allowed_variants: []
  status: active
- id: H211
  family: exit_logic
  class: tradeable_hypothesis
  description: CA-1 + Breakeven stop (move stop to entry after +15bps gain).
  rationale: Test whether locking in a free trade once CA-1 is up 15bps improves
    risk-adjusted returns by eliminating the chance of a winner turning into a loser.
  WHY: CA-1 trades often move strongly in favor before reversing. Breakeven stop
    prevents giving back gains on trades that initially went well.
  RULES:
  - Same entry as CA-1 (ETH slope flip, non-zero sign).
  - Once unrealized P&L reaches +15bps, move stop-loss to entry price (breakeven).
  - Exit early if price reverses to entry price after breakeven is triggered.
  - Otherwise hold up to 8 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      breakeven_trigger_bps: 15.0
  allowed_variants: []
  status: active
- id: H212
  family: exit_logic
  class: tradeable_hypothesis
  description: VS-2 + Breakeven stop (move stop to entry after +20bps gain).
  rationale: Test whether locking in a free trade once VS-2 is up 20bps improves
    risk-adjusted returns without significantly reducing trade expectancy.
  WHY: VS-2 trades have high conviction but 12-bar holds can give back early gains.
    Breakeven stop after +20bps eliminates zero-sum holds.
  RULES:
  - Same entry as VS-2 (ETH slope flip AND volume_btc_pct ≥ 0.80).
  - Once unrealized P&L reaches +20bps, move stop-loss to entry price (breakeven).
  - Exit early if price reverses to entry price after breakeven is triggered.
  - Otherwise hold up to 12 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      breakeven_trigger_bps: 20.0
  allowed_variants: []
  status: active
- id: H213
  family: exit_logic
  class: tradeable_hypothesis
  description: LQ-1 + Combo exit (ATR stop OR liq thesis invalidation, 8-bar max).
  rationale: Test whether combining ATR stop with liquidation-thesis exit improves
    LQ-1 returns by protecting against both technical stop-outs and fundamental
    thesis invalidation.
  WHY: LQ-1 faces two failure modes — price reversal (ATR stop) and cascade resolution
    (liq exit). Covering both should reduce tail losses.
  RULES:
  - Same entry as LQ-1 (onset of long_liq_btc_pct ≥ 0.90, direction=-1).
  - Exit early on ATR stop (1.5× ATR14) OR if long_liq_btc_pct drops below 0.50,
    whichever fires first.
  - Otherwise hold up to 8 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      atr_stop_mult: 1.5
      liq_exit_col: long_liq_btc_pct
      liq_exit_threshold: 0.5
  allowed_variants: []
  status: active
- id: H214
  family: exit_logic
  class: tradeable_hypothesis
  description: CA-1 + Volume collapse exit (exit if volume_btc_pct stays below p30 for 2 bars).
  rationale: Test whether exiting CA-1 trades when trading volume collapses below
    p30 for two consecutive bars improves returns by avoiding low-liquidity drift.
  WHY: CA-1 slope flips are driven by ETH momentum. Volume collapse during the
    hold suggests the momentum is exhausted — exit rather than holding for noise.
  RULES:
  - Same entry as CA-1 (ETH slope flip, non-zero sign).
  - Exit early if volume_btc_pct stays below 0.30 for 2 consecutive bars.
  - Otherwise hold up to 8 bars (time exit).
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      vol_collapse_col: volume_btc_pct
      vol_collapse_threshold: 0.3
      vol_collapse_bars: 2
  allowed_variants: []
  status: active
- id: H215
  family: direction_split
  class: tradeable_hypothesis
  description: CA-1 LONG-only direction split.
  rationale: Isolate whether CA-1's edge comes from bullish slope flips, bearish
    flips, or both equally. Critical given the 365d dataset was predominantly bullish.
  WHY: If LONG trades drive all the edge, SHORT entries should be suppressed in
    the paper trader. Must know before scaling deployment.
  RULES:
  - Same signal as CA-1 (ETH slope flip, non-zero). Only take entries where direction=+1.
  - Hold 8 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_signal: CA-1
      direction_filter: long_only
  allowed_variants: []
  status: active
- id: H216
  family: direction_split
  class: tradeable_hypothesis
  description: CA-1 SHORT-only direction split.
  rationale: Isolate whether CA-1 bearish flips carry independent edge or are
    diluting the signal with flat/negative returns.
  WHY: If SHORT trades fail independently, suppress them. If both directions pass,
    CA-1 is genuinely symmetric and full deployment is justified.
  RULES:
  - Same signal as CA-1 (ETH slope flip, non-zero). Only take entries where direction=-1.
  - Hold 8 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_signal: CA-1
      direction_filter: short_only
  allowed_variants: []
  status: active
- id: H217
  family: direction_split
  class: tradeable_hypothesis
  description: VS-2 LONG-only direction split.
  rationale: Isolate VS-2 bullish flip performance. VS-2 fires on high-volume slope
    flips — but are bullish flips more reliable than bearish in a trending market?
  WHY: If VS-2 LONGs dominate performance, bearish entries add noise. Gate direction
    in paper trader if SHORT side is weak.
  RULES:
  - Same signal as VS-2 (slope flip AND vol_btc_pct >= 0.80). Long entries only.
  - Hold 12 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      base_signal: VS-2
      direction_filter: long_only
  allowed_variants: []
  status: active
- id: H218
  family: direction_split
  class: tradeable_hypothesis
  description: VS-2 SHORT-only direction split.
  rationale: Isolate VS-2 bearish flip performance independently from bullish flips.
  WHY: VS-2 SHORTs in a bull market could be systematically weaker. If SHORT P>0
    fails, suppress all VS-2 short entries in deployed signals.
  RULES:
  - Same signal as VS-2 (slope flip AND vol_btc_pct >= 0.80). Short entries only.
  - Hold 12 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      base_signal: VS-2
      direction_filter: short_only
  allowed_variants: []
  status: active
- id: H219
  family: direction_split
  class: tradeable_hypothesis
  description: VS-3 LONG-only direction split.
  rationale: VS-3 is the best signal at 60bps. Determine if that edge is concentrated
    in bullish flips or is genuinely symmetric across both directions.
  WHY: VS-3 requires slope + volume + total liquidations — a very high bar. If
    LONGs and SHORTs both pass, VS-3 is robust. If only one direction passes, gate
    the other in the paper trader immediately.
  RULES:
  - Same signal as VS-3 (slope flip AND vol p80 AND total_liq p70). Long entries only.
  - Hold 12 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      base_signal: VS-3
      direction_filter: long_only
  allowed_variants: []
  status: active
- id: H220
  family: direction_split
  class: tradeable_hypothesis
  description: VS-3 SHORT-only direction split.
  rationale: Isolate whether VS-3 bearish flips independently justify deployment
    or whether the short side is being carried by the long side.
  WHY: In a bull market, forced short liquidations (total_liq spike) may correlate
    with snapback longs more than continuation shorts. Must verify independently.
  RULES:
  - Same signal as VS-3 (slope flip AND vol p80 AND total_liq p70). Short entries only.
  - Hold 12 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      base_signal: VS-3
      direction_filter: short_only
  allowed_variants: []
  status: active
- id: H221
  family: oi_liq
  class: tradeable_hypothesis
  description: LQ-1 restricted to Asia session (00:00–08:00 UTC).
  rationale: Liquidation cascades have known intraday clustering. Asia open is
    historically the highest-risk window for forced liquidations. Test whether
    LQ-1 edge is concentrated there.
  WHY: If LQ-1 works primarily in Asia hours, time-gating the paper trader to that
    window could improve Sharpe by eliminating weak off-hours entries.
  RULES:
  - Same signal as LQ-1 (onset of long_liq_btc_pct >= 0.90, direction=-1).
  - Only accept entries between 00:00 and 08:00 UTC.
  - Hold 8 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_signal: LQ-1
      utc_window: 00:00-08:00
  allowed_variants: []
  status: active
- id: H222
  family: oi_liq
  class: tradeable_hypothesis
  description: LQ-1 restricted to US session (12:00–20:00 UTC).
  rationale: Test whether LQ-1 liquidation cascade edge concentrates in the US
    trading session vs Asia hours.
  WHY: US session has higher institutional participation and different liquidation
    dynamics than Asia. Comparing H221 vs H222 tells us which session drives LQ-1.
  RULES:
  - Same signal as LQ-1 (onset of long_liq_btc_pct >= 0.90, direction=-1).
  - Only accept entries between 12:00 and 20:00 UTC.
  - Hold 8 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_signal: LQ-1
      utc_window: 12:00-20:00
  allowed_variants: []
  status: active
- id: H223
  family: oi_liq
  class: tradeable_hypothesis
  description: LQ-2 restricted to Asia session (00:00–08:00 UTC).
  rationale: Test whether LQ-2 short liquidation squeeze edge concentrates in Asia
    hours, mirroring the LQ-1 time-of-day hypothesis.
  WHY: Short squeezes may cluster in Asia where leveraged short positions built
    during off-hours get unwound at open. Gating to this window tests that theory.
  RULES:
  - Same signal as LQ-2 (onset of short_liq_btc_pct >= 0.90, direction=+1).
  - Only accept entries between 00:00 and 08:00 UTC.
  - Hold 8 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_signal: LQ-2
      utc_window: 00:00-08:00
  allowed_variants: []
  status: active
- id: H224
  family: oi_liq
  class: tradeable_hypothesis
  description: LQ-2 restricted to US session (12:00–20:00 UTC).
  rationale: Test whether LQ-2 short squeeze edge concentrates in the US session.
  WHY: Paired control for H223. Together H223/H224 reveal whether LQ-2 has a
    session-specific concentration or is uniformly distributed across the day.
  RULES:
  - Same signal as LQ-2 (onset of short_liq_btc_pct >= 0.90, direction=+1).
  - Only accept entries between 12:00 and 20:00 UTC.
  - Hold 8 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_signal: LQ-2
      utc_window: 12:00-20:00
  allowed_variants: []
  status: active
- id: H225
  family: oi_liq
  class: tradeable_hypothesis
  description: CA-1 + OI quality gate (oi_btc_pct >= 0.80).
  rationale: Test whether requiring elevated open interest at CA-1 slope flip entries
    improves edge by filtering to high-participation regime changes only.
  WHY: OI-gated slope flips (H176/OI-1) showed strong edge. Applying the same OI
    gate to CA-1 (broader, more frequent signal) tests whether OI is a universal
    quality filter for slope-based entries.
  RULES:
  - ETH slope flip (sign changes AND non-zero) AND oi_btc_pct >= 0.80.
  - Trade BTC in ETH slope direction. Hold 8 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      oi_btc_pct_min: 0.80
  allowed_variants: []
  status: active
- id: H226
  family: oi_liq
  class: tradeable_hypothesis
  description: CA-1 + funding contrarian gate (funding opposes slope direction).
  rationale: Test whether CA-1 slope flips are higher quality when funding rate
    opposes the direction — a contrarian positioning signal suggesting crowded trades
    are about to be squeezed out.
  WHY: Bullish slope flip with negative funding means bears are dominant in perps
    but trend is turning up — a squeeze setup. Bearish flip with positive funding
    means longs are crowded as trend turns down. Both are contrarian-confirmation entries.
  RULES:
  - ETH slope flip (sign changes AND non-zero) AND funding_btc_sign opposes slope direction.
  - Trade BTC in ETH slope direction. Hold 8 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      funding_gate: contrarian
  allowed_variants: []
  status: active
- id: H227
  family: oi_liq
  class: tradeable_hypothesis
  description: LQ-1 + slope confirmation (cascade onset AND ETH slope already bearish).
  rationale: Test whether requiring the ETH slope to already be bearish at LQ-1
    entry improves returns by only trading cascades that align with trend direction.
  WHY: LQ-1 fires on any long liquidation onset. Adding slope confirmation filters
    to cascades where the market was already trending down — the thesis is doubly
    confirmed. Counter-trend cascades (slope bullish, long liq spike) may be noise.
  RULES:
  - Onset of long_liq_btc_pct >= 0.90 AND eth_slope_sign_1h == -1 at entry.
  - Direction = -1 (short). Hold 8 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      long_liq_btc_pct_min: 0.90
      slope_confirmation: bearish
  allowed_variants: []
  status: active
- id: H228
  family: oi_liq
  class: tradeable_hypothesis
  description: LQ-2 + slope confirmation (squeeze onset AND ETH slope already bullish).
  rationale: Test whether requiring the ETH slope to already be bullish at LQ-2
    entry improves returns by only trading squeezes that align with trend direction.
  WHY: LQ-2 fires on any short liquidation onset. Adding slope confirmation filters
    to squeezes where the market was already trending up — the squeeze amplifies
    existing momentum rather than fighting a downtrend.
  RULES:
  - Onset of short_liq_btc_pct >= 0.90 AND eth_slope_sign_1h == +1 at entry.
  - Direction = +1 (long). Hold 8 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      short_liq_btc_pct_min: 0.90
      slope_confirmation: bullish
  allowed_variants: []
  status: active
- id: H188
  family: oi_liq
  class: tradeable_hypothesis
  description: LQ-1 at p80 threshold — lower bar, higher frequency test.
  rationale: LQ-1 fires at p90 (~4/day). Lowering to p80 doubles frequency but
    tests if the edge degrades. Builds the frequency/edge tradeoff curve.
  RULES:
  - Onset of long_liq_btc_pct >= 0.80. Direction = -1. Hold 8 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      long_liq_btc_pct_min: 0.80
  allowed_variants: []
  status: active
- id: H189
  family: oi_liq
  class: tradeable_hypothesis
  description: LQ-1 at p95 threshold — higher bar, rarer but stronger cascades.
  rationale: Only the most extreme long liquidation cascades. Expect higher per-trade
    edge but fewer trades. Tests whether rarity improves quality.
  RULES:
  - Onset of long_liq_btc_pct >= 0.95. Direction = -1. Hold 8 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      long_liq_btc_pct_min: 0.95
  allowed_variants: []
  status: active
- id: H190
  family: oi_liq
  class: tradeable_hypothesis
  description: LQ-2 at p80 threshold — lower bar, higher frequency short squeeze test.
  rationale: LQ-2 fires at p90. Lowering to p80 increases frequency. Tests if
    short squeeze edge degrades at a lower extremity threshold.
  RULES:
  - Onset of short_liq_btc_pct >= 0.80. Direction = +1. Hold 8 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      short_liq_btc_pct_min: 0.80
  allowed_variants: []
  status: active
- id: H191
  family: oi_liq
  class: tradeable_hypothesis
  description: LQ-1 with extended hold (h=12 bars, 60 minutes).
  rationale: LQ-1 at h=8 passes. Test whether the cascade effect sustains over
    60 minutes or whether the edge is front-loaded in the first 40 minutes.
  RULES:
  - Same signal as LQ-1 (onset of long_liq_btc_pct >= 0.90, direction=-1).
  - Hold 12 bars instead of 8.
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      long_liq_btc_pct_min: 0.90
  allowed_variants: []
  status: active
- id: H192
  family: oi_liq
  class: tradeable_hypothesis
  description: LQ-2 with extended hold (h=12 bars, 60 minutes).
  rationale: LQ-2 at h=8 passes. Test whether the short squeeze effect sustains
    over 60 minutes or whether upside is captured in the first 40 minutes.
  RULES:
  - Same signal as LQ-2 (onset of short_liq_btc_pct >= 0.90, direction=+1).
  - Hold 12 bars instead of 8.
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      short_liq_btc_pct_min: 0.90
  allowed_variants: []
  status: active
- id: H193
  family: oi_liq
  class: tradeable_hypothesis
  description: ETH long liquidation cascade predicts BTC SHORT.
  rationale: ETH and BTC liquidations are correlated but not identical. ETH liq
    may lead BTC moves. Tests cross-asset liquidation signal — if ETH longs get
    wiped, does BTC follow?
  RULES:
  - Onset of long_liq_eth_pct >= 0.90. Direction = -1 (short BTC). Hold 8 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      long_liq_eth_pct_min: 0.90
  allowed_variants: []
  status: active
- id: H194
  family: oi_liq
  class: tradeable_hypothesis
  description: ETH short liquidation squeeze predicts BTC LONG.
  rationale: Mirror of H193. If ETH shorts get squeezed, does BTC rally?
    Cross-asset liquidation signal — ETH squeeze as a BTC entry trigger.
  RULES:
  - Onset of short_liq_eth_pct >= 0.90. Direction = +1 (long BTC). Hold 8 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      short_liq_eth_pct_min: 0.90
  allowed_variants: []
  status: active
- id: H195
  family: oi_liq
  class: tradeable_hypothesis
  description: Liquidation imbalance >= 0.80 (long-side dominant) → SHORT.
  rationale: Tests whether the ratio of long-to-total liquidations is more
    informative than raw magnitude. A skewed imbalance (80%+ of liq is longs)
    suggests one-sided forced selling independent of total volume.
  RULES:
  - Onset of liq_imbalance_btc >= 0.80 (long liq / total liq). Direction = -1. Hold 8 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      liq_imbalance_btc_min: 0.80
  allowed_variants: []
  status: active
- id: H196
  family: oi_liq
  class: tradeable_hypothesis
  description: Cross-asset liq cascade — BTC AND ETH long liq both elevated → SHORT.
  rationale: When both BTC and ETH long liquidations spike simultaneously, the
    cascade is market-wide rather than asset-specific. Test if joint confirmation
    produces a stronger/more reliable SHORT signal than BTC alone.
  RULES:
  - Onset of long_liq_btc_pct >= 0.90 AND long_liq_eth_pct >= 0.70.
  - Direction = -1. Hold 8 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      long_liq_btc_pct_min: 0.90
      long_liq_eth_pct_min: 0.70
  allowed_variants: []
  status: active
- id: H197
  family: oi_liq
  class: tradeable_hypothesis
  description: OI-gated slope flip + liq confirmation (OI-1 + liq gate).
  rationale: OI-1 (H176) has day-asymmetric edge. Adding total liquidation
    confirmation filters to bars where leveraged positions are being forced out,
    potentially removing the regime-asymmetry weakness.
  RULES:
  - ETH slope flip AND oi_btc_pct >= 0.80 AND total_liq_btc_pct >= 0.70.
  - Trade in ETH slope direction. Hold 8 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      oi_btc_pct_min: 0.80
      total_liq_btc_pct_min: 0.70
  allowed_variants: []
  status: active
- id: H229
  family: regime_conditioning
  class: tradeable_hypothesis
  description: CA-1 conditioned on TRENDING HMM regime.
  rationale: Slope flips during a trending regime should be the highest-quality
    entries — the market has directional momentum and the flip is a continuation
    signal, not noise in a choppy environment.
  WHY: If CA-1 edge is concentrated in TRENDING, gate the paper trader to only
    fire CA-1 during TRENDING regime. Could dramatically improve Sharpe.
  RULES:
  - Same signal as CA-1 (ETH slope flip, non-zero) AND hmm_regime == TRENDING.
  - Hold 8 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_signal: CA-1
      regime_gate: TRENDING
  allowed_variants: []
  status: active
- id: H230
  family: regime_conditioning
  class: tradeable_hypothesis
  description: CA-1 conditioned on RANGING HMM regime (expected failure — control).
  rationale: In a ranging market slope flips are noise — price oscillates without
    follow-through. This is the control hypothesis; expected to fail.
  WHY: If H230 fails and H229 passes, regime gating is valid and the RANGING regime
    should suppress CA-1 entries in the paper trader.
  RULES:
  - Same signal as CA-1 (ETH slope flip, non-zero) AND hmm_regime == RANGING.
  - Hold 8 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_signal: CA-1
      regime_gate: RANGING
  allowed_variants: []
  status: active
- id: H231
  family: regime_conditioning
  class: tradeable_hypothesis
  description: CA-1 conditioned on VOLATILE HMM regime.
  rationale: VOLATILE regime is ambiguous — high vol could amplify slope flip edge
    (big moves follow flips) or destroy it (noisy, fast reversals). Must test.
  WHY: If VOLATILE regime is where CA-1 makes or loses most of its money, that
    changes risk management and position sizing decisions.
  RULES:
  - Same signal as CA-1 (ETH slope flip, non-zero) AND hmm_regime == VOLATILE.
  - Hold 8 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      base_signal: CA-1
      regime_gate: VOLATILE
  allowed_variants: []
  status: active
- id: H232
  family: regime_conditioning
  class: tradeable_hypothesis
  description: VS-2 conditioned on TRENDING HMM regime.
  rationale: VS-2 adds volume confirmation to CA-1. In a TRENDING regime with
    elevated volume, a slope flip should be extremely high conviction.
  WHY: VS-3 is the best signal at 60bps. VS-2 in TRENDING might approach that
    level without requiring liquidation data, making it more robust.
  RULES:
  - Same signal as VS-2 (slope flip AND vol_btc_pct >= 0.80) AND hmm_regime == TRENDING.
  - Hold 12 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      base_signal: VS-2
      regime_gate: TRENDING
  allowed_variants: []
  status: active
- id: H233
  family: regime_conditioning
  class: tradeable_hypothesis
  description: VS-2 conditioned on VOLATILE HMM regime.
  rationale: VS-2 already uses a volume gate (p80). VOLATILE regime is also
    high-volume by definition. Test whether VS-2 in VOLATILE double-counts the
    volume signal or uncovers regime-specific edge.
  WHY: If VOLATILE + high volume + slope flip is better than TRENDING + high volume
    + slope flip, that changes when to size up VS-2 entries.
  RULES:
  - Same signal as VS-2 (slope flip AND vol_btc_pct >= 0.80) AND hmm_regime == VOLATILE.
  - Hold 12 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      base_signal: VS-2
      regime_gate: VOLATILE
  allowed_variants: []
  status: active
- id: H234
  family: regime_conditioning
  class: tradeable_hypothesis
  description: VS-3 conditioned on TRENDING HMM regime — highest conviction hypothesis.
  rationale: VS-3 is the best signal (60bps, triple-gated). Restricting to TRENDING
    regime adds a fourth gate — only trade when market structure, volume, liquidations,
    AND slope all align. Potentially the strongest signal in the entire catalog.
  WHY: If VS-3 already works everywhere, TRENDING-only may reduce trade count too
    much. But if edge is regime-concentrated, this unlocks a very high Sharpe subset.
  RULES:
  - Same signal as VS-3 (slope flip AND vol p80 AND total_liq p70) AND hmm_regime == TRENDING.
  - Hold 12 bars, trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 12
    fixed:
      base_signal: VS-3
      regime_gate: TRENDING
  allowed_variants: []
  status: active
- id: H235
  family: mean_reversion
  class: tradeable_hypothesis
  description: VWAP z-score extreme pullback LONG on 365d dataset.
  rationale: Pure VWAP mean reversion has never been tested on the full 365d
    dataset. When BTC price is more than 2 standard deviations below the session
    VWAP, test if it reverts over 6 bars (~30 min).
  WHY: Mean reversion worked in some 180d windows but was inconclusive. 365d provides
    more statistical power. No regime gate — tests the raw mechanism first.
  RULES:
  - vwap_z < -2.0. Direction = +1 (long). Hold 6 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 6
    fixed:
      vwap_z_entry_max: -2.0
  allowed_variants: []
  status: active
- id: H236
  family: mean_reversion
  class: tradeable_hypothesis
  description: VWAP z-score extreme extension SHORT on 365d dataset.
  rationale: Mirror of H235. When price is more than 2 standard deviations above
    the session VWAP, test if it reverts (shorts) over 6 bars.
  WHY: Paired control for H235. Together they reveal whether VWAP MR is symmetric
    or direction-biased on the 365d bull-market-dominated dataset.
  RULES:
  - vwap_z > +2.0. Direction = -1 (short). Hold 6 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 6
    fixed:
      vwap_z_entry_min: 2.0
  allowed_variants: []
  status: active
- id: H237
  family: vol_compression
  class: tradeable_hypothesis
  description: Vol compression breakout gate — CA-1 gated by BTC RV compressed 4+ hours.
  rationale: When BTC realized volatility has been below its p20 for 4+ consecutive
    hours, price is coiling. The next directional move (triggered by ETH slope flip)
    should be stronger than average because suppressed vol releases suddenly. Tests
    whether vol compression is an independent quality gate on CA-1, similar to how
    volume (VS-1) and liquidations (LQ-3) improved the base slope flip signal.
  WHY: If vol compression is just a proxy for low volume, it adds nothing beyond
    VS-1. Expected failure mode — but if RV compression captures a different regime
    than volume (e.g. thin-book coiling vs genuine low participation), it may select
    higher-conviction entries independently.
  RULES:
  - BTC 1h realized vol (RV) has been below the rolling p20 for 4 or more consecutive hours.
  - AND ETH 1h EMA20 slope changes direction (same trigger as CA-1).
  - Trade in direction of slope flip. Hold 8 bars (40 min). Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      rv_compressed_hours: 4
      rv_percentile_threshold: 20
      slope_signal: CA-1
  allowed_variants: []
  status: active
- id: H238
  family: funding_regime
  class: tradeable_hypothesis
  description: Sustained funding persistence SHORT — crowded-long unwind signal.
  rationale: When BTC perpetual funding has been continuously positive for 6+ hours,
    the market is structurally crowded long. Everyone is paying to hold longs. Any
    weakness triggers cascading exits — not just forced liquidations, but voluntary
    position closures too. Tests whether sustained funding persistence is an
    independent directional predictor, entirely separate from momentum signals.
  WHY: |
    Previous funding tests (H121-H144) used instantaneous funding extremes.
    This is different — it tests the PERSISTENCE of positive funding as a crowding
    signal. Expected failure mode: funding can stay positive for days in a bull
    trend, firing into the trend and losing. This is the cleanest test of whether
    crowded-long positioning predicts mean reversion at 40-minute horizons.
  RULES:
  - BTC perpetual funding rate has been positive for 6 or more consecutive hours.
  - No slope or volume gate — funding persistence alone is the signal.
  - Direction = SHORT (fading the crowded long). Hold 8 bars (40 min). Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      funding_positive_hours: 6
  allowed_variants: []
  status: active
- id: H239
  family: oi_liq
  class: tradeable_hypothesis
  description: OI expansion trend cascade — active leverage buildup + bearish slope flip.
  rationale: OI actively growing for 3+ consecutive hours means new leveraged positions
    are being opened continuously — not just that leverage exists, but that it is
    actively accumulating. When that OI expansion coincides with a bearish ETH slope
    flip, those newly-added longs are immediately offside and the cascade is more
    violent. Tests OI trend (slope of OI) rather than OI level, which failed in H176
    due to day-asymmetry.
  WHY: |
    H176 (OI level gate) failed even-day robustness. The hypothesis here is that
    OI expansion TREND is a different and cleaner signal than OI level — it captures
    the dynamic of leverage actively building rather than leverage sitting static.
    Expected failure mode: same Gate.io OI data quality issues as H176.
  RULES:
  - BTC OI has increased in each of the last 3 consecutive 1-hour periods (OI expanding trend).
  - AND ETH 1h EMA20 slope flips to bearish (same trigger as CA-1 SHORT side).
  - Direction = SHORT. Hold 8 bars (40 min). Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      oi_expanding_hours: 3
      slope_signal: CA-1
      direction: SHORT
  allowed_variants: []
  status: active
- id: H240
  family: oi_liq
  class: tradeable_hypothesis
  description: LQ-1 h=12 — 1-bar execution lag replication.
  rationale: H191 (LQ-1 extended to 12-bar hold) passed at 31.0bps bps8 WF (16/17
    folds). This is the execution lag replication — confirms edge survives next-bar
    entry, making it deployment-realistic. Required before LQ-4 shortcode is assigned.
  WHY: LQ-1 at h=8 already survived lag (H184). H191 extends the hold. If the
    cascade continues for 60 minutes, the 1-bar lag cost should be small relative
    to the extended edge capture.
  RULES:
  - Same as H191 (long_liq_btc_pct >= 0.90, onset, SHORT) but entry on the bar
    AFTER the signal fires (1-bar execution lag).
  - Hold 12 bars (60 min). Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 12
    entry_lag: 1
    fixed:
      base_signal: LQ-1
      hold_bars: 12
  allowed_variants: []
  status: active
- id: H241
  family: oi_liq
  class: tradeable_hypothesis
  description: LQ-2 h=12 — 1-bar execution lag replication.
  rationale: H192 (LQ-2 extended to 12-bar hold) passed at 21.2bps bps8 WF (17/17
    folds). Execution lag replication required before LQ-5 shortcode is assigned.
  WHY: LQ-2 at h=8 survived lag (H185, marginal). H192 extends the hold to 12 bars.
    The extended capture window should absorb the 1-bar lag cost more easily than
    the h=8 version did.
  RULES:
  - Same as H192 (short_liq_btc_pct >= 0.90, onset, LONG) but entry on the bar
    AFTER the signal fires (1-bar execution lag).
  - Hold 12 bars (60 min). Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 12
    entry_lag: 1
    fixed:
      base_signal: LQ-2
      hold_bars: 12
  allowed_variants: []
  status: active
- id: H243
  family: oi_liq
  class: tradeable_hypothesis
  description: Long-liq-dominant cascade → SHORT (ML-surfaced liq_imbalance_dir signal).
  rationale: |
    XGBoost round 2 (2026-03-11) ranked liq_imbalance_dir #4 (CV=0.22, BEARISH),
    the strongest NEW ★ feature. The feature = (long_liq / total_liq) - 0.5.
    When long liquidations dominate the cascade, selling pressure persists.
    Distinct from LQ-1 which fires at cascade COMPLETION (extreme event, bounce
    incoming). This tests cascade PERSISTENCE: while longs are still getting
    forced out, keep shorting.
  WHY: |
    Mechanism: when the bulk of liquidations are forced long-sellers (not short
    covers), the waterfall of margin calls has not yet exhausted itself. Forced
    sellers do not care about price — they must exit. The ML found this as the
    most consistent new predictor of negative 40-min returns.
  RULES:
  - total_liq_btc_pct >= 0.70 (above-average liquidation activity, not noise).
  - liq_imbalance_btc >= 0.65 (>=65% of liq is long-side = dominant selling).
  - Onset: condition becomes true (not continuous). Direction = SHORT (-1).
  - Hold 8 bars. Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
  allowed_variants: []
  status: active
- id: H244
  family: oi_liq
  class: tradeable_hypothesis
  description: OI acceleration (velocity spike) → LONG (ML-surfaced oi_velocity signal).
  rationale: |
    XGBoost round 2 (2026-03-11) ranked oi_velocity #10 (CV=0.33, BULLISH).
    oi_velocity = rate of change of OI change = second derivative of OI. When
    OI is not just growing but growing FASTER, fresh speculative leverage is
    entering at an accelerating pace. This is distinct from H239 (OI trending
    up 3 consecutive hours, failed) — velocity captures the burst, not the trend.
  WHY: |
    Mechanism: accelerating OI = a rush of new leveraged longs entering the
    market. These buyers are committed and add upward pressure. Trend continuation
    is likely while the burst persists. The 40-min window captures the immediate
    momentum before positions stabilize. Pure standalone test — no slope gate —
    to see if the ML finding reflects a real mechanism or noise.
  RULES:
  - oi_velocity = (OI_now - OI_1h_ago) - (OI_1h_ago - OI_2h_ago).
  - oi_velocity onset: crosses above its 80th percentile rolling threshold (288-bar window).
  - Direction = LONG (+1). Hold 8 bars. Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
  allowed_variants: []
  status: active
- id: H245
  family: cross_asset_divergence
  class: tradeable_hypothesis
  description: BTC leads ETH on 1h return → LONG (ML-surfaced btc_eth_div_1h signal).
  rationale: |
    XGBoost round 2 (2026-03-11) ranked btc_eth_div_1h #11 (CV=0.35, BULLISH).
    btc_eth_div_1h = BTC 1h return - ETH 1h return. When BTC outperforms ETH on
    the prior hour, the next 40 minutes are bullish. This is the REVERSE of the
    CA family (which uses ETH leading BTC). BTC leadership may signal institutional
    flow or index-driven buying — a cleaner, more committed move.
  WHY: |
    Mechanism: when BTC leads ETH to the upside, it suggests large-cap
    buyers are driving the market (ETH follows later). When ETH leads, it
    is more often retail/speculative. BTC leadership → stronger continuation.
    This is a NEW mechanism independent of ETH slope flip — tests whether
    the leadership DIRECTION matters, not just the direction itself.
  RULES:
  - btc_eth_div_1h = (close_btc / close_btc.shift(12)) - (close_eth / close_eth.shift(12)).
  - Onset: btc_eth_div_1h flips from <= 0 to > 0 AND btc_eth_div_1h > 0.001 (0.1% divergence threshold).
  - Direction = LONG (+1). Hold 8 bars. Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
  allowed_variants: []
  status: active
- id: H242
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: CA-1 (ETH slope flip) gated by BTC below 48h VWAP — ML-surfaced filter test.
  rationale: |
    XGBoost discovery (2026-03-11) ranked dist_to_vwap48_z_btc as the #2 most
    consistently predictive feature (CV=0.23, BULLISH direction), independent of
    all CA/VS/LQ proxies. ML is saying "lower BTC VWAP deviation -> better forward
    returns." This hypothesis tests whether that signal improves CA-1 when used as
    a simple ENTRY GATE: only trade CA-1 when BTC is below its 48h VWAP (z < 0).
  WHY: |
    Mechanism: BTC consolidating below VWAP suggests the prior trend has not fully
    played out. An ETH slope flip in this context may signal resumption of that
    unfulfilled move, rather than a random flip from an extended position. When BTC
    is extended above VWAP (z > 0), the flip may be local exhaustion instead.
  OVER_MINING_WARNING: |
    HIGH RISK of over-mining. This is CA-1 with one additional gate. The CA family
    has been mined extensively (H59-H99+). This test is justified ONLY because the
    gate was surfaced by an independent ML run on held-out data, not by looking at
    CA-1 returns directly. If this fails, do not iterate on VWAP thresholds. One
    test is sufficient to answer whether VWAP-gating improves CA-1.
  RULES:
  - ETH slope flips (eth_slope_sign_1h changes sign, != 0). Direction follows flip.
  - ONLY fire when dist_to_vwap48_z_btc < 0 (BTC below its 48-bar VWAP).
  - Hold 8 bars. Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
    fixed:
      vwap_gate: below_zero
      base_signal: CA-1
  allowed_variants: []
  status: active
- id: H259
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: H252 (OI velocity gate on CA-1, h=24) — 1-bar execution lag replication.
  rationale: |
    H252 (OI velocity gate on CA-1, h=24) PASSed 17/19 folds at 18.4 bps net.
    H251 (h=12) also PASSed 16/19 at 16.8 bps. h=24 is stronger and more consistent.
    Lag replication required before shortcode assignment.
  WHY: Same mechanism as H250/H251/H252. Tests execution realism only.
  RULES:
  - Same as H252: oi_velocity > 0 AND ETH slope flip. Direction follows flip.
  - Entry on bar AFTER signal fires (1-bar execution lag). Hold 24 bars (2h).
  parameters:
    lookback_days: 365
    horizon_bars: 24
    entry_lag: 1
  allowed_variants: []
  status: active
- id: H261
  family: mean_reversion
  class: tradeable_hypothesis
  description: ETH 4h return exhaustion SHORT, h=48 (4h hold). ML-surfaced via XGBoost SHAP round 3.
  rationale: |
    XGBoost SHAP ranked ret_4h_eth #7 at h=4, #9 at h=8, #14 at h=48 across multi-horizon run.
    Direction: BULLISH at short horizons (h=4/8) but BEARISH at longer horizons (h=16/48, CV=0.45).
    The BEARISH direction at h=48 = exhaustion thesis: when ETH has rallied strongly over the past
    4 hours, the move tends to reverse over the following 4 hours. Different from H253-H255 which
    tested BTC 4h return at h=8/12/24 (all failed) — this uses ETH return at a longer 4h hold.
    ETH often leads BTC on directional moves; ETH exhaustion may be a cleaner reversal signal.
  WHY: One test only per ML policy. If this fails, 4h exhaustion thesis is dead for both BTC and ETH.
  RULES:
  - Compute ETH 4h return = (close_eth - close_eth.shift(48)) / close_eth.shift(48)
  - When ret_4h_eth >= rolling p80 (1-year lookback), go BTC SHORT for 48 bars (4 hours).
  - No other filters.
  parameters:
    lookback_days: 365
    horizon_bars: 48
    entry_lag: 0
  allowed_variants: []
  status: active

- id: H260
  family: cross_asset_divergence
  class: tradeable_hypothesis
  description: H257 (BTC-ETH corr decoupling + ETH flip, h=12) — 1-bar execution lag replication.
  rationale: |
    H257 (corr decoupling + ETH slope flip, h=12) PASSed 15/18 folds at 14.1 bps net.
    Lag replication required before shortcode assignment.
  WHY: Same mechanism as H256/H257/H258. Tests execution realism only.
  RULES:
  - Same as H257: btc_eth_corr_2h < p20 AND ETH slope flip. Direction follows flip.
  - Entry on bar AFTER signal fires (1-bar execution lag). Hold 12 bars (60 min).
  parameters:
    lookback_days: 365
    horizon_bars: 12
    entry_lag: 1
  allowed_variants: []
  status: active
- id: H250
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: OI velocity gate on CA-1, h=8 (40 min).
  rationale: |
    XGBoost multi-horizon (2026-03-11): oi_velocity ranked 4/4 horizons consistent
    (avg rank 12, only feature hitting ALL horizons). H244 tested oi_velocity as
    standalone LONG and failed. The signal may only work WITH a directional trigger.
    This tests OI acceleration as a GATE on CA-1: only trade the slope flip when OI
    is accelerating (fresh leverage entering faster and faster).
  WHY: |
    Accelerating OI = a burst of new leveraged longs entering right now. Combined
    with an ETH slope flip (directional trigger), these new participants add fuel
    to the move. Standalone OI velocity has no direction — it needs CA-1 to supply
    the direction filter.
  RULES:
  - ETH slope flips (eth_slope_sign_1h changes, != 0). Direction follows flip.
  - oi_velocity > 0: OI change in last hour exceeds OI change in prior hour.
  - Both conditions true simultaneously = entry. Hold 8 bars.
  parameters:
    lookback_days: 365
    horizon_bars: 8
  allowed_variants: []
  status: active
- id: H251
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: OI velocity gate on CA-1, h=12 (60 min).
  rationale: H250 at h=12. Horizon sweep from launch — liq signals got stronger at h=12.
  WHY: Same as H250. Only horizon changes.
  RULES:
  - Same as H250. Hold 12 bars (60 min).
  parameters:
    lookback_days: 365
    horizon_bars: 12
  allowed_variants: []
  status: active
- id: H252
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: OI velocity gate on CA-1, h=24 (2h).
  rationale: H250 at h=24. Horizon sweep from launch.
  WHY: Same as H250. Only horizon changes.
  RULES:
  - Same as H250. Hold 24 bars (2h).
  parameters:
    lookback_days: 365
    horizon_bars: 24
  allowed_variants: []
  status: active
- id: H253
  family: mean_reversion
  class: tradeable_hypothesis
  description: 4h BTC return exhaustion SHORT, h=8 (40 min).
  rationale: |
    XGBoost multi-horizon (2026-03-11): ret_4h_btc ranked consistently BEARISH
    at h4=#8, h8=#9, h16=#11, h48=#11 (3/4 consistent). When BTC has a strong
    4h upward run, the next intraday move tends negative — momentum exhaustion /
    mean reversion at the 4h timeframe. Never directly tested in this pipeline.
  WHY: |
    After a large 4h up-move, leveraged longs are extended and profit-taking
    sellers appear. The market tends to retrace part of the move. The 5m signal
    fires at the peak of the 4h move — before the retrace has started.
  RULES:
  - ret_4h_btc = BTC 4h return (pct_change(48)).
  - Onset: ret_4h_btc enters top quintile (> rolling 80th percentile, 288-bar window).
  - Direction = SHORT (-1). Hold 8 bars. Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
  allowed_variants: []
  status: active
- id: H254
  family: mean_reversion
  class: tradeable_hypothesis
  description: 4h BTC return exhaustion SHORT, h=12 (60 min).
  rationale: H253 at h=12. Horizon sweep from launch.
  WHY: Same as H253. Only horizon changes.
  RULES:
  - Same as H253. Hold 12 bars (60 min).
  parameters:
    lookback_days: 365
    horizon_bars: 12
  allowed_variants: []
  status: active
- id: H255
  family: mean_reversion
  class: tradeable_hypothesis
  description: 4h BTC return exhaustion SHORT, h=24 (2h).
  rationale: H253 at h=24. Horizon sweep from launch.
  WHY: Same as H253. Only horizon changes.
  RULES:
  - Same as H253. Hold 24 bars (2h).
  parameters:
    lookback_days: 365
    horizon_bars: 24
  allowed_variants: []
  status: active
- id: H256
  family: cross_asset_divergence
  class: tradeable_hypothesis
  description: BTC-ETH 2h correlation decoupling + ETH slope flip, h=8 (40 min).
  rationale: |
    XGBoost multi-horizon (2026-03-11): btc_eth_corr_2h ranked 3/4 horizons
    consistent (avg rank 16.5, BEARISH). When BTC and ETH decouple (2h rolling
    correlation drops to its own low), it signals unusual conditions. Combined with
    an ETH slope flip, it may indicate ETH is making an idiosyncratic move that BTC
    has not yet confirmed — a leading indicator of a sharper-than-normal follow.
  WHY: |
    When BTC and ETH are highly correlated (normal), an ETH flip just means noise.
    When they decouple, ETH is moving for a specific reason (large ETH-native event,
    funding imbalance, liquidation cascade on one asset only). A slope flip during
    decoupling is a higher-conviction signal than a normal flip.
  RULES:
  - btc_eth_corr_2h = rolling 24-bar (2h) correlation of 5m returns.
  - Decoupling: btc_eth_corr_2h < its own rolling 30d 20th percentile.
  - ETH slope flips (eth_slope_sign_1h changes, != 0). Direction follows flip.
  - Both true simultaneously = entry. Hold 8 bars. Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 8
  allowed_variants: []
  status: active
- id: H257
  family: cross_asset_divergence
  class: tradeable_hypothesis
  description: BTC-ETH 2h correlation decoupling + ETH slope flip, h=12 (60 min).
  rationale: H256 at h=12. Horizon sweep from launch.
  WHY: Same as H256. Only horizon changes.
  RULES:
  - Same as H256. Hold 12 bars (60 min).
  parameters:
    lookback_days: 365
    horizon_bars: 12
  allowed_variants: []
  status: active
- id: H258
  family: cross_asset_divergence
  class: tradeable_hypothesis
  description: BTC-ETH 2h correlation decoupling + ETH slope flip, h=24 (2h).
  rationale: H256 at h=24. Horizon sweep from launch.
  WHY: Same as H256. Only horizon changes.
  RULES:
  - Same as H256. Hold 24 bars (2h).
  parameters:
    lookback_days: 365
    horizon_bars: 24
  allowed_variants: []
  status: active
- id: H249
  family: oi_liq
  class: tradeable_hypothesis
  description: H247 (liq imbalance SHORT h=12) — 1-bar execution lag replication.
  rationale: |
    H247 (long-liq-dominant cascade SHORT, h=12) PASSed 17/17 folds at 27.4 bps
    net. Before assigning shortcode LQ-6, must verify signal survives realistic
    execution: enter on the bar AFTER the signal fires (1-bar lag = 5 min delay).
    LQ-1 through LQ-5 all required this step.
  WHY: Same mechanism as H243/H247. Tests execution realism only.
  RULES:
  - Same as H247: total_liq_btc_pct >= 0.70, liq_imbalance_btc >= 0.65, SHORT onset.
  - Entry on bar AFTER signal fires (1-bar execution lag).
  - Hold 12 bars (60 min). Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 12
    entry_lag: 1
    fixed:
      base_signal: H247
      hold_bars: 12
  allowed_variants: []
  status: active
- id: H246
  family: oi_liq
  class: tradeable_hypothesis
  description: H243 horizon sensitivity — liq imbalance SHORT at h=4 (20 min).
  rationale: |
    H243 (liq_imbalance_dir, h=8) PASSed with 16/17 folds positive, 9.8 bps net.
    Horizon sensitivity test: does the selling pressure from a long-dominant
    liquidation cascade resolve in 20 minutes, or does it need longer?
    A real mechanism should work across a range of hold periods.
  WHY: Same signal as H243. Only horizon changes.
  RULES:
  - Same as H243: total_liq_btc_pct >= 0.70, liq_imbalance_btc >= 0.65, SHORT onset.
  - Hold 4 bars (20 min). Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 4
  allowed_variants: []
  status: active
- id: H247
  family: oi_liq
  class: tradeable_hypothesis
  description: H243 horizon sensitivity — liq imbalance SHORT at h=12 (60 min).
  rationale: |
    H243 (liq_imbalance_dir, h=8) PASSed with 16/17 folds positive, 9.8 bps net.
    Horizon sensitivity test: does the cascade persist for a full hour?
    If it does, the signal has a longer window of opportunity and is more robust.
  WHY: Same signal as H243. Only horizon changes.
  RULES:
  - Same as H243: total_liq_btc_pct >= 0.70, liq_imbalance_btc >= 0.65, SHORT onset.
  - Hold 12 bars (60 min). Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 12
  allowed_variants: []
  status: active
- id: H248
  family: oi_liq
  class: tradeable_hypothesis
  description: H243 horizon sensitivity — liq imbalance SHORT at h=24 (2h).
  rationale: |
    H243 (liq_imbalance_dir, h=8) PASSed with 16/17 folds positive, 9.8 bps net.
    Horizon sensitivity test: does the cascade have a 2-hour shadow?
    Liquidation cascades on leveraged crypto can take hours to fully unwind.
    If h=24 also PASSes, the signal is robustly useful, not a 40-min artefact.
  WHY: Same signal as H243. Only horizon changes.
  RULES:
  - Same as H243: total_liq_btc_pct >= 0.70, liq_imbalance_btc >= 0.65, SHORT onset.
  - Hold 24 bars (2h). Trade BTC.
  parameters:
    lookback_days: 365
    horizon_bars: 24
  allowed_variants: []
  status: active

- id: H262
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: OV-1 LONG isolation — OI accelerating + ETH slope flips UP → LONG, h=24.
  rationale: |
    OV-1 (H252) confirmed 18.4 bps net at h=24 firing in both directions via eth_sign.
    OV-1 was ML-surfaced as BEARISH — the SHORT side is what drove the discovery.
    This tests whether the LONG side (OI accelerating + bullish ETH flip) also has
    independent edge, or if the OV-1 aggregate is driven entirely by SHORT performance.
    If LONG side passes, we have a Coinbase-tradeable version of the OV mechanism.
  WHY: |
    OI accelerating + bullish ETH flip = fresh leveraged longs entering with directional
    momentum. Fuel + direction filter. Economically distinct from just watching the flip.
    ONE TEST ONLY — if LONG side fails, the OV mechanism is SHORT-only.
  RULES:
  - ETH slope flips UP (eth_slope_sign_1h changes from <= 0 to +1).
  - OI acceleration: oi_chg_now > oi_chg_prev (OI grew faster in last hour than prior hour).
  - Both true simultaneously = LONG entry. Hold 24 bars (2h).
  parameters:
    lookback_days: 365
    horizon_bars: 24
    entry_lag: 0
  allowed_variants: []
  status: active

- id: H263
  family: cross_asset_divergence
  class: tradeable_hypothesis
  description: CD-1 LONG isolation — corr decoupling + ETH slope flips UP → LONG, h=12.
  rationale: |
    CD-1 (H257) confirmed 14.1 bps net at h=12 firing in both directions via eth_sign.
    CD-1 was ML-surfaced as BEARISH — the SHORT side drove the discovery.
    This tests whether the LONG side (low BTC-ETH corr + bullish ETH flip) also has
    independent edge, or if the CD-1 aggregate is dominated by SHORT performance.
    If LONG side passes, we have a Coinbase-tradeable version of the CD mechanism.
  WHY: |
    Corr decoupling means BTC and ETH are diverging — one is moving independently.
    When ETH then flips UP during a decoupling event, it may signal ETH (and BTC)
    reasserting upward direction. Economically symmetric to the SHORT version.
    ONE TEST ONLY — if LONG side fails, the CD mechanism is SHORT-only.
  RULES:
  - BTC-ETH 2h rolling correlation below its 30d p20 (decoupling event).
  - ETH slope flips UP (eth_slope_sign_1h changes from <= 0 to +1).
  - Both true simultaneously = LONG entry. Hold 12 bars (1h).
  parameters:
    lookback_days: 365
    horizon_bars: 12
    entry_lag: 0
  allowed_variants: []
  status: active

- id: H265
  family: cross_asset_regime
  class: tradeable_hypothesis
  description: H262 (OV-1 LONG, h=24) — 1-bar execution lag replication.
  rationale: |
    H262 (OI accel + ETH flip UP → LONG, h=24) PASSed 13/19 folds at 18.3 bps net.
    Standard lag replication: enter on close[t+1] instead of close[t].
    If edge persists with 1-bar lag, signal is execution-realistic and OV-2 is confirmed.
  WHY: Same as H262. Only entry timing changes.
  RULES:
  - Same as H262: OI accelerating + ETH slope flips UP → LONG.
  - Entry on close[t+1] (1-bar execution lag). Hold 24 bars from entry.
  parameters:
    lookback_days: 365
    horizon_bars: 24
    entry_lag: 1
  allowed_variants: []
  status: active

- id: H264
  family: oi_liq
  class: tradeable_hypothesis
  description: LQ-6 LONG counterpart — active liq + short-liq dominant → LONG, h=12.
  rationale: |
    LQ-6 (H247) fires SHORT when total liq is active AND long-liq dominates (>= 65%).
    This tests the mirror: when liq is active AND short-liq dominates (<= 35% long),
    do short squeezes drive price up? Short liquidations = forced short covering = demand.
    LQ-2/LQ-5 test raw short_liq_btc percentile; this uses the signed imbalance ratio
    (liq_imbalance_btc) — a slightly different feature capturing relative dominance.
  WHY: |
    Forced short covering creates mechanical buy pressure. If short liquidations are
    dominant during an active liq event, the path of least resistance is up.
    ONE TEST ONLY at h=12 (matching LQ-6 confirmed horizon). Do not iterate threshold.
  RULES:
  - total_liq_btc_pct >= 0.70 (active liquidation event).
  - liq_imbalance_btc <= 0.35 (short-liq dominant: <= 35% of liq is long-side).
  - Onset (first bar of condition) = LONG entry. Hold 12 bars (1h).
  parameters:
    lookback_days: 365
    horizon_bars: 12
    entry_lag: 0
  allowed_variants: []
  status: active

- id: H266
  family: oi_liq
  class: tradeable_hypothesis
  description: H264 (LQ-6 LONG, h=12) — 1-bar execution lag replication.
  rationale: |
    H264 (short-liq dominant → LONG, h=12) PASSed 17/17 folds at 27.2 bps net.
    Standard lag replication: enter on close[t+1] instead of close[t].
    If edge persists with 1-bar lag, signal is execution-realistic and LQ-7 is confirmed.
  WHY: Same as H264. Only entry timing changes.
  RULES:
  - Same as H264: total_liq_btc_pct >= 0.70, liq_imbalance_btc <= 0.35, onset → LONG.
  - Entry on close[t+1] (1-bar execution lag). Hold 12 bars from entry.
  parameters:
    lookback_days: 365
    horizon_bars: 12
    entry_lag: 1
  allowed_variants: []
  status: active
